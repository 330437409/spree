# Store API v3 gaps for the mini program

> **Fork-only plan.** Ours, never submitted to `spree/spree`. The tracked
> plans beside it are upstream's and are read-only to us; our decisions are in
> `fork-decisions.md`, never in upstream's `decisions.md`.

**Status:** In progress — gap inventory complete and no question outstanding; **steps 1 to 6 are built** (2026-09-21), each recorded on its own row: the catalogue reads and the share payload, the cart's
selection, batch write, count and the price preview, the order payload with the payment deadline and the confirm-receipt identifiers, the order writes (customer cancellation, hide-from-list and the balance payment), the customer's own profile with account closure mapped onto the erasure request, and the wishlist's paged list with its category tabs. **One row stands open**: the gift-card self-service, gated on `6.1-gift-card-purchase-and-transfer.md`. Promotion selection — the row this plan owns and the last of its own to be built — landed on 2026-09-21. Step 7 has run with every slice.
**Target:** Spree 6.1 (core, `spree/api` and `spree/core`)
**Scope:** The 80 partly-matching endpoints, **plus the three shared surfaces this plan owns on behalf of the set** (ruled 2026-09-18): the price preview route, the order payload, and the one share payload. No other plan may open a second price calculation, register a second order serializer, or compose a share path of its own.
**Depends on:** `6.0-order-cancellation-and-approval.md`, `6.0-cart-order-split.md`, `6.0-6.1-loyalty-gift-cards-store-credits.md`, `6.0-social-login.md`, `6.1-miniprogram-extension-architecture.md`, and — for the three surfaces it merges — `6.1-seller-scoped-pricing.md`, `6.1-seller-service-area-routing.md`, `6.1-flash-sales.md`, `6.1-points-and-growth-value.md`, `6.1-membership-tiers-and-rights.md`, `6.1-phone-verification-and-payment-pin.md`, `6.1-china-invoicing.md`, `6.1-product-reviews.md`, `6.1-delivery-tracking.md`, `6.1-referral-invitations.md`, `6.1-scenario-purchases.md`, `6.1-coupon-wallet.md`, `6.1-group-buying.md`, `6.1-product-authentication.md` (**the four shareable targets that are not a product**), and, for the poster a share may carry, the mini-program identity capability in `spree/providers/social_auth` (ruled 2026-09-18).
**Author:** Spree extension track
**Last updated:** 2026-09-21
**Tracking:** Linear — · GitHub —

## Summary

Of the 566 endpoints the 酒小二 mini program calls, 107 correspond to something Spree already models — 27 exactly, and 80 only nearly. This plan closes the 80. It is deliberately not an extension gem: the work lands in `spree/api` and `spree/core` because a customer-facing read or write that every storefront needs belongs in the Store API, and because an extension gem may not patch core models. Nothing user-visible in the mini program works before this plan does, which is why it is Wave 0 of `6.1-miniprogram-extension-architecture.md`.

## Key Decisions (do not deviate without discussion)

- **Extend Store API v3 in place; build no `/v2-app-mall` facade.** A facade over 80 partly-matching endpoints would re-implement the legacy contract and still need every change below, paying twice.
- **A read that Ransack cannot express becomes an explicit parameter or a dedicated collection action, never a filter the allowlist cannot enforce.** The batch product load is the worked case: `self.whitelisted_ransackable_attributes` on `Spree::Product` (`spree/core/app/models/spree/product.rb:369`) lists `description name slug discontinue_on status available_on created_at updated_at seller_id` — **no `id`** — so `goods/getGoodsByIds` cannot be served by `?q[id_in][]=` until either the allowlist gains `id` or the endpoint gains a first-class parameter.
- **A seller-scoped catalogue is scoped through variants, never `Product#seller_id`.** A site is a seller and a seller holds *offers* on the operator's master catalog, so `Product` rows are store-wide and their own `seller_id` is nil (`fork-decisions.md`, 2026-09-17). `seller_id` is nonetheless on the product Ransack allowlist (`spree/core/app/models/spree/product.rb:369`), which makes `?q[seller_id_eq]=` look like the seller scope while returning the operator's own listings — a silent wrong result, because the relation is valid and returns rows. The scope is `Product.joins(:variants).merge(Variant.for_seller(seller))` (`spree/core/app/models/spree/variant.rb:211`), and the price and stock a seller's storefront shows are its offer's, not the master product's.
- **A customer-facing cancellation goes through the existing cancel workflow, not a new one.** `docs/plans/5.5-6.0-order-cancellation-and-approval.md:69` already anticipates this — "a future customer self-cancel makes the actor on the order" — and the Store API has no cancel route today (`spree/api/config/routes.rb`, Store `orders` is `only: [:show]`).
- **Writes declare `resource_permitted_attributes` and never override `permitted_attributes`**, per `AGENTS.md`, so extension-contributed attributes survive. This matters here because three of the extensions in this set add writable product and cart attributes.
- **Every serializer change runs the full type generation pipeline** (typelizer → Zod → integration specs → swaggerize → SDK tests) and the OpenAPI diff is reviewed, because the regeneration is deterministic: a diff means the response really changed.
- **Cart selection is persisted on the cart line item, and completion copies only the selected lines.** The choice is durable state the client writes before checkout, not a flag applied at checkout: `cart/getCartInfo` returns a `choose` boolean per line beside a server-computed `chooseTotalQuantity`, and `cart/chooseCart` writes it as `{ choose, idSet }` — one bulk write over a set of line ids, not a per-line call. The settle page is reached as `?type=cart` and `order/cartSubmit` sends only settlement fields (address, coupon, points, delivery time), so **no request in the checkout path carries the chosen ids** — the server resolves the selection itself. The flag therefore belongs to the cart's line item and not the order's: 6.0 separates the two models and completion copies (`6.0-cart-order-split.md`, Key Decision 3), so an unselected cart line is never copied and the order never holds one. This confines the selection's reach to cart-side calculation.
- **A cart's money answers are computed over its selected lines, so the selection is an input to calculation and not a filter over it.** The client renders a minimum-order verdict the server derives from the chosen set: `cartHintHandle` compares the cart's money figure against the period minimum and only speaks when the chosen count is above zero (`miniprogram/pagesF/static/behaviors/cartBehavior.js:106-113`), and the caller passes `chooseTotalQuantity` (`miniprogram/pagesF/cart/cart.js:405`). A cart page and a settle page that disagree about the amount is the failure this prevents — the two read the same figure at different moments, so there is one calculation, scoped once.
- **Choosing between promotions is a shopper preference over a server-computed candidate list, not a typed code, so `carts/discount_codes` does not cover it.** The control appears only when a line has **more than one** candidate (`item.promotionList.length > 1`, `miniprogram/pagesF/static/template/cart-goods.wxml:17`), which a coupon entry would never require, and it opens a radio list of the server's own candidates with the current one pre-selected (`components/promotion/switch-promotion/switch-promotion.wxml`). The write carries both identities of the same candidate — `{ id: cartId, promotionId, promotionCode }`, where the code is read off the chosen entry in the list the server supplied (`cartBehavior.js:278-291`) — so the code is a second name for a candidate and never customer input; the cart has no code field at all. The list is per line and server-supplied (`cart.js:395`), each entry carrying `id`, `label` and `code`, beside a group-level `promotionDetail` describing the one currently applied (`cart.js:400-402`). **Corrected 2026-09-18, and the correction narrows the work rather than widening it:** core does compute a candidate set per line — `Spree::Adjusters::Promotion` builds the applicable promotions for each line item and picks the winner (`spree/core/app/models/spree/adjusters/promotion.rb:8-10,34-45,110-118`), and `6.0-6.1-split-adjustments.md:110` ships that path deliberately winner-only and stacking-ready. What does not exist is **wire exposure** of the candidates the calculation already reached, and **a stored preference** the calculation honours on the next run. Writing a second computation is the trap: it would drift from the adjuster's the first time either changed, and a shopper who picked a promotion would be charged by the other one. The shape of the preference (per line, per group, or per cart) stays part of the work, not an implementation detail.
- **The checkout page's aggregate already exists and it is the price preview; do not build a page-shaped endpoint beside it.** The settle page loads with one request per entry path — `calc/buyCalc` or `calc/cartCalc` (`miniprogram/pagesF/settlement/settlement.js:239,271`) — and reads **fifty-eight** page-level fields straight off that one response, including the payment methods (`payConfigV2VoList`), the coupon candidates and the coupons barred from this cart (`couponDetailList`, `banCouponDetailList`, `bigCouponId`), the points verdict (`integralTotal`, `integralNum`, `integralFlag`), the delivery promise (`deliveryTime`, `speedDelivery`), the invoice types (`invoiceType`, `invoiceSpecialType`), store-credit availability and its refusal reason (`balanceType`, `balanceReason`, `balanceNotPassword`), the discount lines (`minusList`) and the membership flags (`svipSite`, `svipUser`) — the full list is the whitelist at `settlement.js:445`. Addresses are the one thing outside it, and correctly so: they come from the address book, a customer resource the same component reads on other pages too (`components/addr/choose/choose.js:56`). So the recommendation below is not a new aggregate but an extension of the preview: one call that answers what the page needs, computed once, because every field in it is a verdict the calculation already had to reach.
- **The menu is a composite of catalogue, customer history, recommendation and marketing zones, so it is not a Store API resource.** A root menu node carries a category id **and** a `menuType` that selects one of three different fetch strategies for its children: `combo` and `guess` go to the combo-zone list, `market` goes to the market activity goods page, everything else goes to the ordinary children endpoint (`miniprogram/pagesA/make-order/make-order.js:566`). Several of those types are not catalogue nodes at all — `user` and `often` are computed from the customer's own purchase history, `guess` is a recommendation, `market` is an operator activity zone (`make-order.js:294,475`) — and second-level zone headers carry their own types (`all`, `recommend`, `common`, `market`, `make-order.wxml:47`). A node type that switches fetch strategy, over zones that mix the catalogue with CRM and merchandising, is a presentation contract this app owns; no storefront-neutral `menu` action can hold it. The catalogue half it does need — a category tree with children and counts — is the part worth adding to `categories`. The sort terms are not a new concept either: the two lists are the client's own constants (`make-order.js:27-63`), and `order_category/menuChildrenSortTermNew` is defined and never called, so the sorts actually sent are comprehensive, sales, price high-to-low and price low-to-high — plus the customer's own purchase count, which only means anything inside the history zone above. Of those, the two price orders already exist as `ascend_by_price` and `descend_by_price` on the product allowlist (`spree/core/app/models/spree/product.rb:370-376`) and comprehensive is the endpoint's default order; only a sales count and the per-customer purchase count need a sortable aggregate, and the second belongs to a zone that is not a catalogue node.

## Design Details

### Order reading and submission (19 endpoints)

| Mini-program call | Gap |
| --- | --- |
| `order/getUserOrderPage`, `order/orderSearch`, `userOrderPage` | List own orders — `GET /api/v3/store/customers/me/orders` exists (`spree/api/config/routes.rb:117`); needs the filters the app uses (status, time range, keyword). **Built** (2026-09-20): status and the time range were already expressible — `q[status_eq]`, `q[payment_status_eq]`, `q[completed_at_gt]`/`lt` — and the keyword now reaches the goods' names as well, asked as an EXISTS so an order with several matching lines is still one row. The reference's own filter list was wrong and is corrected: it documented `q[state_eq]` and `q[payment_state_eq]`, neither of which the allowlist carries, and a filter outside the allowlist is silently ignored rather than refused |
| `order/delUserOrder` | Hiding an order from the customer's own list — **built** (2026-09-21). `DELETE /api/v3/store/customers/me/orders/{id}` takes the order off the customer's side of their orders and answers in the delete's shape, because that is what the customer asked for: nothing is destroyed. The flag is `customer_hidden_at` on the order — one customer per order, so a join table would model many viewers where there is one — and it stays a flag rather than a soft delete, so the merchant's views, fulfillment, refunds and reporting are untouched. The scope carries it, so a hidden order leaves the list and the lookup by id alike; there is deliberately no un-hide (see `fork-decisions.md`, 2026-09-21, the hidden order) |
| `order/cancelOrder` | Customer self-cancel. **Built** (2026-09-21): `POST /api/v3/store/customers/me/orders/{order_id}/cancellation` creates a cancellation (an optional `reason_id` and a `note`, both stamped on the order; no canceler, because a customer is not an actor in this system), `GET /api/v3/store/order_cancellation_reasons` lists the reasons still in use, and the order payload carries `cancellable` and `status` — the button and the write answer the same question, and a customer sees that their order was called off |
| `order/orderReminder` | "Please ship sooner" — a notification, not order state. It belongs to `6.1-notifications.md`, which owns the platform's one sender; the message relay is a buyer-seller conversation and does not take it (`6.1-notifications.md:29`) |
| `order/getNoOvertimePaymentGoods`, `getOvertimePaymentInfo` | **Corrected 2026-09-20: these are 超时赔付 — late-delivery compensation, not unpaid-order expiry.** The client's review screens read an approval status, its nodes and a rules blurb (`pagesF/order/over-time/over-time.js:52-76`), and the goods popup is headed 以下商品不支持超时赔付. No concept of it exists here and no plan claims it yet. The payment deadline those rows were mistaken for is the countdown on the order list and detail (`orderPayEndTime`, `orderPayLimitTime`, `tamp`) — **built** as `payment_deadline`, see Key Decisions |
| `order/getWebappOrderConfirmParam`, `orderConfirmRes`, `hasOrderPrice` | **Not a checkout-page aggregate.** The first two are the WeChat confirm-receipt handshake: `getWebappOrderConfirmParam` takes an `orderNo` and returns the three identifiers `wx.openBusinessView({ businessType: 'weappOrderConfirm' })` needs (`merchantTradeNo`, `orderNo`, `transactionId`), called from `confirmReceipt` (`miniprogram/static/behaviors/btnEventOrderBehavior.js:182-190`); `orderConfirmRes` takes an `orderId` and is called from `App.onShow` when WeChat returns with `status === 'success'`, to record that the receipt was confirmed (`app.js:74-84`). Neither needs an endpoint: the first is three attributes on the order serializer, the second is a write against the order. `hasOrderPrice` has no caller in this build |
| `order/getOrderBase`, `getOrderBaseCtms`, `getNewFinishOrder`, `getVirtualOrderDetails` | Order detail variants. Prefer one order serializer with the fields the app needs over four endpoints |
| `order/getDeliveryInfo`, `getDeliveryPhone`, `getDeliveryPosition` | Delivery contact and position. Depends on `6.1-delivery-tracking.md`; read-only additions to the order serializer |
| `order/buySubmit`, `cartSubmit`, `offlineSubmit`, `popularizeSubmit` | Four submission paths that are one Spree completion with different cart provenance. Collapse into cart → `complete` |
| `order/balancePayOrder` | Paying an order with store credit — **built** (2026-09-21), at the order level as ruled 2026-09-18. `POST /api/v3/store/customers/me/orders/{order_id}/store_credits` settles what the order still owes from the customer's own stored value, through the same `Spree::StoreCredits::Apply` the cart uses and never through a settlement of its own — which is what keeps the payment PIN's say intact (`6.1-phone-verification-and-payment-pin.md:37,126-129`). It is all or nothing, and what it applies it captures in the same call, because an order is paid rather than reserved; the reasoning and the refusals are in `fork-decisions.md`, 2026-09-21 (the balance payment) |

### Cart and checkout (17 endpoints)

| Gap | Detail |
| --- | --- |
| Batch add and batch update (`cart/addCartBatch`, `setCartBatch`) | One request carrying many lines. **Built** as `POST /api/v3/store/carts/{cart_id}/items/batch`: the quantities are the set the cart should end up holding (idempotent), an entry names a variant or a line of this cart, the set recalculates once, and an entry the cart refuses comes back in the cart's `warnings` while the rest applies — "applied atomically" is narrowed to *requests* that cannot be applied at all (see Key Decisions) |
| Cart selection for checkout (`cart/chooseCart`) | A cart line carries a persisted `selected` flag that both calculation and completion read, and the cart serializer exposes the chosen count. The client writes it in bulk over a set of line ids (`{ choose, idSet }`) and never sends that set to checkout, so the store surface is one bulk write plus the count — see Key Decisions |
| Promotion selection (`cart/changePromotion`) | **Built** (2026-09-21). `POST /api/v3/store/carts/{cart_id}/promotion_selection` names one of the line's `promotion_candidates` — the list the engine already computed per line and the cart payload now carries, beside `promotion_id`, the promotion whose line-level discount the line actually got. The choice is a `chosen_promotion_id` on the line, and the adjuster honours it in `apply_line_item_discounts` rather than in `select_applicable`, so the winner policy this plan isolated stays untouched. Both identities come from the server's own list — a promotion that is not a candidate, or a code that no longer matches the candidate's own, is refused — and a promotion that applies to several lines needs `line_item_id` to say which one is meant. A choice that stops applying falls back to the engine's winner; see `fork-decisions.md`, 2026-09-21 (the picker's answer) |
| Price without a cart (`calc/buyCalc`, `calc/cartCalc`, `calc/superValueCalc`) | A price preview from variant IDs and quantities with no cart created. Nothing exists; the cart is the only calculator today. This is the highest-value single addition in this plan, because the app calls it on every product page |
| `cart/getCartCount` | A cheap count for the tab bar. **Built** as a dedicated lightweight read, `GET /api/v3/store/carts/{cart_id}/count`, answering the cart's own `total_quantity` and `selected_quantity` and nothing else — see Key Decisions |
| `cart/getCartRecommendGoods` | Recommendations in the cart. Depends on what recommendation source we choose; may be served from the existing products endpoint with a scope |

### Customer account (17 endpoints)

| Gap | Detail |
| --- | --- |
| `info/modifyPhone` | Changing the phone number needs the verification service from `6.1-phone-verification-and-payment-pin.md`; the customer update itself exists (`PATCH /api/v3/store/customers/me`) |
| `info/cancelUser` | Account deletion — **ruled 2026-09-21: it reuses the GDPR erasure request.** `POST /api/v3/store/customers/me/data_requests` with `kind=erasure` is the path; the client's 注销账户 is that request, confirmed with the account password (or, for a WeChat account that has none, the phone-code attestation that lands with `6.1-phone-verification-and-payment-pin.md`). The client's four blockers — open orders, membership, balance, after-sales (`miniprogram/pagesD/personal/verifi-phone.js:248-268`) — are deliberately **not** enforced: the right to erasure does not depend on what a customer still owes or holds, and their orders survive as anonymised records. The client may warn about them before submitting, from reads it already has (see `fork-decisions.md`, 2026-09-21, the customer's own profile) |
| `info/get`, `set` | Profile read and write — **built** (2026-09-21). The profile the client's editor sends is on the customer now: `nickname`, `gender`, `birthday` and `city` are columns, and the avatar the customer already had is exposed as `avatar_url` (written as `avatar`, a direct-upload signed id — the same split the admin profile makes). An empty gender stores as NULL. `getRecordingSetting`/`setRecordingSetting` have **no caller in this build**: no endpoint, like `hasOrderPrice` |
| `info/getAllInfo` | **Corrected 2026-09-21: not a profile read.** It is the personal-center aggregate — coupons, points, balance, gift cards, SVIP rights and goods — assembled from resources owned by `6.1-coupon-wallet.md`, `6.1-points-and-growth-value.md` and `6.1-membership-tiers-and-rights.md`. It belongs to whichever of those plans owns that page |
| `info/newUserCheck` | First-order detection — **built** (2026-09-21) as `orders_count` on the customer payload: the completed orders this customer has in the request's store, so a customer of a sibling store is still new here. A field rather than an endpoint, per this plan's one-payload rule; the client derives its boolean (`miniprogram/static/behaviors/areaGoodsBehavior.js:34-40`, `pagesF/cart/cart.js:645-665`) |
| `info/updatePassword`, `token/resetPassword` | Present (`password_resets`), and the mini program should use it rather than a second path |
| `token/getByWechatMp`, `secondByWechatMpDecode`, `bindByWechatMpDecode`, `getWechatMpBaseInfo` | Mini-program login. Extends the official `spree_social_auth` gem, not this plan — see the architecture plan's "extend official work" table |

### Catalogue (13 + 7 + 2 + 1 endpoints)

| Gap | Detail |
| --- | --- |
| Batch product load (`goods/getGoodsByIds`) | Ransack `id_in` is unavailable; add either `id` to the product allowlist or a first-class `ids` parameter |
| Share metadata (`goods/getGoodsSharing`, `share/getShareInfo`) | Title, subtitle, image and path for a WeChat share card — **now the one share resource this plan owns**, `POST /api/v3/store/shares` with `target_type: product` (ruled 2026-09-18), rather than four domains composing a path each |
| Purchase history (`goods/newBuy`, `goods/oftenBuy`) | "Bought before" and "bought often", computed from the customer's completed orders. A scope plus an ordering, not stored data |
| Sale attribute set (`goods/getSaleNameSet`) | The option values a product actually sells in, for the picker. A serializer addition |
| Menu-shaped category tree (`goodsCategory/getCategoryList`, `order_category/menuRoot`, `menuChildrenInfo*`, `menuChildrenSortTermNew`, `getCategoryChildren`, `categoryAttr`, `getCategoryAttrNum`) | A menu node is a category id **plus a `menuType` that selects one of three fetch strategies**, and several types are not catalogue nodes at all — see Key Decisions. Only the catalogue half belongs here: a category tree with children and counts. The sort terms are the client's own constants (`make-order.js:27-63`) and `menuChildrenSortTermNew` is defined and never called. The filter attributes (`categoryAttr`, `getCategoryAttrNum`) are the existing `products/filters` surface with counts |
| Filters and search (`goods/getByCategory`, `getGoodsMarketByKeyword`, `hotWord/*`) | Present via `products` and `products/filters`; add the sort terms the app exposes and check whether hot words are a merchant-managed list or a search-provider concern |
| Brand pointers on the existing reads (`normal.js:977`, `result.js:205`) | The product payload's `discernId` and the search response's `brandKeywordVo` point into `6.1-product-authentication.md`'s content from reads this plan already serves, and the brand facet rides `products/filters` (`brandIdSet`, `available-goods.js:363-366`). Three fields here; the content they point at is that plan's |
| Wishlist grouping (`collect/category`, `collect/page`) | **Built** (2026-09-21). `GET /api/v3/store/wishlists/{wishlist_id}/items` is the page's list — paginated, most recently collected first, and narrowed with `category_id` — and `GET /api/v3/store/wishlists/{wishlist_id}/items/categories` is the tabs above it: the categories any collected good falls into, in the catalogue's own order. **The row's guess was wrong and is corrected**: the grouping needs no field on the wishlist item — the categories come from the goods themselves, so a field would have been a second copy of the catalogue that drifts. A tab naming a category this store does not have is a 404, not an empty page, and the list carries the pagination the client's `collect/page` sends (`current`/`size`) |
| Gift card self-service (`getMyGiftCardPage`, `getMyGiftCardMain`, `getGiftCardInfo`, `getGiftCardSalesList`, `getGiftCardGoodsList`, `giftCardBalanceDetailsPage`, `giftCardActive`, `receiveGiftCard`, `getUserGiftCardOrderRecordPage`) | Store API has `carts/:cart_id/gift_cards` and `customers/me/gift_cards` index/show. **Claiming and activating are missing** — `6.1-gift-card-purchase-and-transfer.md` owns them |
| Policies (`article/privacyAgreement`, `article/userAgreement`) | Present (`GET /api/v3/store/policies`) |
| Analytics data access (`sensors/*`, 5 endpoints) | Read-only product payloads for the client-side tracker. Serve from `products/index` with the fields the tracker wants; **no ingestion endpoint in Spree** — the mini program sends to Sensors directly |
| Store credit and gift card payment on a cart (`order/balancePayOrder` and the checkout aggregate) | `carts/store_credits` and `carts/gift_cards` exist |

### Work that is not an API gap

Three groups in the 80 are deliberately excluded from this plan because they belong to another owner:

- **WeChat Pay (4 endpoints)** — already built in `spree/providers/wechat_pay`.
- **Mini-program login (3 endpoints)** — extends `spree_social_auth`.
- **Verification codes and the payment PIN (1 endpoint here, 8 in total)** — `6.1-phone-verification-and-payment-pin.md`.

### The composite read surface, owned here (ruled 2026-09-18)

Two aggregates in this set had several plans each adding a field and nobody merging them: the price preview **named no path anywhere in this plan**, and the order payload was written onto by five plans while one of them registered a serializer of its own. Both are this plan's now, because every plan in the set already defers to this one as Wave 0, and the alternatives — a plan of its own, or no owner at all — buy a document or a drift.

**`POST /api/v3/store/price_preview` — one route, named, for the three `calc/*` calls.** The request carries either a set of variant ids with quantities (`calc/buyCalc`, `calc/superValueCalc`) or a cart id (`calc/cartCalc`), and it answers both the product page's price read and the settlement page's aggregate in one shape — the fifty-eight page-level fields the settle page reads off one response (`:29`) are verdicts the calculation already reaches. It is a computation rather than a resource, so it is written rather than read; what matters is that there is exactly one of it. The path is fixed here rather than left to implementation because five plans' migration steps block on it.

| What the preview answers | Owning plan |
| --- | --- |
| price, stock and limits resolved through the request-scoped channel, with `goods/getEstimate`, `presale/estimate` and the seckill estimates as sources of the same call | `6.1-seller-scoped-pricing.md:101-132` |
| the delivery area and delivery promise for the chosen address | `6.1-seller-service-area-routing.md:257` |
| the activity's refusal flags (`allows_points`, `allows_coupons`) and the seckill verdict | `6.1-flash-sales.md:40,161` |
| the points verdict and the earning threshold | `6.1-points-and-growth-value.md:132,407` |
| the membership flags (`svipSite`, `svipUser`) | `6.1-membership-tiers-and-rights.md` |
| `balanceNotPassword` | `6.1-phone-verification-and-payment-pin.md:129` |
| the invoice verdicts the settle page needs (`invoiceType`, `invoiceSpecialType`) — the merge preview is that plan's own separate read, ruled 2026-09-18 | `6.1-china-invoicing.md:10,38` |

**The order payload is one serializer, and this plan merges it.** Five plans add fields to `orders/:id` and six nested route groups hang off it; the merge point is a table here rather than a registration per plan, because "one order serializer with the fields the app needs" (`:44`) stops being true the first time two plans each register their own.

| What the order payload carries | Owning plan |
| --- | --- |
| the payment deadline, the delivery contact and position, and the confirm-receipt identifiers (`merchantTradeNo`, `orderNo`, `transactionId`) | this plan, `:43-45`. **Partly built** (2026-09-20): the deadline is `payment_deadline`; the confirm-receipt trio rides the order's own `payments[]` — `response_code` is the merchant order number WeChat knows the order by, and `merchant_id`/`gateway_transaction_id` come from the gateway's contract, null for one that keeps neither. The delivery contact and position remain, with `6.1-delivery-tracking.md` |
| `orderEvaluate`, `orderDeliveryEvaluate`, `integralNum` | `6.1-product-reviews.md:325-330` |
| the invoice fields (`isApplyInvoice`, `isReplenishInvoice`, `invoiceId`, `invoiceStatus`, `invoiceDto`) | `6.1-china-invoicing.md:95` |
| `balanceNotPassword` | `6.1-phone-verification-and-payment-pin.md:129,158` |
| the delivery trajectory and the signing record — read-only fields here, with their own nested routes | `6.1-delivery-tracking.md:59-62` |
| the participants of an invitation — a nested write, not a field | `6.1-referral-invitations.md:168` |

Add a field by adding a row here; a plan that registers its own order serializer is how two payloads for one resource start. The three nested route groups stay their owners' — this plan owns the payload, not their routes.

**`POST /api/v3/store/shares` — the one share payload (ruled 2026-09-18).** Every share in this app has the same three fields and one grammar, and four plans were about to compose them separately: this plan's product card (`:77`), `6.1-referral-invitations.md`'s invitation payload, `6.1-coupon-wallet.md`'s `coupon_holdings/:id/share`, `6.1-group-buying.md`'s team link and poster, and `6.1-product-authentication.md`'s hub copy. They now go through one route.

```json
// POST /api/v3/store/shares  { target_type, target_id, …domain params }
{ "title": "…", "subtitle": "…", "image_url": "…",
  "path": "/pages/index?type=inviteGoods&typeId=id-…_parentId-…_originalSiteId-…",
  "scene": "…", "qrcode_url": "…", "poster_url": "…" }
```

- **`path` is composed here, once.** The client assigns it straight to `onShareAppMessage().path` and decodes it with its own grammar — `_` separates fields, `-` means `=`, resolved against a `type` table (`locationLinkBehavior.js:7-13`) — and that grammar gets one implementation rather than four. The `target_type` vocabulary is the same `api_type` shorthand the rest of v3 uses. **Built with one wrinkle the grammar forces:** v3's own ids contain the separator (`prod_UkLWZg9DAJ`), so the composer percent-escapes `% _ - & =` in every value and the client's decoder is left as it is — the page reads its query through a URL parser, which is where the escaped value comes back. What can be shared is `Spree.shareable_targets`, an `api_type`-keyed map each plan adds its own row to; the route carries no branch per kind.
- **The target answers a contract, not a registry.** A model that can be shared implements `share_descriptor(context)` returning the title, the image and the identifiers its path needs; the route resolves `target_type` to the model through a map this plan declares, and a model that does not implement the contract is not shareable. This is the transfer primitive's duck-typed shape rather than a sixth registered-subclass family.
- **Composing may mint what the payload needs.** An invitation share needs a binding and a team share needs a join link, so the composer is allowed to create or refresh one inside the request — it is a `POST` for that reason — which keeps the client's single call per share.
- **The QR and the poster are the identity capability's.** `qrcode_url` and `poster_url` are filled where the target supports them, minted through the mini-program scene-code capability `spree/providers/social_auth` owns (ruled 2026-09-18) — the one owner of the `access_token` — so no domain fetches a token or renders a poster itself.
- **The accepted cost, stated rather than discovered.** A generic resource carries the union of its targets' parameters: an invitation passes a binding, a coupon a token, a group buy a team id, the academy nothing but a hub id. Each domain keeps its own *data* and its own endpoint (the binding is still the referral plan's resource, the token still the coupon wallet's), so what is shared is the payload and the grammar, not the identity of the thing shared.

| Who shares | What it adds here | Where its data stays |
| --- | --- | --- |
| the product card (`:77`) | nothing — the product is the target | this plan's serializer |
| `6.1-referral-invitations.md` | the binding it mints, and the invite scene | that plan's `spree_invitation_bindings` |
| `6.1-coupon-wallet.md` | the transfer token | that plan's transfer row (`6.1-transfer-primitive.md`) |
| `6.1-group-buying.md` | the team id and its join link | that plan's `spree_group_buys` |
| `6.1-product-authentication.md` | the hub id, and copy only | that plan's `spree_discern_hubs` |

## Migration Path

1. **Product and catalogue reads** — batch load, purchase history, sale attributes, the category tree with children and counts, and **`POST /api/v3/store/shares`** with the product as its first target. Lowest risk and no schema change, it unblocks the product pages, and it lands before the other four targets add their rows. **Built** (2026-09-20): the batch load is a first-class `ids` parameter on the products collection — not `id_in` on the allowlist the constraint refuses — answering only what the store sells; the purchase history is `Spree::Product.purchased_by`, read through `GET /customers/me/purchase_history` with `sort=recent|frequent` over one set; the sale attribute set is `expand=sale_option_values`, the values on the variants a shopper can buy; a category carries `products_count` beside `children_count`; and the share payload is `Spree::Shares::Compose`, with `Spree::shareable_targets` naming what can be shared and `Spree::Product#share_descriptor` answering the product's card.
2. **Cart** — the line selection and its bulk write, batch operations, the cart count, and **`POST /api/v3/store/price_preview`**, which is the single most-called new endpoint and the one five plans block on. **Partly built** (2026-09-20): the price preview is built, and so is the selection — a `selected` flag on the cart line (ticked when the line is added), `PATCH /api/v3/store/carts/:cart_id/selection` writing it in bulk over a set of line ids, `selected` on each line and `selected_quantity` on the cart in the payload, and completion copying only the ticked lines. **The money scoping Key Decision 28 rules is built too** (2026-09-20, second slice): `priced_line_items` is the one seam every money path reads — the totals workflow's subtotal and row fold, the promotion adjuster, the tax item list, the inventory-unit builder behind every package and delivery rate, and completion's copy — so a cart's money is the ticked lines' figures and a cart with nothing ticked cannot be checked out. The counts stay whole-cart; see `fork-decisions.md`, 2026-09-20 (later still), which also records the piece that was left there: **built** (2026-09-21, a slice of its own) — unticking a line gives its stock back at once and a tick takes the hold again, so a hold always matches what the cart's money is about. **The cart count is built** (2026-09-20, late): `GET /api/v3/store/carts/{cart_id}/count` answers the two counts and nothing else, for the badge a storefront renders on pages with no other business with the cart. **The batch write is built too** (2026-09-20, the batch write): `POST /api/v3/store/carts/{cart_id}/items/batch` writes a set of lines in one pass — a combo added whole, an order re-added, a group's quantities changed together — and the cart's write surface is complete.
3. **Checkout and order reads** — the settlement-page fields on the preview, then the order payload table above applied in one pass rather than plan by plan. **Partly built** (2026-09-20): the order payload carries `payment_deadline` — when an unpaid order stops being payable, the store's `unpaid_order_timeout_minutes` from the moment it was placed, null once nothing is owed or the store keeps no timeout — and it is a display fact, with nothing cancelling an expired order on the server's side (see `fork-decisions.md`, 2026-09-20, the payment deadline). The settlement-page fields remain.
4. **Order writes** — customer cancellation, hide-from-list, and the order-level balance payment, which spends through `Spree::StoreCredits::Apply`. **Hiding an order is built** (2026-09-21): `DELETE /api/v3/store/customers/me/orders/{id}` stamps `customer_hidden_at` and the customer's scope reads it, so the order leaves the customer's list and their lookup without leaving the merchant's. **The balance payment is built** (2026-09-21): `POST /api/v3/store/customers/me/orders/{order_id}/store_credits` settles what an order still owes from the customer's own stored value — all or nothing, captured in the same call, through the same service the cart applies through.
5. **Customer** — account closure, new-customer detection, profile fields. **Built** (2026-09-21): the profile is four customer columns with the existing avatar exposed, `orders_count` answers the first-order branch, and account closure is the existing erasure request rather than a second soft-close. `info/modifyPhone` and the login rows stay with the phone-verification and social-login plans.
6. **Wishlist grouping and gift card self-service**, the latter gated on `6.1-gift-card-purchase-and-transfer.md`. **The wishlist half is built** (2026-09-21): a paginated items read with a category filter, and the categories read that draws the tabs. The gift-card half still waits on that plan.
7. Regenerate types, Zod, OpenAPI and SDK tests after each step; review the OpenAPI diff.

## Constraints on Current Work

- **Do not add a customer-visible write to the Store API without an explicit `resource_permitted_attributes` list** and a spec proving a foreign store's ID returns 404 rather than updating another tenant's record.
- **Do not add `id` to the product Ransack allowlist as a convenience.** If batch load is expressed as a filter, it must be scoped so it cannot enumerate another store's catalogue; a first-class `ids` parameter on a scoped collection is the safer shape.
- **Do not build a second password reset or a second login path** for the mini program. Reuse `password_resets` and extend `spree_social_auth`.
- **Do not give the Store API a bulk envelope** (a shape that returns per-item results for a batch of writes) — `6.0-admin-api.md` still has that as an open question, and a second dialect would be worse than the current one-line-per-request shape.
- **Do not model cart selection as two checkout modes.** The cart carries one selection state — a per-line tick, a bulk write over a set of line ids, a server-supplied chosen count — and no "check out everything" mode beside it; the client has no select-all control at all, only per-line ticks and a per-group bulk tick, so "check out all" is that state with every line ticked. The selection write is a bulk write but needs no per-item result envelope — the client reloads the cart afterwards rather than reading per-line outcomes (`miniprogram/pagesF/static/behaviors/cartBehavior.js:52-54`) — so the "no bulk envelope" constraint above is untouched.
- **Do not compute a cart's money over its whole line set and filter it afterwards.** The selection is an input to calculation (see Key Decisions), so a cart has one amount, produced once, and both the cart page and the settle page read it. Two code paths that reach the same figure by different routes will disagree on the next promotion change.
- **Do not open a second price calculation, and do not move the preview's path.** `POST /api/v3/store/price_preview` is the one computation; a plan that needs a field on it adds a row to the table above, and a plan that needs it for a new source (presale, a seckill, an estimate) adds the source rather than the route. `6.1-seller-scoped-pricing.md:101`'s `products/:id/purchase` was the competing shape and does not exist.
- **Do not register a second order serializer.** The payload is merged here from the table above; a plan that registers its own serializer through `Spree.api.order_serializer` splits one resource into two payloads that then drift (`6.1-product-reviews.md:329-330` is the case to convert when this lands).
- **Do not compose a share path anywhere else, and do not fetch a token to render a QR.** One route returns the payload (`POST /api/v3/store/shares`), one implementation encodes the client's `type`/`typeId` grammar, and the QR and poster come from the identity capability's `access_token` service rather than from a second cache — WeChat invalidates the previous token whenever a new one is obtained, so a second fetcher breaks the first.
- **Do not let a shareable target keep its own payload shape.** A target that wants to be shared implements the descriptor contract and adds a row to the table above; a plan that composes title/image/path itself is how the four implementations come back. The accepted cost is named in the section above: this resource carries the union of its targets' parameters.
- **Do not settle an order-level balance payment outside `Spree::StoreCredits::Apply`.** The payment PIN is enforced in that service instead of in the API layer (`6.1-phone-verification-and-payment-pin.md:37`), so a bespoke settlement — including a scenario-style one — spends a customer's balance without the check.

## Open Questions

All four questions are settled as of 2026-09-17 by reading the mini program at `miniprogram/`; every client path below is relative to it, and each ruling is in Key Decisions. The original questions are kept for the record.

1. ~~**How is "check out only the ticked lines" modelled?**~~ **Settled 2026-09-17: Option A — a `selected` flag on the cart line item — and the flag is the cart's, not the order's.** The client decides it, on four findings. The choice is durable cart state: `cart/getCartInfo` returns a `choose` boolean per line beside a server-computed `chooseTotalQuantity` (`pagesF/cart/cart.js:216,319,408`), and `cart/chooseCart` writes it in bulk over a set of line ids (`{ choose, idSet }`, `pagesF/static/api/cartApi.js:7-9`, `pagesF/cart/cart.js:588-605`). Checkout carries no id list at all: the settle page is opened as `?type=cart` with no ids (`pagesF/cart/cart.js:757-759`), `orderSubmit` builds its payload from `calcParams` plus order-level fields only (`pagesF/settlement/settlement.js:868-887`), and `calc/cartCalc` sends the same shape (`:271-279`) — the buy-now path is the one that names goods explicitly, through `calcGoodsList` (`:673-674`), which is exactly the difference between "these items" and "my ticked items". The client **repairs** the selection rather than merely reading it: when the server marks a chosen line disabled it calls `chooseCart` with `choose: false` for those ids and reloads (`pagesF/cart/cart.js:216-224`), which is behaviour a transient UI flag would not need. And the selection is not one of two modes — the cart has per-line ticks and a single bulk tick per combo group (`comboChooseCart` sends every line of the group, `:510-521`) with no global select-all control anywhere, so "check out everything" is this one state with every line ticked. Option B is out because there is exactly one cart per site (`getCartInfo` takes only `siteId`, `:209-214`) and the choice lives inside it as a per-line flag; Option C is out because the choice exists and is enforced long before checkout, not at it.

   **The scope of the change is smaller than the question assumed.** Its objection — that a `selected` boolean puts a UI concept in the data model and forces every promotion, tax and shipping rule to ignore unselected lines consistently — describes a platform where the cart *is* the order. In 6.0 they are separate models and completion copies (`6.0-cart-order-split.md`, Key Decision 3), so an unselected cart line is simply never copied (`workflows/spree/carts/complete.rb:308-316` iterates `cart.line_items`; the same scope filters it) and no order ever holds one. The selection's reach is therefore **cart-side calculation only**, which is where the client's own server already applies it.
2. ~~**Is the promo-selection endpoint worth building?**~~ **Settled 2026-09-17: yes, and it is the second option — the shopper chooses between automatic promotions.** The question asked whether `cart/changePromotion` is "apply my coupon" (already covered by `carts/discount_codes`) or "choose between two automatic promotions" (a rule-engine change). It is the second, and more sharply than the question supposed: the control is rendered only when a line has more than one candidate, it presents the server's list as a radio group with the current choice pre-selected, and the write names one entry of that list. The candidate set, the current choice and the labels are all server-side, so the work is not a preference column alone — the promotion engine has to be able to answer "which promotions could apply to this line, and which one does the shopper want", and re-price with the answer. Conflict risk is real and is why this is a decision rather than a detail.
3. ~~**Does the mini program need a page-level aggregate at all?**~~ **Settled 2026-09-17: it needs one, it already has one, and it is the price call — so extend the preview rather than build a page-shaped endpoint.** The settle page makes one request to load (`calc/buyCalc` or `calc/cartCalc`) and reads fifty-eight page-level fields off the response, covering payment methods, coupon candidates, points, the delivery promise, invoice types and store credit. Addresses come from the address book as a separate customer resource, which is the right split and stays. The recommendation is therefore narrowed: the aggregate is not a new endpoint but the set of verdicts the preview returns, kept in one computation precisely because the page reads one figure.
4. ~~**Is the menu-shaped category response a Spree feature or an app-level concern?**~~ **Settled 2026-09-17: an app-level concern, with the catalogue half belonging here.** A menu node's `menuType` chooses between three fetch strategies, and `user`, `often`, `guess` and `market` are customer history, recommendation and operator merchandising rather than catalogue nodes, so a storefront-neutral `menu` action would be modelling one app's page composition. What this plan takes is the part every storefront wants: a category tree carrying children and counts. The "sort-term concept that does not exist yet" is dropped — the client never asks the server for sort terms, and the four it sends are orderings over existing fields.

## References

- `.tooling/miniprogram-endpoints/endpoints-spree-mapping.md` — the per-endpoint mapping these gaps are derived from, including the A/B/C/D verdict and the reason for each
- `spree/api/config/routes.rb` — the current Store API v3 surface
- `spree/core/app/models/spree/product.rb:369-376` — the Ransack allowlists, and the missing `id`
- `docs/plans/5.5-6.0-order-cancellation-and-approval.md:69` — the anticipated customer self-cancel
- `spree/core/app/workflows/spree/carts/complete.rb:308-316` — the line-item copy that the selection scopes; `spree/core/app/models/concerns/spree/purchase/totals.rb` — `priced_line_items`, the seam every money path reads (a cart answers with its ticked lines, an order with all of its own), overridden in `spree/core/app/models/spree/cart.rb`
- `miniprogram/pagesF/cart/cart.js:209-224,405-408,510-521,588-605,757-759`, `miniprogram/pagesF/static/api/cartApi.js:7-9`, `miniprogram/pagesF/static/template/cart-goods.wxml:7-9`, `miniprogram/pagesF/settlement/settlement.js:271-279,673-674,868-887`, `miniprogram/components/j-floating/bar/bar.wxml` — the cart selection: read, bulk-written, repaired, and carried into checkout without an id list. Every client path in this plan is relative to `miniprogram/`
- `miniprogram/pagesF/static/template/cart-goods.wxml:17`, `miniprogram/components/promotion/switch-promotion/switch-promotion.wxml`, `miniprogram/pagesF/static/behaviors/cartBehavior.js:266-296`, `miniprogram/pagesF/cart/cart.js:395,400-402` — promotion selection: the control gated on more than one candidate, the radio list of server-supplied candidates, the write naming one of them, and the per-line list beside the group-level current promotion
- `miniprogram/pagesF/settlement/settlement.js:239,271,445`, `miniprogram/components/addr/choose/choose.js:56` — the checkout page's one load request and the fifty-eight fields it reads from the response, with addresses outside it
- `miniprogram/pagesA/make-order/make-order.js:27-63,294,475,566`, `miniprogram/pagesA/make-order/make-order.wxml:34-47` — the menu: the client's own sort-term constants, the `menuType` values including the history, recommendation and market zones, and the three-way dispatch on `menuType`
- `miniprogram/static/behaviors/btnEventOrderBehavior.js:182-207`, `miniprogram/app.js:74-84`, `miniprogram/static/api/btnOrderApi.js:13-19` — the WeChat confirm-receipt handshake the plan had recorded as a checkout aggregate
- `spree/api/app/controllers/spree/api/v3/resource_controller.rb:285-300,338-340` — the `sort` parameter and its `-field` notation
- `docs/plans/6.0-cart-order-split.md`, `6.0-6.1-loyalty-gift-cards-store-credits.md`, `6.0-social-login.md`
- `AGENTS.md` — API controller, serializer, permitted-attribute and type-generation conventions
