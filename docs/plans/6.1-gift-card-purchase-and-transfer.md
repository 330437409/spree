# Gift cards: purchase, ledger, activation and transfer

> **Fork-only plan.** Ours, never submitted to `spree/spree`. The tracked
> plans beside it are upstream's and are read-only to us; our decisions are in
> `fork-decisions.md`, never in upstream's `decisions.md`.

**Status:** Draft — tentative, pending review · designed as an extension of official phase 2, four rulings needed (Open Questions); Open Question 3's design half is now answered; the donation is a row of the shared transfer primitive (ruled 2026-09-18)
**Target:** Spree 6.1 (extends the core gift card plus the loyalty plan's phase 2)
**Depends on:** `6.0-6.1-loyalty-gift-cards-store-credits.md` (phase 2, unbuilt), `6.0-digital-assets.md`, `6.1-scenario-purchases.md`, `6.1-grant-and-benefit-primitive.md` (**the welfare grant is a kind of the shared grant row** — ruled 2026-09-18), `6.1-transfer-primitive.md` (**a donation is a row of the shared primitive, and this plan owes it the hold release** — ruled 2026-09-18), `6.1-ledger-primitive.md` (**the card's history is a row of the shared ledger** — ruled 2026-09-18), `6.1-miniprogram-extension-architecture.md`
**Author:** Spree extension track
**Last updated:** 2026-09-17
**Tracking:** Linear — · GitHub —

## Summary

The mini program calls 21 gift-card endpoints: buy a card as a product, activate it, receive it, give it to someone else, watch its balance move, and claim subsidised or campaign cards. Spree already models the instrument — `Spree::GiftCard` carries a code, an amount, an amount used, an amount authorised, a status, an expiry and an optional owning customer (`spree/core/app/models/spree/gift_card.rb:25-46`), with apply, remove, redeem and cancel workflows and read-only Store API access at `carts/:cart_id/gift_cards` and `customers/me/gift_cards` (`spree/api/config/routes.rb:56,120`). What is missing is everything around it: buying one, seeing its history, and moving it between people. This plan supplies those by extending the official phase 2 design rather than by building a parallel prepaid instrument — the loyalty plan explicitly forbids a second non-physical flag.

## Key Decisions (do not deviate without discussion)

- **Buying a card is the official phase 2 design, not a new one.** `6.0-6.1-loyalty-gift-cards-store-credits.md:88-106` already specifies it: a `spree_products.gift_card` boolean, variants as denominations, a `spree_gift_card_recipients` row per line item, and idempotent issuance from `order.paid` through `Spree::GiftCards::IssueFromOrder`. `:131` forbids a second "digital-only" flag that would have to be reconciled with `gift_card`. This plan implements that design and adds to it; it does not restate it.
- **A donation is a post-issue transfer and must not become "recipients version two".** A recipient is collected *before* issue, on a cart line, and is part of how the card is born. A donation is a card that already exists and changes hands. They are different tables because they are different moments, and the loyalty plan already rejected putting recipient fields on `spree_line_items` (`:26`).
- **The card gets a ledger, because the mini program shows one.** `Spree::StoreCredit` has `Spree::StoreCreditEvent` (`spree/core/app/models/spree/store_credit_event.rb:9-19`) and a card has no equivalent, yet `giftCardBalanceDetailsPage` is a page of exactly that history. The ledger records issue, redeem, refund, transfer in, transfer out and expiry, with an amount and an actor.
- **A card is a bearer instrument; restricting it to a customer is an action, not a default.** The loyalty plan leaves `customer_id` unset on issue for this reason (`:102`). A donation therefore does not require the recipient to have an account — the token in the link is the claim, and an account is created or linked at acceptance.
- **Stored value, so it surfaces under Loyalty.** `6.0-6.1-loyalty-gift-cards-store-credits.md:127`.
- **Codes never leak.** They do not appear in events, logs, serializers, share images or a buyer's own order confirmation; only the recipient's delivery carries the code (`:132`).
- **A subsidised or campaign card is a grant, not a purchase.** `*Welfare*` cards arrive without payment, so they need a record of who granted them, from which campaign, when they arrived and when they were claimed — otherwise the card's origin is unknowable and the liability is unaccounted for. That record is **a kind of the shared grant row** (`6.1-grant-and-benefit-primitive.md`), with this plan's side table carrying only what the primitive does not.

## Design Details

### Extending phase 2

The official phase 2 substrate is used unchanged: the product flag, denominations as variants, the recipient table, and issuance on `order.paid`. One boundary has to be drawn first, because phase 2 describes only one of the two channels this client has.

**Phase 2 describes the cart channel; the mini program uses the scenario channel.** A gift card bought on the storefront enters a cart as a product, carries recipient fields on its line item, and is issued by `Spree::GiftCards::IssueFromOrder` on `order.paid`. A gift card bought in the mini program is a **scenario order**: `gift-card/addOrder` sends `siteId`, `userId` and `orderGoodsList` — no cart, no line items, no address and **no recipient** (`miniprogram/pagesE/gift-card/details/details.js:355-395`) — and settles through `gift-card/payByWxMp`, with the card issued on settlement like every other scenario kind (`6.1-scenario-purchases.md`). The recipient is chosen later, by donating the card (below), and the blessing message travels with the donation rather than with the purchase. So the recipient table is the storefront's substrate and this plan's donation table is the mini program's, and each channel's issue step stays with its own channel. `IssueFromOrder` needs no discriminator and no guard: a scenario order never reaches `order.paid`, and the membership that shares the same product flag is bought through a scenario order too.

This plan adds:

- **Cover artwork** (`getCoverTypeSet`): a store-scoped catalogue of card designs, referenced by the recipient row so a card remembers what it looked like. It is presentation data, so it is a small store-owned table or a custom field, not a column on the card.
- **Activation** (`giftCardActive`): a card issued in a dormant state that the purchaser or recipient activates. The design half of Open Question 3 is now settled by the mini program, which ships the step for gift cards as well as for membership cards: the client calls `giftCardActive` with the card's own **code** when the card's status is `not_active`, and its detail sheet knows four further states — `used` (already activated), `expired`, `card_refund` and `frozen` (`miniprogram/pagesE/components/gift-card/detail-pop/detail-pop.js:60-77`, `miniprogram/pagesE/static/api/giftCardApi.js:28-30`). So a dormant status and an activation workflow are required, not optional. Two consequences the earlier draft did not carry: the client's five states do not map onto `Spree::GiftCard`'s four (`active`/`partially_redeemed`/`redeemed`/`canceled`, `gift_card.rb:21`) — `not_active` is missing, `frozen` and `card_refund` have no equivalent — and activation is addressed by the code, which is the bearer instrument, so the endpoint has to accept it rather than an id. Whether activation is a *regulatory* requirement is still unanswerable here; it no longer blocks, because the model below is the same either way.
- **The ledger** (see below).
- **Donation and transfer** (see below).
- **Welfare grants** (see below).

### The ledger

The card's history is **a row of the shared ledger primitive** (`6.1-ledger-primitive.md`, ruled 2026-09-18) — the same row the points ledger and the distributor's earnings use — with the card as the account, its six actions (`issue`, `redeem`, `refund`, `transfer_in`, `transfer_out`, `expire`) as the kinds, the order or donation as the source, and the actor and memo in metadata. The card's `amount_used` and `amount_authorized` remain authoritative for the balance, so `balance_after` is written null here: the primitive records that the card is the instrument, and the balance-details page reads the entries rather than summing them.

**This closes the open question the plan used to carry** — whether the ledger belongs in core beside `StoreCreditEvent` or in this extension. It belongs to the primitive, which is core, and this plan no longer designs a table.

### Donation and transfer

A donation is **a row of the shared transfer primitive** (`6.1-transfer-primitive.md`), not a table of this plan's, pointed at the card. The primitive owns the opaque token, the window and the endings — `pending`/`accepted`/`canceled`, with `expired` derived from the date rather than stored — so the token is addressable without a session and the mini program's `receiveGiftCardDonationUrl` works before sign-in, the shape company invitations already use. What this plan implements is the contract on `Spree::GiftCard`: **given** places the hold, **accepted** moves ownership and writes the two ledger rows, **canceled** releases the hold.

A donation does not move money until it is accepted. While pending, the card is held against being spent by the donor — the card already supports holds for this reason — and cancelling releases it. Accepting moves ownership and writes two ledger rows, `transfer_out` and `transfer_in`.

**The hold is the one obligation this design puts on this plan rather than on the primitive.** The primitive stores no `expired` status and runs no sweep, so a pending donation that has passed its date cannot release the hold by itself: a date nobody reads cannot release money. The release therefore happens from this plan's own path, is idempotent, and is stated in Constraints. It is the single place in the transfer design where an expiry needs a reader.

**A membership card is a different instrument that now shares the row.** `6.1-membership-tiers-and-rights.md` (which supersedes `6.1-membership-and-loyalty-points.md`) owns its own dormancy, claim window and void against `spree_membership_cards`, and states plainly that a membership is never stored as a `Spree::GiftCard` — so the shared row carries a membership card there and a gift card here, and neither plan's card semantics touch the other's. What the two share is the journey, not the instrument: a membership has no balance for a hold to sit against, so its pendency is a status rather than an `amount_authorized`, and the "release the hold, the balance never changed" rule below has no analogue there.

### Welfare grants

A welfare card's grant is **a kind of the shared grant row** (`6.1-grant-and-benefit-primitive.md`), not a table of this plan's — the primitive's own ruling names it as the worked example of a kind that is neither consumable nor expiring, because the card is issued immediately and carries its own amount and expiry. What that row does not carry, and this plan's small side table keyed to the grant does, is who granted the card and on which campaign's or subsidy's authority, plus when it was claimed; the arrival time is the grant's own `granted_at` and the claim is its status. The card is issued immediately, as the store does owe it, and the grant records that it was not bought. A welfare card is not taxable on issue for the same reason a bought card is not (`6.0-6.1-loyalty-gift-cards-store-credits.md:92`): the tax is due on what the card later buys.

### Store API surface

| Method and path | Purpose |
| --- | --- |
| `GET /api/v3/store/customers/me/gift_cards` | **Existing** |
| `GET /api/v3/store/customers/me/gift_cards/:id/ledger` | The balance-details page |
| `GET /api/v3/store/gift_card_products` | Denominations and available cover artwork |
| `POST /api/v3/store/gift_card_donations` | Give a card away |
| `GET /api/v3/store/gift_card_donations/:token` | Public, token-addressed: the recipient sees the gift before signing in |
| `POST /api/v3/store/gift_card_donations/:token/accept` | Claim it, optionally creating an account |
| `DELETE /api/v3/store/gift_card_donations/:token` | Cancel while pending |
| `POST /api/v3/store/gift_cards/:id/activate` | Activation, addressed by the card's code |
| `GET /api/v3/store/welfare_gift_cards`, `POST .../:id/claim` | Campaign and subsidised cards |

Purchases run through `6.1-scenario-purchases.md` with `kind: 'gift_card'`, so the twelve per-scenario payment endpoints collapse to one create plus a channel.

## Migration Path

1. Confirm which parts of the official phase 2 design this plan implements and which stay upstream, so the two do not diverge. Phase 2 is unbuilt and its plan was last updated 2026-09-14.
2. Cover artwork and the welfare grant row, which need no purchase flow.
3. The ledger, and the balance-details endpoint that reads it.
4. Donation and the token-addressed claim — a row of `6.1-transfer-primitive.md`, landing with its other two consumers in one change so the set never runs two transfer vocabularies at once. This step owes the primitive the hold release named below.
5. Card-products read and the purchase flow, gated on phase 2 landing and on the "several cards on one order" question.
6. Activation only if Open Question 3 requires it.
7. Regenerate types, Zod, OpenAPI and SDK tests as each step lands.

## Constraints on Current Work

- **Do not add a second prepaid-instrument flag or a second gift-card model.** `spree_products.gift_card` is the flag.
- **Do not let `Spree::GiftCards::IssueFromOrder` grow a discriminator.** It runs on `order.paid`, which only a cart order reaches. A scenario purchase creates no `Spree::Order`, so a mini program gift card and a membership — which share the `spree_products.gift_card` flag — never arrive there. Anything keying on the flag must still ask what kind of product it is before acting, but the issuer is not that place.
- **Do not put recipient or donation fields on `spree_line_items`.** Both have their own tables, for the reasons the loyalty plan gives at `:26`.
- **Do not let a card code into an event, a log, a serializer or an image.** Only the recipient's delivery carries it.
- **Do not model a bought card's sale as taxable on issue.** The tax follows what the card buys.
- **Do not permit paying for a gift card with a gift card or store credit.** The loyalty plan sets this at `:95` to stop one balance being laundered into another.
- **Do not release a pending donation's hold by editing the card's balance.** Release the hold; the balance never changed.
- **Do not leave a hold on an expired donation, and do not wait for a sweep.** The primitive stores no `expired` status and runs no job, so this plan releases a stale hold from its own read path, idempotently — a card whose pending donation is past `expires_at` is spendable again the next time it is read or spent. A date nobody reads cannot release money, and this is the one place the design accepts a reader because of that.
- **Do not stretch this plan to cover a membership card.** It is not a `Spree::GiftCard`, so this plan's contract implementation covers prepaid instruments only. `6.1-membership-tiers-and-rights.md` owns the membership card's tables and its four transitions, implements the same contract on its own card, and neither plan gates the other.

## Open Questions

1. ~~**Does the ledger belong in core beside `StoreCreditEvent`, or in the extension?**~~ **Settled 2026-09-18: neither — it is a row of the shared ledger primitive** (`6.1-ledger-primitive.md`), which is core and carries the points ledger and the distributor's earnings too. The question assumed this plan designed a table of its own; the ruling removed that assumption, and the card's history now costs this plan no design at all.
2. **What does "several gift cards on one order" mean now?** The loyalty plan deliberately keeps one card per order in 6.1 and defers the rest (`:22,138`). The mini program sells bundles and card products, which is several. Until this is answered, neither the purchase flow nor the coupon bundle in `6.1-coupon-wallet.md` can be built, because both are instances of it.
3. **Is an activation step required for prepaid instruments in this market?** Half answered, and the half that was missing is the one that blocked design. The **product** question is settled: the mini program ships a dormant-then-activate step for gift cards, not only for membership cards — `giftCardActive` is called with the card's own code when its status is `not_active`, and the same sheet knows `used`, `expired`, `card_refund` and `frozen` (`miniprogram/pagesE/components/gift-card/detail-pop/detail-pop.js:60-77`). So the status model has to carry a dormant state, and it has to reconcile the client's five states with `Spree::GiftCard`'s four. Whether it is a *regulatory* requirement is still not determinable from this repository, and the operator's compliance side should confirm it — but nothing waits on the answer now, because the model is the same either way. The membership plan reaches the same product conclusion from its own client evidence and **independently**: its card is dormant for the same reason, but it is not a gift card and does not need this ruling.
4. **Are welfare cards a marketing expense or a liability with a campaign attached?** The ledger treats them as value owed from the moment they are issued. If the operator instead recognises them only when claimed, the grant row needs a second state and the balance reader has to exclude unclaimed cards.

## References

- `spree/core/app/models/spree/gift_card.rb:25-46,194-201`, `spree/core/app/models/spree/gift_card_batch.rb`, `spree/core/app/workflows/spree/gift_cards/{apply,remove,redeem,cancel}.rb`
- `spree/core/app/models/spree/store_credit.rb:121-183`, `spree/core/app/models/spree/store_credit_event.rb:24-56` — the ledger pattern to follow
- `spree/api/config/routes.rb:56,120,742-743,856` — the existing card surfaces, Store and Admin
- `docs/plans/6.0-6.1-loyalty-gift-cards-store-credits.md:3,12,20,22,26,88-106,127,131,132,138` — the phase 2 design and its binding constraints
- `docs/plans/6.0-digital-assets.md` — the digital delivery profile a card product uses so it never creates a shipment
- `docs/plans/6.1-scenario-purchases.md`, `6.1-coupon-wallet.md`, `6.1-miniprogram-extension-architecture.md`
- `docs/plans/6.1-transfer-primitive.md` — the shared row this plan's donation becomes, its three-method contract, and the hold release this plan owes it
