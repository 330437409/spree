# Fork decisions — the Spree extension track

> **Fork-only plan.** Ours, never submitted to `spree/spree`. The tracked
> plans beside it are upstream's and are read-only to us; our decisions are in
> `fork-decisions.md`, never in upstream's `decisions.md`.

This file holds **our** decisions. `docs/plans/decisions.md` is **upstream's**
file: it is tracked, it changes with every upstream release, and we do not edit
it. Everything here was originally appended there by mistake and moved out on
2026-09-18, when the fork was resynchronised with upstream.

**Rules.** Read upstream's log for upstream's decisions; record ours here. Cite
an entry by its dated heading, never by line number — this file grows and
upstream's moves under us on every merge. Plans in this set that depend on a
decision here name it in their `Depends on:` header the same way they name a
plan.

The entries below are in the order they were written.

## 2026-09-10 — WeChat Pay lands as a gateway gem; async refunds become an opt-in gateway capability

Two decisions from `6.0-wechat-pay-gateway.md`, both of which reach past that
plan.

**Payment gateway credentials stay on `PaymentMethod` rows, and a gateway is
not an `Spree::Integration`.** This restates the 2026-07-30 cardinality
decision because `6.0-integrations-admin.md` carries a blanket constraint —
"new provider gems must ship an Integration subclass" — that reads as covering
payment gateways. It does not: integrations are the store-level singleton
connection (delivery rates, tax, fulfillment, pickup points), where one
credential serves many config rows, and their `(store, type)` uniqueness
actively fights the merchant-entity model that payments need. A gateway
subclassing `Spree::Integration` is wrong, not merely redundant. A scope note
was added to that plan's constraint.

**A gateway may declare asynchronous refunds, and only such a gateway's refund
rows carry a status.** `spree_refunds` has no status column today — a refund is
either credited (`transaction_id` present) or destroyed on failure — and
`credit_allowed` sums every row. WeChat Pay accepts a refund request and settles
it later, sometimes failing after acceptance, so the truth cannot be recorded in
that shape. Core gains `Spree::PaymentMethod#async_refunds?`, **false by
default**, plus a `processing`/`completed`/`canceled` status on `spree_refunds`
and a `credit_allowed` that ignores no-longer-live refunds.

**Constraints now:** with `async_refunds?` false nothing about the refund path
may change — Stripe's existing refund specs are the guard, and they must pass
untouched; and nothing is added to the refund path that cannot be expressed for
both kinds of gateway. WeChat's own statuses map non-trivially and the mapping
is deliberate: only `SUCCESS` and `CLOSED` are terminal per WeChat's own
documentation, so `SUCCESS` is `completed`, `CLOSED` is `canceled`, and — the
part worth stating — **`ABNORMAL` stays `processing`, not `failed`**. An abnormal
refund is non-terminal and can still succeed, so calling it failed would both
misstate the money to the operator and corrupt the balance: `credit_allowed`
excludes failed refunds, so a still-live refund marked failed would free that
amount as creditable while WeChat could still complete it — a double-refund
path. The provider's own status goes in the refund's `metadata`, which the model
and the admin serializer already carry.

The general rule, which any future async-refund gateway inherits: **a
non-terminal provider state must never be mapped onto a terminal canonical one,
however alarming the provider's wording is.**



## 2026-09-17 — Mini-program feature gems live in `spree/extensions/`, a recorded deviation from the local-extension ignore rule

Plan: `6.1-miniprogram-extension-architecture.md`.

The plan recommended `spree/extensions/<name>/` for its feature gems and `.custom-extensions/` turned out to be this repository's actual convention: `.gitignore:81-82` ignores that directory under the comment "Local custom extensions (developed outside the core monorepo tree)", and `spree_crm` is a working precedent — ten migrations applied in `server/db/schema.rb`, server-side migrations mounted from the engine rather than copied into `server/db/migrate/`, routes added through `Spree::Core::Engine.add_routes`. So the plan's recommendation contradicted a stated rule and had to be either withdrawn or recorded. It is recorded.

**Decision.** The eighteen feature gems live in `spree/extensions/<name>/` with flat gem names (`spree_member_tiers`), and payment gateways stay in `spree/providers/<name>/`. `.custom-extensions/` remains what its ignore rule says it is — a place for one-off local work — and nothing in this set goes there.

**Consequences for other work.** The three reasons are that `.custom-extensions/` is gitignored, so CI cannot see a gem placed there while the plan's own constraint requires a new gem to be wired in three places or its specs never run; `spree_crm` has no git remote at all, making it a single copy; and `scripts/worktree/setup.sh` never provisions the directory, so a worktree created the documented way has neither the gems nor the `server/Gemfile` line that names them. `spree/extensions/` reuses the three-level path glob at `server/Gemfile:29`, which the comment there says exists for `spree/providers/*`; each gem still needs its own `gem '…'` line and its own `eval_gemfile('../../common_spree_dependencies.rb')` plus `gem 'spree', path: '../../'`. These gems are committed to the author's fork, not upstream — they must not be offered as a pull request against `upstream/spree`. Before the first new gem lands, the CI project list has to gain the two gems already in the repository that are missing from it: `.github/workflows/tests.yml` lists neither `providers/wechat_pay` nor `providers/social_auth`, so their specs have never run in CI.

## 2026-09-17 — A mini-program "site" is a seller, and every Store API request is seller-scoped

Plans: `6.1-miniprogram-extension-architecture.md`, `6.1-seller-service-area-routing.md`.

The mini program sends a `siteId` on every request (`miniprogram/static/config/api.js:42`) and calls the resulting object a site. Reading the restored client made it look like a place of business — it carries business hours, a map point and a twenty-five minute delivery promise — and the plan accordingly proposed `Spree::StockLocation` with `kind: 'store'`, having rejected Store, Channel, Market and a new model. The author has ruled otherwise: **a site is a seller.** The platform is an operator only, and the seller is the unit a customer shops from. The catalogue is the one exception, and the entry below corrects it: products are the operator's master catalog, and what a seller owns is its *offer*.

**Decision.** `siteId` resolves to a `Spree::Seller`. The seller's premises is that seller's own stock location (`Spree::StockLocation#seller_id`, `kind: 'store'`), so the client's single `site` object is a seller *and* its one stock location collapsed into one shape, which is what it looks like when each seller has exactly one premises. Resolving the seller is therefore request-scope infrastructure rather than a delivery detail: every Store API call in this application is made in the context of one seller, and the `resolve_seller` step in `6.1-seller-service-area-routing.md` is that mechanism rather than a routing-only helper.

**Consequences for other work.** Spree's Store API v3 is store-scoped and carries no seller context, so seller scoping has to be added as a first-class capability before any feature gem can be correct; it precedes Wave 0 rather than sitting inside it, and `6.1-seller-service-area-routing.md` is promoted to the keystone accordingly. Every gem in the set is seller-scoped at the point where a seller is chosen — not in the catalogue, which the entry below establishes is store-wide and the operator's. `6.1-site-scoped-pricing.md` is renamed `6.1-seller-scoped-pricing.md`, and per-seller invoicing is now the expected reading because the platform does not sell. Membership entitlement is explicitly **not** settled here and stays an open question: a seller-scoped system does not say whether a tier or a point balance belongs to one seller or to the platform across sellers.

## 2026-09-17 — A membership and a points balance belong to the customer, not to a seller

Plan: `6.1-membership-and-loyalty-points.md`.

The decision above — a site is a seller — left the tenancy of a tier and a point balance open, and the membership plan had drafted around the ambiguity: `spree_memberships.seller_id` nullable, with null meaning store-wide and a value meaning the seller-scoped SVIP the mini program sells, and the balance already per customer. The author has ruled: **a membership belongs to the customer.**

**Decision.** The member, the tier and the points balance are customer-scoped and span every seller in the store. `spree_memberships` keys on `(store_id, customer_id)` and carries no seller column; `spree_loyalty_accounts` likewise. The purchase that produces an entitlement is still seller-scoped — the cart, the order and the payment belong to the seller that sold — but the entitlement it grants does not.

**Consequences for other work.** The programme's participation allowlist is a list of **sellers**, not stock locations, so `spree_loyalty_programme_stock_locations` becomes `spree_loyalty_programme_sellers`, and "site" anywhere in that plan now means a seller rather than a row in a stock-location table. Because one balance is spendable at every seller, a redemption at one seller may be funded by points earned at another, so every ledger entry records the seller and the order that earned or spent it even though the balance itself is not divided by seller — without that attribution the platform cannot settle between sellers. The client's `isSvipSite` flag and `getSvipSiteList` endpoint are not explained by this ruling and remain a narrowed open question in the plan.

## 2026-09-17 — The catalogue is the operator's master catalog; a seller holds offers, not products

Plans: `6.1-miniprogram-extension-architecture.md`, `6.1-store-api-miniprogram-gaps.md`.

The author supplied the missing premise: the site belongs to the operator, a seller is the operator's franchise shop, and a seller's role is to distribute the site's products. Upstream already models exactly that, and the model shipped before we looked. `6.0-multi-vendor-marketplace.md:202-210` records Decision 11 as shipped on 2026-08-16: several sellers may sell the same item and **the seller lives on the variant, not on a separate offer record**. A **master product** is a product whose `seller_id` is nil — the marketplace's shared catalog, the operator's. A seller's row on one is an **offer**, and an offer *is* a variant: `variants.seller_id` and `variants.delivery_profile_id` are kept on a master product and blanked on an owned one, `Variant#resolved_seller` and `Variant.for_seller` resolve the seller either way, `Seller has_many :variants` is exactly the seller's rows on master products, and `Products::SelectBuyBox` picks the offer a product leads with. The Store API already returns `seller_id` on every variant and the buy box winner's price. The half that had not shipped is being delivered by upstream pull request #14576 *Let sellers list offers against master catalog products* (branch `feat/seller-master-catalog-ui`, open), which adds `products.open_to_sellers`, a `status` on every variant with its own review lifecycle recorded on `Spree::ProductSubmission#variant_id`, and the operator and seller surfaces — implementing `docs/plans/6.0-seller-master-catalog-listings.md`, which is not in this repository until that pull request merges.

**Decision.** A mini-program site is a seller, and a seller distributes the operator's catalogue by holding **offers** — variant rows carrying its `seller_id` on master products. The catalogue itself is store-wide and the operator's. A seller owns a product only where the operator also lets it submit its own (`6.0-seller-product-submission.md`); that is the owned-product case, and it is not the franchise case this application models.

**Consequences for other work.** A seller-scoped catalogue is reached through `Variant.for_seller`, never `Product.for_seller`. On a master catalog `products.seller_id` is nil, so scoping a product list by it returns the operator's listings rather than the seller's offers — an empty or wrong storefront, and it fails silently because both are valid relations that return rows. The pattern is `Product.joins(:variants).merge(Variant.for_seller(seller))`. `Products::SelectBuyBox` takes `product:`, `currency:` and `option_value_ids:` and **has no seller parameter**, so in a seller-scoped storefront it leads with a first-party or rival offer; constraining it is a design requirement rather than a tuning detail, and the service is swappable through `Spree::Dependencies.product_buy_box_service`. The client's `shopId` is a seller's warehouse — `Spree::StockLocation` carrying that `seller_id`, which is where its offers' stock sits, matching the upstream rule that prices are the seller's and stock is in the seller's warehouses. `open_to_sellers` and `variants.status` are unmerged: no gem in this set may depend on either until #14576 lands, and once it does, every shopper-facing read of a product's variants must exclude rows that are not `active`.

## 2026-09-17 — Master-product catalog mode with seller offers is committed core scope, not a dependency to wait on

Plans: `6.1-miniprogram-extension-architecture.md`, `6.1-store-api-miniprogram-gaps.md`, `6.0-multi-vendor-marketplace.md`.

The author pointed at [spree/spree#13323](https://github.com/spree/spree/issues/13323) and said the merge is written into the plan. It is. That issue is the roadmap for open-sourcing the multi-vendor module into core, and its own words are **"we will open source most of the new, improved multi vendor module as part of Spree core (for free!)"**, followed by an eleven-item checklist whose last entry is **"Master-product catalog mode with Vendor offers"** — the item pull request #14576 delivers. So the offer model is not a proposal to schedule around; it is a committed core deliverable on a public checklist, and the plan set designs against it rather than carrying a fallback.

**Decision.** Treat master-product catalog mode as core platform scope that will arrive. A gem in this set consumes `products.open_to_sellers` and the per-variant `status` with its review lifecycle; none of them may build a parallel listing, offer or seller-product-approval mechanism. This narrows the previous entry's caution: the *plans* may depend on those columns from now on, because whether they arrive is settled — only *code* waits, and only until the migration exists.

**Consequences for other work.** Reconciling the OSS/Enterprise scope table at `6.0-multi-vendor-marketplace.md:19` against #13323's checklist found one substantive gap: ten items map onto existing rows, and "Master-product catalog mode with Vendor offers" appeared nowhere in the table despite being committed core scope. It is now a row pointing at its sister plan `6.0-seller-master-catalog-listings.md`. ("Payment method agnostic" needs no row — it is a property of the payment split, not a scope item.) The Enterprise boundary is unchanged, and restated here because it bounds what these plans may assume for free: **payout reporting and health monitoring, the Shopify and WooCommerce integrations, and the category mapper stay paid**, while the commission engine, order and payment splitting, the fund ledger and basic Stripe Connect payouts come to core per that table's 2026-07-15 amendment.


**Re-homed 2026-09-18.** The table in upstream's `6.0-multi-vendor-marketplace.md`
was reconciled against that pull request's eleven-item OSS checklist, and
master-product catalog mode — the one committed item with no row anywhere — was
added there. Recorded here because the reconciliation is ours; the table itself
is upstream's document and is not annotated by this fork.

## 2026-09-17 — The operator funds the member discount; core's seller ledger assumes the seller does

Plans: `6.1-membership-and-loyalty-points.md`.

The author ruled that the operator funds the member discount (Open Question 7 of the membership plan). The ruling also settles the shape question that sat beside it: one party funds it and that party is not the seller, so the tier catalog is the operator's, store-wide, priced with `automatic` rather than `fixed`, and `spree_loyalty_programme_sellers` governs who awards and accepts points rather than whose prices are discounted.

**Decision.** The operator bears the difference between the base price and the member price, and the seller is paid as though the discount had not happened.

**Consequences for other work.** Core documents the opposite position as its v1 stance, so the ruling has to be paid for explicitly rather than configured. `Spree::Commissions::CalculateLine.base_for` charges the fee on `subject.taxable_basis` and says that "discounts come off either way … a promotion is the seller's concession in v1" (`spree/core/app/services/spree/commissions/calculate_line.rb:77-82`). The seller's earning agrees: `SellerTransfers::Create#earned_amount` is `order.total` less the commission lines (`spree/core/app/workflows/spree/seller_transfers/create.rb:106-119`), so **any reduction the customer sees reduces the seller's payout automatically, with nothing warning**. Funding it from the operator therefore requires an explicit credit row to the seller, for the seller's shortfall against their base-price net rather than for the whole discount, because the commission base stays where the 2026-09-17 commission ruling put it.

That credit has no home yet. `SellerTransfer::KINDS` is a frozen two-value constant (`%w[earning refund_reversal]`) and `validates :kind, inclusion: { in: KINDS }` binds that array when the class loads, so an extension cannot add a third kind. The settlement machinery itself needs no change: `SellerPayouts::Sweep` partitions unsettled rows by **sign**, not by kind (`rows.partition { |row| row.settlement_amount.negative? }`), so a positive row of any kind is swept into the payout and `SellerBalance` follows it. One core change — a `subsidy` kind plus the workflow that writes it — therefore buys funding that the existing machinery already pays for, whereas a gem-local second ledger does not, because the sweep and the balance would never see it.

**Corrected 2026-09-18: it is five edits, not one.** An audit of the membership plan checked the sentence above against the code and found the sweep is the only place that reads by sign. Four others read by kind, and each would silently mishandle a `subsidy` row: `scope :awaiting_provider` is `earnings.retryable.where(payout_id: nil)` (`spree/core/app/models/spree/seller_transfer.rb:91`), so the credit would never be handed to the payout provider; `Spree::SellerTransfers::Reverse` finds its counterpart with `.earnings.find_by(order_id:)` (`reverse.rb:42`) and `SellerTransfer#reversible_amount` returns zero unless the row is an earning (`:127-128`), so a subsidy could never be reversed when the sale is refunded; and `refund_id` carries a non-partial unique index (`db/migrate/20260822000001_create_spree_seller_transfers.rb:46-47`) whose behaviour the reversal's `RecordNotUnique` recovery is built on (`reverse.rb:52,70`). The ruling of 2026-09-18 is therefore **five coupled edits**, and the membership plan now carries them as its core change with the files named. This is the same "the operator owes a seller money" shape as a points redemption at one seller against points earned at another, so both should be settled as one mechanism. A first-party order needs no row at all: `SellerTransfers::Create#ensure_creditable` halts when the order has no seller, because the money is already the operator's.

## 2026-09-17 — Commission follows the actual selling price, for a member price and a promotional price alike

Plans: `6.1-membership-and-loyalty-points.md`.

The author ruled that a seller's commission is taken on the actual selling price — a member price and a promotional price alike (Open Question 8 of the membership plan). The ruling confirms what core already does rather than changing it.

**Decision.** `base_for` keeps reading `subject.taxable_basis`, which is `[amount + taxable_adjustment_total, 0].max` (`spree/core/app/models/spree/line_item.rb:246-270`). A promotional discount reaches it through `taxable_adjustment_total`; a member price arrives already reduced as the line's own price. Either way the fee follows what the customer paid. **No core change to `base_for`**, and no change to the "a promotion is the seller's concession in v1" position that the funding ruling has to pay for separately.

**Consequences for other work.** A price-list member price leaves no `Discount` row at all — the walk returns a `Spree::Price` (`spree/core/app/models/spree/pricing_provider/internal.rb:36-38`) and the line records where it came from in `price_list_id` and `price_source` (`spree/core/app/services/spree/carts/price_items.rb:97`) — so the rule needs nothing from an extension to come out right, and the operator-funded subsidy equally cannot be read off a discount row. The subsidy is the seller's shortfall against their base-price net, `discount × (1 − commission rate)`, so **it must be calculated and persisted when the transfer is created and never recomputed later**, because the base price it compares against can move after the sale; `SellerTransfers::Create` already runs once at completion, so the credit snapshots with it. The formula's dependence on the rate is arithmetic rather than a defect — the operator receives less commission precisely because the customer paid less, and its net is the same whichever base the fee is charged on. Because `Spree::Commissions::Context#commission_basis` weighs a value band on that same discounted figure (`spree/core/app/models/spree/commissions/context.rb:54-68`), a member discount can drop a sale into a lower band and lower the rate with it; that is worth knowing before commission bands are set for a store that runs a membership.

## 2026-09-17 — A purchasable membership rides the gift-card flag, and that flag stops meaning stored value

Plans: `6.1-membership-and-loyalty-points.md`, `6.0-6.1-loyalty-gift-cards-store-credits.md`, `6.1-gift-card-purchase-and-transfer.md`.

The author ruled that a purchasable membership is a gift-card-flagged product (Open Question 3 of the membership plan). Reading the mini program found the client agreeing with the ruling and showing why it is the right one: a membership is bought through `vip/addOrder` then `vip/payByWxMp`, which sits structurally beside the gift card's `gift-card/addOrder` then `gift-card/payByWxMp` and, like it, never touches the cart; and what the customer receives is literally a card, held in the wallet "待激活/赠送" (`miniprogram/pagesB/cards/mine/mine.wxml:20-21`), giveable to a friend with a claim window after which "会员卡退回账户中" (`miniprogram/pagesB/cards/presentation/presentation.wxml:19`), and moved through `activation` plus donation `success` → 已赠送, `invalid`/`recycled` → 已失效, `activation` → 已激活, `expired` → 已过期 (`miniprogram/pagesB/cards/record/record.js:49`).

**Decision.** `spree_products.gift_card` marks a purchasable membership as well as a gift card, and no second non-physical flag is added — which is what the loyalty plan's own constraint demands (`6.0-6.1-loyalty-gift-cards-store-credits.md:131`). The flag's meaning therefore widens from "prepaid money the store owes" to "prepaid and not a physical good". A membership is never stored as a `Spree::GiftCard`: it has no face value to spend, and its home is `spree_memberships`.

**Consequences for other work.** Three of the five switches the flag throws are wrong for a membership and must branch on the kind instead: `promotionable` false and exclusion from price lists (a membership sale should be discountable), refusal of payment with a gift card or store credit (buying a membership from a credit balance is ordinary), and non-taxability on the grounds that the tax falls on what the card later buys (a membership is a service period, and the sale is taxable in many jurisdictions). The two that travel are no inventory tracking and a digital delivery profile, and the second is already core's job through the delivery profile and `Product#digital?` rather than the flag's. The one thing the flag does **not** disturb is `Spree::GiftCards::IssueFromOrder`. This entry originally claimed the opposite — that because a membership was "issued on `order.paid` too", the flag stopped being the discriminator and the issuer had to key on the linkage instead. **That claim is withdrawn**, same day, by the scenario-order ruling recorded below: a membership is issued on a scenario order's settlement, which creates no `Spree::Order`, so `order.paid` never fires for one and the workflow cannot reach it. And because paying issues a **dormant** card whose term begins at activation or at a claim, "issued when the order is paid" is no longer the same as "the term has started": `spree_memberships.starts_at` is written on activation, and its `status` carries `dormant`/`active`/`gifted`/`recycled`/`expired`. The membership shares the activation-and-transfer *situation* with `6.1-gift-card-purchase-and-transfer.md` — dormant state, claim window, return to the account, `recycled` void — and not its recipient half, because a membership is given through the wallet rather than by naming an email address at checkout. **Superseded in part, same day:** the membership card does not inherit that lifecycle from the gift-card plan at all; it owns its own tables and transitions. See "A membership card is its own instrument" below.


**Amended 2026-09-18, re-homed.** This analysis was originally written into
upstream's `6.0-6.1-loyalty-gift-cards-store-credits.md`; it belongs here,
because it is about *our* membership plan. The flag marks a purchasable
membership as well as a gift card, so nothing may treat it as "stored value":
the three switches that only make sense for stored value — `promotionable`
false and exclusion from price lists, refusal of payment with a gift card or
store credit, and non-taxability — do not fit a membership, which is a service
period whose sale is taxable in many jurisdictions. The per-kind behaviour
branches on the kind, not on the flag. `IssueFromOrder` is unchanged: both
purchases are scenario orders (`vip/addOrder`, `gift-card/addOrder`) and never
reach `Spree::Order#paid`, so it stays the cart channel's issuer and a
membership never arrives there. Nothing keying on the flag may act without
asking what kind of product it is — and a membership is never stored as a
`Spree::GiftCard`, having no face value to spend.

## 2026-09-17 — A points redemption is a discount on the order, not a tender

Plans: `6.1-membership-and-loyalty-points.md`.

Open Question 5 of the membership plan asked whether a points redemption applied to a cart is a `Spree::Discount` with `kind: 'loyalty'` (the value `6.0-6.1-split-adjustments.md:194,307` lists but the shipped model does not accept — see the entry on the third `Discount` kind below) or a credit-like payment. The mini program settles it in favour of the discount.

**Decision.** Points spent on an ordinary order are a discount, `kind: 'loyalty'`. The client sends a boolean `useIntegral` on `calc/buyCalc`, `calc/cartCalc`, `order/buySubmit` and `order/cartSubmit`; the server answers with `integralTotal` (available), `integralNum` (points that would be spent) and `integralMinus` (the money off), plus an `integral` entry inside the order's ordinary `minusList` — the same array that holds `coupon` and `first_order` (`miniprogram/pagesF/static/behaviors/settlementBehavior.js:43`, `miniprogram/pagesF/static/template/order-discount.wxml:42-61`). The toggle is opt-in, some activities refuse it through `seckillIntegralFlag`, and the client performs no arithmetic: the money off is the server's number.

**Consequences for other work.** A credit-like design would put points in the tender list beside the gift-card balance, which is not where the client keeps them — a balance is a tender guarded by a payment password ("支付密码将用于下单时礼品卡余额的抵扣", `miniprogram/pagesB/me/password/set-pay/set-pay.wxml:12`), while points are one more reduction row. No new core model is needed; the ledger entry points at the discount, which is core's record. Three details belong in the plan. Points earned are computed on the amount paid **after** the deduction ("商品积分值将按照优惠后（扣除积分抵扣、优惠券等活动）实付金额进行换算"), so the earning rule reads the discounted total rather than the base. The redemption ratio is not the earn ratio: `getIntegralConversionRatio` is the earn side ("每消费N元得1积分") and is display-only — the client multiplies nothing — while redemption has its own figure ("每N个可抵扣1元"). And expiry is a banner over the balance (`willExpireIntegral`/`willExpireDate`), not a row kind: the ledger's filters are only 全部/收入/支出 and its label is a server-supplied `integralExplain`, so an `expire` entry may never need to be listed at all. The points mall is a different mechanism and stays one: `integral-mall/addIntegralMallOrder` creates a standalone order paid wholly in points (`convertType: 'integral'`, with no payment call at all) or in points plus cash (`point_amount`), with no address step, which is `spree_loyalty_rewards` and `spree_loyalty_redemptions` rather than a cart reduction.

## 2026-09-17 — The growth-value ranking is a backend subsystem, not a read-only view

Plans: `6.1-membership-and-loyalty-points.md`.

Open Question 4 asked whether the growth-value leaderboard is a Spree concern at all, and recommended that the ledger feed a read-only ranking view while "the ranking itself belongs to whichever system already computes it". Reading the mini program shows that recommendation cannot stand as written.

**Decision.** The per-customer growth-value **ledger** is ours, and the **ranking** is a real subsystem that must be named as either this backend's or an explicit external dependency. It cannot be left unattributed, because the client embeds a user's rank in ordinary endpoints: the goods-category response feeds `nationalRankingNum`/`siteRankingNum`/`withinLine`/`beforeValue` straight into a product listing (`miniprogram/pagesB/god-user/goods/goods.js:99-105`). Either this backend computes the ranking, or every listing is allowed to call the service that does.

**Consequences for other work.** The client is strictly read-only, which fixes the server's contract. It never sorts, so ordering is server-authoritative (`miniprogram/pagesB/god-user/center/center.js:123-134`); it draws page one's first row at **rank 4**, so the server supplies a podium snapshot *plus* a globally ordered paginated tail (`:183-186`); and it renders positions the server computes — `nationalRankingNum`, `siteRankingNum`, a capped `rankingNum` such as `"50+"`, `withinLine`, `onListCount`, the entry threshold `onListValue`, and the gap to the neighbour above on each board (`miniprogram/pagesB/god-user/center/center.wxml:75`). The ranking is per period and per site, with a national rollup, so the ledger needed two things the plan did not carry and now does: a **`period_key`** on `spree_loyalty_entries`, because the detail screen groups by calendar month and the board frames a period with a start and an end (`periodType` 1 = current, 2 = previous, `center.js:90-96`); and a **per-product accrual coefficient**, because 酒量值 accrues at a rate the product itself carries — the client reads `goods.wineCoefficient` and can sort a category by it (`wine:desc`). That coefficient is merchant-managed merchandising data, so it belongs in Custom Fields rather than in a column on `spree_products`. The screen also needs signed explained deltas and a lifetime total (`miniprogram/pagesB/god-user/detail/detail.js:42-44,58-59` — `wineValue` for the period, `wineValueTotal` overall) and period lifecycle metadata (`startDate`/`endDate`/`overDate`/`topN`, with a title that is awarded and retained). One premise corrected: `miniprogram/pagesD/tavern/**` is not this feature — it is referral commission ("酒友") and mentions 酒量值 nowhere.

## 2026-09-17 — A refunded membership moves its term on the server; no client ever sees a refund

Plans: `6.1-membership-and-loyalty-points.md`, `6.1-scenario-purchases.md`.

Open Question 6 asked how a membership term and a refund interact, and noted that `6.1-scenario-purchases.md` raises the same question for every scenario kind. Reading the mini program answers it in a way that serves both: the interaction is entirely server-side, and the vocabulary for it already exists.

**Decision.** A refund moves the entitlement's own **absolute end datetime**, and anything the order granted is reversed by the same order-linked negative-entry pattern the platform already uses. No client gains a refund action. The client has no refund endpoint at all, its membership order list renders no buttons, and nothing revokes a right — so the whole behaviour is expressible in fields it already reads.

**Consequences for other work.** The term is an absolute end datetime (`endDate`/`svipEnd`) the client renders verbatim and never computes, so shortening or voiding it displays correctly with no client change; the membership's `status` takes the `recycled` value the client already renders as 已失效 for a membership card (`miniprogram/pagesB/cards/record/record.js:49`). Three precedents make this house style rather than a new invention: `getUserLevelRecord` carries a `consume_return` ("消费退回") source with a **signed** change keyed to an `orderNum`, so loyalty value is already reversed per order; the platform already withdraws unused exchanged coupons when a membership period ends ("当期会员{{endTime}}结束后，所有未使用的券将撤回并失效"); and a refunded order is already excluded from membership statistics ("已退款订单不计入统计"). The one thing with no precedent is an **already-shipped** annual gift — the client knows only 待发货/已发货/已领取, so recalling one needs a new status. Because a scenario purchase has the same shape, this is the answer `6.1-scenario-purchases.md` should take too: the entitlement's end datetime is the single source of truth, and it is moved.

## 2026-09-17 — The seller dimension of a membership lives in the programme allowlist, not on the membership

Plans: `6.1-membership-and-loyalty-points.md`.

The 2026-09-17 ruling that a membership belongs to the customer left one residual: the client carries an `isSvipSite` flag and a `getSvipSiteList` endpoint, which looked like a second, seller-scoped product. Reading the site record closes it.

**Decision.** A membership is the customer's, and only its **usable scope** is the seller, expressed through the programme's participating-seller allowlist. No seller column is added to `spree_memberships` or `spree_loyalty_accounts`. **The mechanism is corrected below** (entry "The participating-seller boolean is a column on the seller"): the seller axis is a boolean column on the seller, not an allowlist table. The negative half of this decision — no column on the membership — stands.

**Consequences for other work.** The client holds one customer-level `isSvip` flag with one end date and never a site-to-membership map, while gating every SVIP price, purchase, card activation, code exchange and right on a single server-supplied boolean per site (`globalData.site.siteSvip`, `miniprogram/static/state/siteState.js:55`) — a logged-in SVIP sees no SVIP price outside a seller that enables it (`miniprogram/components/received-price/svip-price/svip-price.wxml:6`), and checkout counts an order as SVIP only when the user is SVIP **and** the seller is (`miniprogram/pagesF/settlement/settlement.js:678-685`). That is exactly `spree_loyalty_programme_sellers`, whose "empty means every seller" convention the plan already follows, so the seller axis needed no new table. `getSvipSiteList` needed no reading to settle the question: it is dead in this build, referenced nowhere outside its own definition, as are `vip/getSvipList` and `rights/getUserSvipRights`.

## 2026-09-17 — A membership card is its own instrument, and its lifecycle belongs to the membership gem

Plans: `6.1-membership-and-loyalty-points.md`, `6.1-gift-card-purchase-and-transfer.md`, `6.1-distribution-and-commissions.md`.

The author ruled that the membership card is decoupled from the gift-card plan: it is built against its own table, and activation, gifting, claiming and voiding are owned by the membership gem rather than inherited from `6.1-gift-card-purchase-and-transfer.md`.

**Decision.** A card is an instrument and a membership is an entitlement, so they are separate rows. `spree_membership_cards` holds what is bought, held, given away and voided; `spree_memberships` holds the period a named customer is entitled to, produced by activation. The gift is a third table, `spree_membership_card_transfers`, carrying a plain `membership_card_id` and an opaque `token` from `has_secure_token` — the `spree/core/app/models/spree/company_invitation.rb:21` pattern. The four transitions, 激活 activation, 相赠 gifting, 领取 claiming and 作废 voiding, are this gem's. A membership card is **not** a `Spree::GiftCard` and is never a row in `spree_gift_card_donations`; the two plans share the situation of a card bought dormant and activated later, and share no tables and no workflows. Neither plan gates the other, so the membership card can be built without waiting on the gift card plan's unbuilt phase 2.

**Why separate rows rather than one card that becomes a membership.** Three facts in the client force it rather than merely allowing it. A customer holds **several** dormant cards at once — the wallet counts them ("您有{{cardCount}}张酒小二会员卡待激活/赠送", `miniprogram/pagesB/cards/mine/mine.wxml:20`) — which "one membership per customer per level" cannot express. A card carries facts a membership cannot: whether it may be given away at all (`item.giftTransfer`, `:36`) and the deadline to activate or give it away ("请于{{item._validUntil}}前赠送/激活", `:32`). And a card that was given away stays in the giver's record as 已赠送 (`miniprogram/pagesB/cards/record/record.js:49`), so the card keeps its buyer while the entitlement goes to the claimer.

**A claim and an activation are the same transition, entered from two doors.** The client sends one call and distinguishes the doors with `activateType` — `"my"` from the wallet (`miniprogram/pagesB/cards/mine/mine.js:118-119`), `"relay"` from the received gift (`miniprogram/pagesB/receive-gifts/receive-gifts.js:100-104`). Giving each door its own resource also removes an ambiguity the client itself carries: its `cardId` parameter means a card's id in the wallet and the **giving's** id on the receive page, which the share link supplies (`miniprogram/pagesB/cards/presentation/presentation.js:60`, `miniprogram/static/behaviors/locationLinkBehavior.js:200-202`).

**Consequences for other work.** The public read of a gift is addressed by the giving's opaque `token` and does not live under `customers/me`, because the receive page reads it before sign-in and only asks for login when the visitor activates (`miniprogram/pagesB/receive-gifts/receive-gifts.js:74,97`). The token must therefore never be derived from the card, whose id travels in clear through the wallet, the activate call and the cancel call (`miniprogram/pagesB/cards/mine/mine.js:95,118,160`). `spree_memberships` loses the `dormant` and `gifted` statuses, which were always the card's, and the card carries the `order_id` and the `membership_id` that link the two — concrete foreign keys, keeping the repository's "concrete foreign keys, no polymorphic source" rule (`spree/core/app/models/spree/commission_line.rb:8-9`). Every membership now comes from a card, which is what anchors its period to the order that paid. The distribution plan's payee fork narrows to a single instance: a membership card's gift is no longer a second subject in a transfer table, so Option A against Option B in `6.1-distribution-and-commissions.md` Open Question 1 is a question about the seller ledger's payee and nothing else. One thing this ruling does **not** fix: a gift-card product and a membership product both still carry `spree_products.gift_card`, and `Spree::GiftCards::IssueFromOrder` still keys on that flag alone, so separating the tables does not separate the trigger — the guard belongs in the issuer, not in the schema.

## 2026-09-17 — A cart line carries the shopper's tick, and completion copies only the ticked lines

Plans: `6.1-store-api-miniprogram-gaps.md`.

Open Question 1 of the Store API gaps plan offered three mechanisms for "check out only the ticked lines" and asked the author to rule before the cart work could start. The mini program already answers it, so this one needed reading rather than ruling.

**Decision.** A cart line item carries a persisted `selected` flag. The cart's money is computed over the selected lines, checkout is scoped to them, and completion copies only them. Option A of the question, placed on the **cart's** line item rather than the order's. The Store API surface the client needs is one bulk write over a set of line ids plus a chosen count on the cart serializer.

**Why the client decides it.** The choice is durable cart state, not a transient UI flag: `cart/getCartInfo` returns a `choose` boolean per line beside a server-computed `chooseTotalQuantity` (`miniprogram/pagesF/cart/cart.js:216,319,408`), and `cart/chooseCart` writes it in bulk over a set of line ids as `{ choose, idSet }` (`miniprogram/pagesF/static/api/cartApi.js:7-9`, `miniprogram/pagesF/cart/cart.js:588-605`). No request in the checkout path carries the chosen ids: the settle page is opened as `?type=cart` with none (`miniprogram/pagesF/cart/cart.js:757-759`), `orderSubmit` builds its payload from `calcParams` plus order-level fields only (`miniprogram/pagesF/settlement/settlement.js:868-887`), and `calc/cartCalc` sends the same shape (`:271-279`) — the buy-now path is the one that names goods explicitly, through `calcGoodsList` (`:673-674`), which is precisely the difference between "these items" and "my ticked items". The client also **repairs** the selection rather than only reading it: when the server marks a chosen line disabled the client calls `chooseCart` with `choose: false` for those ids and reloads (`miniprogram/pagesF/cart/cart.js:216-224`), which a flag applied at checkout would never need. And the selection is not one of two modes — the cart offers per-line ticks and a single bulk tick per combo group, which sends every line of that group (`:510-521`), with **no select-all control anywhere**, so "check out everything" is this one state with every line ticked rather than a second mechanism.

**Why the other two options are out.** Option B, one cart per selection, contradicts a single cart per site: `getCartInfo` takes only `siteId` and answers with every line and its flag (`miniprogram/pagesF/cart/cart.js:209-214`). Option C, splitting at checkout, contradicts the selection being enforced before checkout — it gates the cart's own minimum-order verdict and the client repairs it across page loads, so there is persistent state to keep.

**Consequences for other work.** The flag belongs to the cart and not the order because 6.0 separates the two models and completion copies (`6.0-cart-order-split.md`, Key Decision 3): `copy_line_items!` iterates `cart.line_items` (`spree/core/app/workflows/spree/carts/complete.rb:308-316`), so an unselected cart line is never copied and no order ever holds one. That shrinks the question's own objection — that a `selected` boolean forces every promotion, tax and shipping rule to ignore unselected lines consistently — to **cart-side calculation only**, where the client's server already applies it: the minimum-order hint is gated on the chosen count and compares the cart's money figure against the period minimum (`miniprogram/pagesF/static/behaviors/cartBehavior.js:106-113`, called with `chooseTotalQuantity` at `miniprogram/pagesF/cart/cart.js:405`). The shared money readers that must learn the scope are `Spree::Purchase::Totals#quantity` and `#amount`, which read `line_items` directly (`spree/core/app/models/concerns/spree/purchase/totals.rb:9-16`). Two constraints are recorded on the plan: do not model selection as two checkout modes, and do not compute a cart's money over the whole line set and filter it afterwards. The plan's three remaining questions were settled the same way on the same day — promotion selection, the checkout aggregate, and the menu-shaped category response.

## 2026-09-17 — Promotion choice is a per-line preference over a server-computed candidate list

Plans: `6.1-store-api-miniprogram-gaps.md`.

Open Question 2 asked whether the mini program's `cart/changePromotion` is "apply my coupon" — already covered by `carts/discount_codes` — or "choose between two automatic promotions", which is a rule-engine change. The client answers it, and more sharply than the question framed it.

**Decision.** A cart line carries the shopper's **choice among the promotions the server has already found eligible for that line**. The engine must be able to answer "which promotions could apply here", expose them with their labels, remember which one the shopper wants, and price the cart with that answer. A coupon code is not this and `carts/discount_codes` does not cover it.

**Why it is not coupon entry.** The control is rendered only when a line has **more than one** candidate: `wx:if="{{((!item.gift) && (item.promotionList.length > 1))}}"`, labelled 换促销 (`miniprogram/pagesF/static/template/cart-goods.wxml:17`). A coupon entry has no reason to require two. It opens a radio list of the server's own candidates with the current one pre-selected and its label printed from the response (`miniprogram/components/promotion/switch-promotion/switch-promotion.wxml`), and the write names one of them: it sends `{ id: cartId, promotionId, promotionCode }`, where both identities are read off the chosen entry in the list the server supplied (`miniprogram/pagesF/static/behaviors/cartBehavior.js:278-291`). So the code is a second name for a candidate, never customer input, and the cart has no code field at all. The candidate list is per line and server-supplied, each entry carrying `id`, `label` and `code` (`miniprogram/pagesF/cart/cart.js:395`), beside a group-level `promotionDetail` naming the one currently applied (`:400-402`).

**Consequences for other work.** Two things are undecided and belong to the implementation rather than to this ruling: the scope of the preference (per line, per combo group, or per cart — the client sends a line's id with a candidate from that line's list while the currently-applied promotion it displays is the group's), and how a stored preference interacts with a promotion that stops being eligible, which the client already meets — it renders "超活动限制，以非新人价结算" on a ticked line whose new-customer price no longer applies (`miniprogram/pagesF/static/template/cart-goods.wxml:26`). This is a promotion-engine addition with genuine conflict risk and is not to be approximated with a code field.

## 2026-09-17 — The checkout page's aggregate is the price preview, and the recorded confirm-receipt endpoints are not an aggregate

Plans: `6.1-store-api-miniprogram-gaps.md`.

Open Question 3 asked whether the mini program needs a page-level aggregate, and the plan's gap table recorded `order/getWebappOrderConfirmParam`, `orderConfirmRes` and `hasOrderPrice` as forming one — "totals, available payment methods, delivery options, coupon candidates, addresses, invoice titles, in one response". Reading the client, that row is wrong and the aggregate is somewhere else.

**Decision.** The checkout page's aggregate already exists and it is the price call. Extend the preview — `calc/buyCalc` and `calc/cartCalc` — rather than add a page-shaped endpoint beside it. The address book stays a separate customer resource. The two confirm-receipt endpoints need no endpoint at all.

**Why the row was wrong.** Both are the WeChat confirm-receipt handshake, not checkout. `getWebappOrderConfirmParam` takes an `orderNo` and returns the three identifiers `wx.openBusinessView({ businessType: 'weappOrderConfirm' })` requires — `merchantTradeNo`, `orderNo`, `transactionId` — and is called from `confirmReceipt` (`miniprogram/static/behaviors/btnEventOrderBehavior.js:182-207`). `orderConfirmRes` takes an `orderId` and is called from `App.onShow` when WeChat returns with `status === 'success'`, to record that the receipt was confirmed and tell the order list to refresh (`miniprogram/app.js:74-84`). The first is three attributes on the order serializer; the second is a write against the order. `hasOrderPrice` has no caller in this build.

**Why the preview is the aggregate.** The settle page loads with **one** request per entry path — `calc/buyCalc` or `calc/cartCalc` (`miniprogram/pagesF/settlement/settlement.js:239,271`) — and reads **fifty-eight** page-level fields off that response, enumerated as the whitelist at `settlement.js:445`: the payment methods (`payConfigV2VoList`), the coupon candidates and the coupons this cart may not use (`couponDetailList`, `banCouponDetailList`, `bigCouponId`), the points verdict (`integralTotal`, `integralNum`, `integralFlag`), the delivery promise (`deliveryTime`, `speedDelivery`), the invoice types (`invoiceType`, `invoiceSpecialType`), store-credit availability with its refusal reason (`balanceType`, `balanceReason`, `balanceNotPassword`), the discount lines (`minusList`) and the membership flags (`svipSite`, `svipUser`). Addresses are the one thing outside it, and correctly so: they come from the address book, which the same component reads (`miniprogram/components/addr/choose/choose.js:56`) and other pages share.

**Consequences for other work.** Every one of the fifty-eight is a verdict the calculation already had to reach, so folding them into the preview is not the API taking on a screen's layout — it is the same computation reporting what it decided. The plan's recommendation is narrowed accordingly and the migration path's third step no longer promises "one page-level endpoint".

## 2026-09-17 — The ordering menu is a page composition, not a Store API resource

Plans: `6.1-store-api-miniprogram-gaps.md`.

Open Question 4 asked whether the menu-shaped category response is a Spree feature — a first-class `menu` action — or an app-level concern. The plan also carried "a sort-term concept that does not exist yet". Reading the client settles both, and drops the second.

**Decision.** The menu is the app's page composition and does not belong in the Store API. The catalogue half it needs does: a category tree carrying children and counts. No sort-term concept is created.

**Why the menu is not a catalogue resource.** A root menu node is a category id **plus** a `menuType` that selects one of three different fetch strategies for its children: `combo` and `guess` go to the combo-zone list, `market` goes to the market activity goods page, everything else goes to the ordinary children endpoint (`miniprogram/pagesA/make-order/make-order.js:566`). Several of those types are not catalogue nodes at all — `user` and `often` are computed from the customer's own purchase history and are given their own icons by the client (`:294`), `guess` is a recommendation, and `market` is an operator activity zone whose entries are marked to the client as non-catalogue (`:475`). Second-level zone headers carry their own types, `all`, `recommend`, `common` and `market`, and drive the sort-header modal (`miniprogram/pagesA/make-order/make-order.wxml:47`). A node type that switches fetch strategy, over zones that mix the catalogue with customer history, recommendation and merchandising, is a contract this app owns; a storefront-neutral `menu` action would be modelling one page's composition as a commerce primitive.

**Why no sort-term concept.** The two sort lists are the client's own constants, selected by `menuType` rather than fetched (`miniprogram/pagesA/make-order/make-order.js:27-63`, applied at `:360`) — and `order_category/menuChildrenSortTermNew` is defined in the API module and **called nowhere** (`miniprogram/pagesA/static/api/makeOrderApi.js:37-39`). The sorts actually sent are comprehensive, sales, price high-to-low and price low-to-high, plus the customer's own purchase count inside the history zone. Of these the two price orders already exist as `ascend_by_price` and `descend_by_price` on the product Ransack allowlist and comprehensive is the products endpoint's default order, while `ResourceController` already translates a `sort` parameter (`spree/api/app/controllers/spree/api/v3/resource_controller.rb:285-300,338-340`). Only a sales count and the per-customer purchase count need a sortable aggregate, and the second belongs to a zone that is not a catalogue node.

**Consequences for other work.** The plan's catalogue work narrows to the tree with children and counts, and its migration path no longer claims a schema change for sort terms. The filter attributes the app uses (`categoryAttr`, `getCategoryAttrNum`) are the existing `products/filters` surface with counts rather than a new concept.

## 2026-09-17 — A membership purchase is a scenario order, and gifting it is the same purchase with a different purpose

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-scenario-purchases.md`.

Two draft plans answered the same endpoint in opposite ways. `6.1-membership-tiers-and-rights.md` called `vip/addOrder` "an ordinary order, then the standard payment session" and mapped the summary to `Spree::Cart` and the purchase history to `Spree::Order`. `6.1-scenario-purchases.md` listed `vip` as one of its six kinds, and its fourth key decision says a scenario purchase creates no `Spree::Order` unless it genuinely delivers physical goods. The plan that owns the feature and the plan that owns the frame disagreed, and both were still Draft.

**Decision.** The operator ruled it: **a membership purchase is a scenario order.** `6.1-membership-tiers-and-rights.md` registers the `vip` kind and owns what the purchase costs, what settling it issues, and the reads only the kind can answer — the pre-purchase check and the post-settlement read-back. `6.1-scenario-purchases.md` owns the frame, the endpoint, the lifecycle, the payment channel and the customer-scoped list. The purchase therefore creates no cart, no line item, no tax treatment and no shipment.

**Why the client decides it.** Buying a term is one call, and its payload is a package id, the site, the coordinates, a referral code and whether the buyer keeps it or gives it away — no cart, no line item, no address, no quantity, no shipping choice (`miniprogram/pagesB/components/member/pay-suspension/pay-suspension.js:143-148`). An `orderNum` comes back and goes straight to the gateway (`:155,166-172`). Nothing under `pagesB/member/` or `pagesB/cards/` mentions a recipient, an address or a shipment.

Core does ship a way to sell something that needs no address — `Spree::FulfillmentProvider::Digital#requires_address?` is false and `auto_fulfill?` is true (`spree/core/app/models/spree/fulfillment_provider/digital.rb`), selected by `DeliveryProfile#digital?` (`delivery_profile.rb:49-52`). It was considered and refused, for two reasons: it still reaches the customer through a cart and a checkout this app never builds, and the same frame is wanted for the other five kinds, three of which have no delivery profile at all. The term stays a `Spree::Product` with a `Spree::Price` so the operator can price, list and manage it; it is the *purchase* that is not a cart.

**Consequences for other work.** Gifting needs no second endpoint. `svipPurpose` is `1` to keep the term and `2` to give it away (`pay-suspension.js:96`, from 赠好友 at `pay-suspension.wxml:25`), and both produce a **dormant card owned by the buyer**. For `2` the page states it outright — "购买后的会员卡礼品将暂存至【我的-常用功能-我的卡包】中，您可选择将会员卡赠给好友" (`pay-suspension.wxml:41`) — then opens the card wallet instead of the success modal and does not mark the buyer a member (`:95-99,219-224`). That is why `spree_membership_cards.customer_id` stays "the buyer, and it does not move on a claim": a purchase for someone else begins in the same state as one for the buyer, and giving it away is the same transfer either way. Second, `vip/addOrderCheck` is **not** a stock check; it answers whether a term already overlaps, and the page warns "自{{lowerData._lowEndTime}}起，您的权益将变更为{{svipCurList.name}}权益" (`pay-suspension.wxml:62,64`). It is advisory — the page buys on an **empty** answer and on a failure alike (`:85,87`); a non-empty answer opens the warning popup whose confirm proceeds — and it is the visible edge of the one-tier-group-per-customer rule. Sold out is a separate verdict the page holds before it calls anything (`:113`). Third, the gift-card issuer needs no guard at all, and the "discriminator is the linkage" rule this entry first recorded is withdrawn. `Spree::GiftCards::IssueFromOrder` runs on `order.paid`; a membership is issued on a scenario settlement and never reaches it. The gift card's own purchase is a scenario order too — `gift-card/addOrder` carries the same `siteId`/`userId`/`orderGoodsList` shape and no cart, no address and no recipient (`miniprogram/pagesE/gift-card/details/details.js:355-395`) — so on this client the workflow runs only for the storefront cart channel that phase 2 designs it for. The two issuers are disjoint, and neither gem has to learn about the other. The residual exposure is a membership product put into a cart, which is the membership gem's requirement to prevent where a tier binds a product, not something the issuer detects. Fourth, the membership card's link back to its purchase is a `scenario_order_id`, not an `order_id`, since there is no order to point at. This also settles a disagreement between the two membership plans: `6.1-membership-and-loyalty-points.md` Open Question 3 had already recorded that `vip/addOrder` → `vip/payByWxMp` is "structurally identical to `gift-card/addOrder` → `gift-card/payByWxMp`, and neither goes through the cart" — the same reading reached here from the purchase payload.

## 2026-09-17 — The gift-card flag stays shared, and the issuer guard it was said to need is withdrawn

Plans: `6.0-6.1-loyalty-gift-cards-store-credits.md`, `6.1-gift-card-purchase-and-transfer.md`, `6.1-membership-and-loyalty-points.md`, `6.1-membership-tiers-and-rights.md`, `6.1-coupon-wallet.md`, `6.1-scenario-purchases.md`.

Reconciling the shared `spree_products.gift_card` flag after the scenario-order ruling. **The widening stands**: a purchasable membership carries the flag, and its meaning is "prepaid and not a physical good". What does not stand is the consequence recorded alongside it. Three plans were carrying a requirement that `Spree::GiftCards::IssueFromOrder` stop keying on the flag and start keying on "the linkage", on the premise that a membership reaches `order.paid` the way a gift card does.

**Decision.** The premise is false and the requirement is dropped. `IssueFromOrder` keeps keying on the flag alone and needs no discriminator, because it runs on `Spree::Order#paid` and a scenario purchase creates no `Spree::Order`. A membership is issued on a scenario settlement, and so is a gift card bought in the mini program.

**Why the client decides it.** Both purchases have the same shape. `gift-card/addOrder` sends `siteId`, `userId` and `orderGoodsList` — no cart, no line items, no address and no recipient (`miniprogram/pagesE/gift-card/details/details.js:355-395`) — returns an `orderNum`, and settles through `gift-card/payByWxMp`, exactly as `vip/addOrder` settles through `vip/payByWxMp`. Phase 2's cart path is the storefront channel, and on this client it is the only channel that reaches `order.paid` for either product.

**Consequences for other work.** The two issuers are disjoint by construction, so neither gem has to learn about the other and no column is added to a substrate that has none. The residual exposure is a membership product put into a cart, which the membership gem must prevent where a tier binds a product. A third plan carried the same error and is corrected with the rest: `6.1-coupon-wallet.md` was going to extend `IssueFromOrder` so a paid line item could issue coupon codes for a purchased bundle. `coupon_combo/addCouponCombo` sends only `{ couponComboId, siteId }` and settles through `coupon_combo/payCouponComboByWxMp` (`miniprogram/pagesC/coupon-combo/details/details.js:162-172`) — the same scenario shape a third time — so a bundle's codes are minted by that kind's settlement step and the issuer is left alone. This ruling does not settle everything about the flag, though: because the mini program's purchase carries no recipient, phase 2's `spree_gift_card_recipients` is the storefront's substrate and the donation table in `6.1-gift-card-purchase-and-transfer.md` is the mini program's — the recipient is chosen after the purchase, by donating the card, and the blessing message travels with the donation rather than with the purchase.

## 2026-09-17 — A kind is a subclass and a registration, never an enum the client's strings define

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-membership-and-loyalty-points.md`. Applies to every kind list in this plan set.

The membership plan had modelled a right as a content row carrying an `api_type` from a closed vocabulary of ten strings, on the argument that the client switches on exactly those and a kind outside the list renders with a dead button. It then accepted the client's six member-centre section names as "a contract, not operator data". The operator rejected both: the client's logic is worth learning from, but the backend must not be shaped by it, and the answer is the rule/registry approach Spree already uses.

**Decision.** A right is a **registered typed subclass**. `spree_membership_rights` is an STI table whose column is `type` — not `api_type`, which is derived — and each kind is a class under `Spree::MembershipRights::` listed in `config.spree.membership_rights` via `registers_subclasses_via`, `PreferenceSchema` included. The ten strings the client switches on are the **built-in kinds**, not a closed set. The kind list reaches the admin through a registry picker (`GET /api/v3/admin/membership_rights/types` → `{ type, label, description, preference_schema }`) rather than a hardcoded array.

**Why the repository already argues it.** `Spree::CommissionRule` states the principle in its own comment: *"A rule that could only name a record — this seller, this category — left everything else unsayable… Each of those was a schema migration and an edit to the matcher. As a class it is a subclass and a registration."* The same holds here, and harder: an enum column cannot carry a coupon's settings, and each new kind would be a migration plus an edit to the read. The families to copy are `PromotionAction` (`registers_subclasses_via { Spree.promotions.actions }`) and `CommissionRule`, and the controller shape is `Admin::PromotionRulesController#types`.

**Why the client decides which layer is the model.** The client reads the same rights **twice**. The rights page reads one **flat array** — every right with its own `action`, `showName` and `open` flag, no grouping (`yeargift/getYearRightsDetailsVo`, `miniprogram/pagesD/interests/interests.js:128-173`). The member centre reads the same rights **grouped** into six keys (`center.js:245-262`, keys at `center-item.js:35-40`). A flat list keyed by a type string is what a registry produces; the grouping is a second reading of it. So the flat read is the model and the grouping is a projection, and the Store API's rights resource is the flat one.

**Consequences for other work.** A right is an **entitlement**, not a condition — the tier it belongs to *is* the condition, so its shape is `PromotionAction` and not `PromotionRule`, and `isHave` and the `condition` array the client prints are both derived rather than stored. The six member-centre keys are each kind's own `presents_as` declaration, so a kind naming an existing panel joins it with no change to the serializer, the model or the admin; nothing in the backend may enumerate the six. A kind declaring a new panel has no panel in this client until a client release adds one, which is a limit on one view and not on the domain — the kind is still authored, published, listed on the rights page and served by the Store API, so it must not be refused at creation or hidden from the picker. The rule generalises to the rest of the set: wherever a plan is tempted to validate a kind against a literal array, or a serializer is tempted to branch on a kind, the answer is a subclass plus a registration. The first further instance was found by applying it: `spree_membership_grants` carried a `kind` reading `birthday`/`annual`/`member_day`/`monthly` — three of which restate right kinds (`birthday_double_integral`, `give_gift`, `svip_date`) — so the grant now points at the right that caused it and the period is the kind's own computation rather than a case statement in the job.

**The sweep across the rest of the set found two more, and one that already passed.** `6.1-coupon-wallet.md` called `spree_coupon_campaigns` a typed-subclass family in the same sentence that gave it a `kind` column and a `settings` JSON blob — a subclass whose configuration lives in a hash no class owns is a subclass in name only, and the AGENTS.md `PreferenceSchema` shape is what makes the registration mean anything. It is now `type` plus `preferences`, `include Spree::PreferenceSchema`, `registers_subclasses_via { Spree.coupon_campaign_types }` and a `type_must_be_registered` validation, matching `Spree::CommissionRule` line for line, with its six acquisition kinds named as the **built-ins** and the picker reading the registry. The membership history file's `spree_loyalty_rewards` — "typed by `api_type`: a variant, a coupon, or a membership" — is superseded outright; see the entry below. And `spree_point_products.jxe_type` was checked and **passes the rule's own test, so it was left alone**: it cites `Spree::CommissionLine` for its shape, its prose says the issue step is registered by whichever plan owns the thing being issued, and its three known payloads are concrete foreign keys under an exactly-one-of validation rather than a kind validated against a literal array. **One seam is raised rather than settled.** The *issue step* is registered, but the *payload* is a concrete foreign key, so a kind a later plan registers still needs a column — which is the cost `CommissionLine` accepts knowingly, since core's own line carries a fixed set of subjects. The rule's purpose is that a new kind is a class and a registration; here half of it is. Whether a fourth payload is expected decides whether that matters, and no plan currently expects one.

## 2026-09-17 — The membership/points split renamed every points table, and two claims went with the names

**Decision.** The split that produced `6.1-membership-tiers-and-rights.md` and `6.1-points-and-growth-value.md` kept the membership tables' names and renamed every points-side table. `6.1-membership-and-loyalty-points.md` is design history now, and its table carries the mapping row by row: `spree_loyalty_accounts` → `spree_point_accounts`, and **the shape changed with the name** — one row per `(store, customer, kind)` carrying a single `balance`, not one row per customer carrying two balances as four columns; `spree_loyalty_entries` → `spree_point_transactions` plus `spree_point_grants` and `spree_point_allocations`; `spree_loyalty_rewards` → `spree_point_products`; `spree_loyalty_redemptions` → a scenario order; `spree_loyalty_programmes` → store preferences and the membership's grants. Two entries above name the old tables. Neither rename is retroactive: **the rulings in those entries stand and the table names in them do not.**

**Two claims are withdrawn rather than renamed.** The seller-dimension entry concluded that the seller axis "needed no new table" because `spree_loyalty_programme_sellers` already expressed it. **That table has no successor.** Seller scope is a property of the individual grant — a coupon a right issues carries its own `limitSite`/`moreSite` ("seller scope is a property of the individual grant, not of the membership", `6.1-membership-tiers-and-rights.md:659-663`) — and the participating-seller read now has **no owner at all** — the two `vip/*` candidates for it, `getSvipSiteList` and `getSvipList`, are dead in the client and were excluded on 2026-09-17 (see the entry below). So the residual those entries called closed is open again, and the ruling they made is not what reopens it: the entitlement is still the customer's and only its usable scope is the seller. The ranking entry gave the ledger a `period_key`; the ranking is excluded gamification, so no period column survives into `spree_point_transactions` and the growth-value ledger is undivided by period.

**One endpoint is unowned for the same reason.** `delIntegralOrder` — "remove a redemption from my list" — was moved out of `6.1-points-and-growth-value.md` as part of the purchase, but `6.1-scenario-purchases.md:90-98` lists create, list, show and cancel and **no delete**, so nothing owns it yet. Removing a record from the customer's own list is a soft delete on their side and not the same act as abandoning an unpaid purchase.

**Why the plan's own table recorded none of this until now.** Its banner had already declared the file design history and struck three rows — `spree_membership_levels`, `spree_membership_level_benefits` and `spree_loyalty_grants` — while leaving the other six points-side rows reading as live. A reader looking for "what tables does the points programme use" would have found two complete and contradictory sets, one of them in a file that says it is not a plan. The struck rows are now the whole superseded set, which is what the banner claimed all along.

## 2026-09-17 — The birthday is a core customer column, and the member add-on pack is a tier-bound coupon

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-coupon-wallet.md`, `6.1-scenario-purchases.md`.

Two boundaries the membership plan had left open are now ruled by the operator.

**Decision one — the birthday is core.** `rights/getVipBirthday` reads a nullable `birthday` date column on `spree_customers`, and the core customer form accepts it. It is not a gem-owned profile row and not a table of its own. The argument is the one the plan's own open question made: it is generic profile data — any store may want birthday marketing — so a one-column migration on the model that already holds `first_name`, `last_name` and `phone` is smaller and less surprising than a row that has to be joined to be read. The gem keeps the *use* of it — the `birthday_double_integral` right and the period its grant keys on — and core keeps the *fact*.

**Decision two — the member add-on pack is a promotion coupon bound to the tier.** The member centre sells it as 会员加量包 through `rightsCouponsVo`, and it had been left as an open question about whether buying one needed a scenario-order kind of its own. It does not. It is a **bundle of promotional coupons**, so it belongs to the coupon family, and `6.1-scenario-purchases.md:61` already declares a `rights` kind whose own row names both plans — which is the split the ruling makes explicit. Three `rights/*` endpoints follow it to `6.1-coupon-wallet.md` (`getRightsAddDetails`, `addCouponOrder`, and the dead `getRightsAddDetailsCouponList`), and the membership plan keeps exactly one thing: the `add_bag` right that names the tiers which may buy a pack. That is the shape of every other right — the entitlement is the tier's, and the tier *is* the condition.

**Why the pack is not a second registration.** The coupon wallet already owns the bundle shape: a settled `coupon_combo` scenario order mints the bundle's codes into the wallet, idempotently, keyed to that order. A member pack is that object carrying a tier condition, so registering a kind for it would be a third name for one thing. The tier condition is read as eligibility rather than stored on a pack, and the buyer's term end bounds it — which is why the pack detail carries `svipEndDate`, and why the binding belongs to the membership gem rather than to a column on a bundle.

**Consequences.** `6.1-membership-tiers-and-rights.md` drops from 48 endpoints to 45 and from four open questions to two, and its `rights/` family is 7 of 12 rather than 10 of 12. `6.1-coupon-wallet.md` gains the three endpoints and one Store API read, and names the membership plan as a dependency. Neither plan gains a table, and neither gains a scenario kind.

## 2026-09-17 — Two dead `vip/*` endpoints are excluded, and the participating-seller notion is left with no owner

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-membership-and-loyalty-points.md`, `6.1-miniprogram-extension-architecture.md`.

The membership plan carried an open question about `vip/getSvipList` and `vip/getSvipSiteList`: both are declared in the client and called nowhere, and the hypothesis on the table was that `getSvipSiteList` might be a participating-seller read rather than dead. The operator ruled that the pair has no value and is **excluded**.

**Decision.** Both endpoints are 排除, and the open question is withdrawn rather than answered. The membership plan's scope drops from 45 endpoints to 43, its `vip/` family is 19 of 22, and it is down to a single open question. Nothing replaces the package list: `vipBuyPage` already returns it, taking `siteId` as a parameter.

**Why the participating-seller reading was wrong.** The client never asks the server which sites sell a term. Every site object it loads carries its own boolean (`globalData.site.siteSvip`, `miniprogram/static/state/siteState.js:55`), and more than twenty call sites read it — product detail, checkout, the member centre, the profile page. The answer therefore travels on the site resource the caller already holds, and a separate list endpoint would be a second way to ask one question. That is also why it is not membership-domain work: "which sites sell memberships" is a fact about a site.

**What this leaves open, and it is a real gap.** The participating-seller notion now has **neither a table nor an endpoint**. The entry above recorded that `spree_loyalty_programme_sellers` has no successor, and the two `vip/*` candidates that could have read it are excluded. So no plan in this set says where that per-site boolean is authored or stored — a column on the seller, a membership-side allowlist, or a store preference with per-seller exceptions. The **read** is settled: it travels on the seller surface `6.0-multi-vendor-marketplace.md:727` owns (`GET /api/v3/store/sellers`), which the history file already anticipated by refusing to add a second list — not the routing plan's surface, which owns only `location/resolve_seller`. The storage is not. **Resolved by the entry "The participating-seller boolean is a column on the seller" below**: the storage is a column on `spree_sellers`, and the notion has an owner again.

**An earlier citation was wrong on its own terms.** Four places in `6.1-membership-and-loyalty-points.md` and one here named the participating-seller read as "that plan's Open Question 2". It was Open Question 1, so the reference never pointed at it, and the question is now withdrawn.

## 2026-09-17 — The participating-seller boolean is a column on the seller

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-membership-and-loyalty-points.md`, `6.1-miniprogram-extension-architecture.md`.

Two entries above first withdrew the claim that the seller axis "needed no new table", then recorded the resulting absence as a real gap — "no owner at all", then "neither a table nor an endpoint". Reading the client closes it. What the withdrawn entry got right is its negative half, that the seller axis is **not** a column on the membership, so `spree_memberships` and `spree_point_accounts` still gain nothing. What it got wrong is the positive half, that the axis therefore lives in an allowlist table of its own: the axis is a column on the other party, the seller.

**Decision.** The per-site membership boolean is a **column on the seller**. The operator authors it, the seller's own public profile read carries it, and the membership gem only reads it. There is no membership-side allowlist table, and `spree_loyalty_programme_sellers` correctly has no successor: a second table holding one boolean that already travels on the seller resource is the duplication `AGENTS.md` forbids, and the "empty means every seller" convention it needed has no equivalent to remember, because a boolean has no empty state.

**The evidence is new, and it is what makes this a reading rather than a preference.** The client never asks for this value. `home/allConfig` returns a `siteDetails` object and the client assigns that whole object as its site — `miniprogram/static/utils/report.js:24-35` destructures `n.siteDetails` and calls `getApp().globalData.setSiteInfo(o)` — and `setSiteInfo` replaces the global site wholesale (`miniprogram/static/state/siteState.js:68-79`) over a default of `siteSvip: !1` (`:55`). So the flag is a field of the site payload by construction, and more than twenty call sites read it from there. A value that travels on a resource is stored on that resource. This also disposes of the two `vip/*` candidates a second time: neither could have been the source, because the value is already in the payload every screen holds.

**Consequences for other work.** The read the history file already specified stands unchanged — the boolean travels on the seller surface, and there is still no second list. What changes is that the storage is named, so the four places in `6.1-membership-and-loyalty-points.md` that read "neither a table nor an endpoint" and the architecture plan's line that left it open inside the membership plan now state the resolved form. The membership plan needs no endpoint for it. Because a gem patches no core model, the column is a small core addition on `spree_sellers`, the same move async refunds made for the WeChat Pay gateway, rather than a decorator or a parallel table.

**What this sequence is worth recording for.** The gap was manufactured, not found. The withdrawn entry named the right conclusion and the wrong reason (a table that had no successor), the next entry kept the reason's failure and dropped the conclusion, and the third reported the absence as an open question for the author. Three entries moved the question rather than answering it, while every fact needed to answer it was one grep away in `miniprogram/`. A conclusion stated with a weak reason should be re-argued, not withdrawn.

## 2026-09-17 — The points deduction is built beside the points mall, it needs a third `Discount` kind, and points are debited at creation on both paths

Plans: `6.1-points-and-growth-value.md`, `6.0-6.1-split-adjustments.md`.

The points plan's Open Question 4 asked which plan owned the `useIntegral` deduction on an ordinary order, on the reading that the ruling above left the mechanism unowned. The operator ruled that **both** mechanisms are built: the deduction on the ordinary order, and the mall's standalone order. That does not reopen the ruling — its own closing sentence keeps the mall as "a different mechanism" — it settles the half the ruling left open, which is where the deduction is stored.

**Decision.** The deduction belongs to `6.1-points-and-growth-value.md` and is written as `Spree::Discount` with `kind: 'loyalty'`. It adds no endpoint of its own: the boolean rides core's `calc/*` and `order/*Submit`. The mall is unchanged.

**"Reserved" was the wrong word, and it was mine.** The ruling above called `'loyalty'` "the value `6.0-6.1-split-adjustments.md:194,307` reserved". The split plan does list it among the kinds (`:194`) and does say custom discounts write it (`:307`), but the shipped model froze the list at two values — `KINDS = %w[promotion manual].freeze` (`spree/core/app/models/spree/discount.rb:16`), validated with `inclusion: { in: KINDS }` (`:27`) — and the serializer publishes it as a closed enum (`spree/api/app/serializers/spree/api/v3/discount_serializer.rb:8`). Nothing was reserved, and `'loyalty'` is not merely unvalidated: it is not representable.

**The shape to copy is `Spree::Fee`'s, the only unfrozen `KINDS` in core.** Nine of the ten freeze theirs; `Fee` does not, and says why — "Extensions may append their own kinds — the list is validated, not frozen" (`fee.rb:10,14`) — with the validation reading the constant through a lambda so an append is seen (`inclusion: { in: ->(_fee) { KINDS }, allow_blank: true }`, `:25`) and the open tier on the wire (`enum_type_name: 'FeeKind'`, `fee_serializer.rb:8`), which `spree/api/lib/typelizer/open_enums.rb:1-9` names as the archetype. `Discount` becomes the second. The tradeoff is stated rather than buried: because the points gem is an extension, an appended kind makes the generated `DiscountKind` union depend on which extensions are installed. `Spree::Actor` refuses that tradeoff — a frozen `BUILT_IN_KINDS` naming only what core registers, an open union, no autocomplete for an extension's kind (`actor.rb:14-21`). Either shape is defensible; a gem un-freezing a core constant is not, so this lands in `spree/core` and `spree/api`. No column and no migration change: `spree_discounts` already permits a row with no promotion source (`6.0-6.1-split-adjustments.md:353`) and the adjusters already clamp so combined discounts never push an adjustable below zero (`:99`).

**Why not a promotion action, which would need no core change.** The promotion engine reconciles its own rows destructively — `adjustable.discounts.where(kind: 'promotion').destroy_all unless best` (`6.0-6.1-split-adjustments.md:269`, with `:271,275`) — so a discount whose amount depends on a customer's balance and an opt-in boolean, which is not a "best action wins" computation, would be cleared by any recalculation in which no action is best.

**When the points leave the balance (ruled the same day).** At creation, on both mechanisms. The mall's debit is proven rather than chosen: `addIntegralMallOrder` returns `convertType` and the client branches on the **answer**, so a redemption that completes with no payment call at all can only have been settled by the create request (`pagesD/integral/goods/goods.js:226,253,262-267,272`). The deduction is debited in the same act — `order/buySubmit` or `order/cartSubmit` is what persists the `loyalty` row, and the points leave with it. Debiting at payment was refused because it leaves a window in which one balance is spent twice, once in the mall and once here.

**The cost of that ruling is stated rather than left to be discovered.** A mall order that is never paid has a state to sit in (`6.1-scenario-purchases.md:35,52`); an ordinary order does not. `Spree::Order::STATUSES` is `draft`, `placed`, `canceled` (`spree/core/app/models/spree/order.rb:130`), with no outcome for an abandoned purchase, and core runs no job that closes one — so an order submitted with a points discount and then never paid holds those points until something cancels it. The reversal therefore has to exist, and the seam already does: `Spree::Orders::Cancel` declares `hooks :before_cancel, :after_cancel` (`spree/core/app/workflows/spree/orders/cancel.rb:11`) and runs `after_cancel` inside the transaction that stamps the status (`:55-59`). The return belongs in that hook rather than in a subscriber on `order.canceled` (`:66`), because the same transaction that cancels the order must not be able to leave the balance spent if it rolls back. The `loyalty` row stays on the order — it records what the order carried, and the reversal is the ledger row.

## 2026-09-17 — Points are earned by hooking an event, and 评价积分 belongs to the review gem

Plans: `6.1-points-and-growth-value.md`, `6.1-product-reviews.md`, `6.1-miniprogram-extension-architecture.md`.

The points plan's Open Question 3 asked where **评价积分** went, offering two answers — a non-order trigger, or leaving the channel out. It was framed that way because the plan had proposed an extensible **rule family** for earning on the `CommissionRule` pattern and had asked for a ruling on it. The operator declined that proposal, and then stated the principle instead: **earning is extended by hooking an event, because Spree is event-driven** — developing the review gem adds a hook to the earning rules rather than a row to a rule table.

**Decision.** Earning channels are extended **by hooking an event**, not by registering a rule. The points ledger service is the only door into a balance and is public from the gem's first release; a gem that earns points on something that is not an order writes a subscriber for its own event and calls that service. Nothing registers a matcher, nothing is validated against a literal array, and **no `PointRule` family is built**.

**评价积分 goes to `6.1-product-reviews.md`.** The review gem owns both the amount and the trigger, because a review is not a purchase and the order lifecycle cannot reach it. The client divides it the same way already: `integralNum` rides the order payload (`miniprogram/static/behaviors/orderListBehavior.js:66`, read into the prompt at `pages/order/order.js:188-193`) and the review prompt renders it as "得{{N}}积分" beside 立即评价 (`miniprogram/components/order/evaluate-medal/evaluate-medal.wxml:13`), so the server states what a review is worth **before the review exists**. The channel has its own line in the 赚积分 block, "评价有礼获得积分奖励" (`pagesD/integral/mall/mall.wxml:55-56`).

**Why the three settings are not a fourth setting.** 消费积分 (`integralRatio`), 商品积分 (`item.integral`, with a cap on the line rather than on the store) and the 满 N 元门槛 (`shoppingIntegralThreshold`) are all measured on the order the customer actually paid, so they are preferences and one per-line input read by the single subscriber the points gem owns. A channel that is not an order cannot be written that way — that is what makes it a hook and not a setting. **生日双倍 and 会员日双倍 are a third thing again**: multipliers the membership rights own and the points gem reads (`6.1-membership-tiers-and-rights.md:486,489`), registered by neither.

**The 评价 domain undercounts the review gem by three, and the corrected figure is 8.** `6.1-miniprogram-extension-architecture.md` listed the plan as ~5 endpoints, the size of the **评价 domain heading**, but three more are filed under 订单 and annotated **评价体系**: `order/orderEvaluate`, `order/getOrderEvaluateInfo` and `order/orderDeliveryEvaluate`, all graded C (`endpoints-spree-mapping.md:135,147,149`). The row now reads **8**, which is the figure the mapping's own grouping already carried for this domain (`:714`) — so the undercount came from the domain heading, not from the inventory that headed it. **A first pass at this entry said ~7 and this is a correction to it**: the delivery submit was missed because the two *write and read* endpoints were counted as a pair and the second write was not looked for. This is the second misfiling found by reading the endpoints rather than the domain headings — the membership plan's 16 endpoints filed under 优惠券与促销 were the first.

**The predecessor proposal is withdrawn rather than left standing.** The plan said that if the set were ever opened to extension, `Spree::CommissionRule` was the shape to copy "with one change: it only gates (`applicable?` returns a boolean) while an earning rule must also compute, because no sibling holds the number the way `Spree::CommissionRate` does". That paragraph is gone. The shape is a subscriber plus the owner's public service, and the architecture plan now carries the rule so that no later plan re-derives the rule-table reading.

## 2026-09-18 — Verification codes are one table with a non-consuming check, and the payment PIN is enforced in the tender through a core seam

Plans: `6.1-phone-verification-and-payment-pin.md` (new), `6.1-scenario-purchases.md`, `6.1-marketplace-message-relay.md`.

Four rulings, taken together because each constrains the others. **A code is a row** — `spree_verification_codes` carrying phone, purpose, channel, digest, attempts, expiry and the verified/consumed timestamps: the two alternatives, a `generates_token_for`-style signed token with no table (`spree/core/app/models/concerns/spree/customer_methods.rb:29-36`) and `Rails.cache` as the rate limiter already uses, cannot count attempts across processes nor answer what was sent to which number, which the per-phone rate limit and any abuse review both need. **The check does not consume; the write does** — the client posts the same code to `common/checkPhoneCode` and then again to `info/updatePaySetting` (`miniprogram/pagesD/personal/verifi-phone.js:141-156`, `pagesB/me/password/set-pay/set-pay.js:66-82`), so a consuming check breaks the set-PIN flow, and returning a token breaks it differently because the client has nowhere to carry one. **The PIN is its own table** — `spree_payment_pins`, unique per store and customer — rather than a column on `spree_customers`: the birthday entry above settles where **profile data** goes, and a credential with a lockout, a rotation story and an erasure obligation is not profile data. **The PIN is enforced by a core seam**, a registry consulted once in the balance settlement path, not by an API-layer check, which would guard one caller of the tender and leave every other one open; the capability is generic, so it lands in core and is recorded here, the `async_refunds?` precedent.

**Purpose is two values, not six.** No call site sends a purpose — `grep purpose` over the restored source returns nothing — and one verify endpoint serves binding, phone change, password reset and cancellation, so the server cannot enforce a finer distinction than the endpoint that issued the code. The purposes are `account` and `payment`, and the issuing route decides which.

**Consequences for other work.** `6.1-scenario-purchases.md:122`'s Open Question 2 is answered: a balance settlement must present a PIN proof when the customer has one that is required, and the flag the client reads (`balanceNotPassword`) means exactly that fact rather than "this order skips the check" — the client treats it as licence to send an empty string (`miniprogram/components/order/balance-pay/balance-pay.js:76`). `6.1-marketplace-message-relay.md` is **amended rather than bypassed**: it gains a channel registry whose first non-email member is SMS, because the architecture plan forbids a second outbound mechanism.

## 2026-09-18 — The administrative division tree becomes `spree_administrative_divisions`, and the Cainiao province map retires

Plans: `6.1-administrative-division-gem.md` (new), `6.1-seller-service-area-routing.md`, `6.1-cainiao-logistics-provider.md`, `6.1-miniprogram-extension-architecture.md`.

The architecture plan's inventory already ruled this an extraction ("Extraction, not design", `:107`); what was missing was the document, so the rulings are about what moves and what does not. **The model moves verbatim** — `Spree::AdministrativeDivision` with its five levels, GB/T 2260 plus bureau township codes, `dataset_version` and `source`, as specified in the seller plan's `### AdministrativeDivision` section — and gains two columns the existing design lacks: `first_pinyin` and `pinyin`. The client made those necessary rather than nice: the province-level rows of the location list are sorted by `firstPinYin` and derive their A–Z anchors from it (`miniprogram/pagesB/location/list/list.js:250-258, list.wxml:106-111`), so the tree half of that payload cannot be answered from the specified model. (Two corrections the verification pass made to this evidence, neither changing the ruling: it is the location list page rather than the picker — the picker is a three-level province/city/district cascade that neither sorts nor shows anchors — and the rows it sorts also carry a `siteList`, whose half is the seller plan's read.)

**The seller plan keeps everything above the tree**: bindings on `StockLocation`, the polygon and its validator, reverse geocoding and its cache, `Decision`, `resolve_seller` and its endpoint. **The gem is `spree_administrative_divisions`**, at `spree/extensions/spree_administrative_divisions/`, and its reference data ships inside it under `data/administrative/`, imported by a rake task. That is new ground in this repository — no gem ships reference data today, no `data/` directory exists under `spree/`, and every gemspec globs `{app,config,db,lib,vendor}/**/*` (`spree/providers/wechat_pay/spree_wechat_pay.gemspec:25`), so the new gem's glob gains `data` or a packaged gem silently loses its dataset.

**The Cainiao province map retires.** `6.1-cainiao-logistics-provider.md:120-126` ships 34 ISO-code-to-Chinese-name rows because `Spree::Address#state_validate` clears `state_name` and `state.name` answers in English. The tree answers that natively, so `lib/spree_cainiao/province_names.rb` goes and `AddressParams` reads a division — with the ISO-to-bureau crosswalk, 34 rows, joining the gem's versioned mapping assets beside the Tencent and Amap files, which keeps vendor codes out of storage as `Decision 9` requires.

**The "five amendments" the architecture plan names at `:107` are now defined**, because the phrase appeared exactly once in the repository and defined nothing: the DeliveryZone exemption the seller plan already writes at `:559-569`; `resolve_seller` restated as request scope rather than a routing helper; the plan's `StockLocation` columns reconciled with "a gem patches no core model"; this gem named as a dependency in both headers; and the three-place wiring. The reading is recorded here so the next reader inherits a definition rather than a count.

## 2026-09-18 — The ChinaUMS gateway is withdrawn: the client cannot select it, and every scenario has a WeChat path

Plans: `6.1-miniprogram-extension-architecture.md` (the Wave 1 inventory row), `6.1-scenario-purchases.md`, and the withdrawn `6.1-chinaums-payment-gateway.md`.

Reading the client before writing the plan dissolved it. The inventory carried it as Wave 1's "second Chinese gateway alongside `spree_wechat_pay`" at ~8 endpoints, and the endpoints are real — but nothing in the client can select the route. `PayType.UNION` is defined and referenced nowhere (`miniprogram/static/types/commonTypes.js:5`); the settlement page renders `payConfigV2VoList` as customer-pickable rows yet forwards only `payType: 1` (WeChat) or `28` (gift-card balance), every other value collapsing to `null` (`pagesF/settlement/settlement.js:874-875`, with the byte-identical twin at `components/j-format/j-format.js:1573-1574`); and `order/chinaumsWxPay` answers with a payload the client hands straight to `wx.requestPayment` (`pagesF/pay/ctms/ctms.js:181-203`). The shopper pays through WeChat either way: ChinaUMS is an acquiring route behind the same tender, not a tender. The server's own vocabulary agrees — `payTypeText` names 云闪付 and no screen offers it (`static/utils/utils.js:263`).

**Decision.** The gateway is not built. The plan is **withdrawn rather than left as a draft**, because a draft in the inventory is an instruction to a later session. Nothing is left unserved: all six scenario endpoints have a WeChat counterpart — `vip/payByWxMp`, `rights/payCouponByWxMp`, `coupon_combo/payCouponComboByWxMp`, `integral-mall/payByWxMp`, `teamBuy/payTeamBuyByWxMp`, `gift-card/payByWxMp` — so the twelve endpoints the architecture plan describes as differing "only in what is being bought" resolve to six implemented channels and six that are not.

**What would reopen it.** Only the merchant contract. If the operator's acquiring agreement is with 银联商务 rather than with WeChat Pay directly, the route becomes necessary again, and the withdrawn design is the shape to rebuild: the session class, the relayed launch payload (never signed locally), `mini_program` as the only scene, credentials on the payment method, and the refund rule decided by the acquirer's own status vocabulary. `spree/providers/wechat_pay` is the template. `payment_channel` on `spree_scenario_orders` (`6.1-scenario-purchases.md:34`) keeps `chinaums` as a seam for that day; nothing reads it yet.

**Why this is recorded rather than remembered.** This is the third time reading the endpoints corrected the inventory instead of the headings — the review gem's three misfiled 订单 endpoints and the membership plan's sixteen under 优惠券与促销 came first. The cost of the check was one grep, and the saving is a gateway nobody would have called.

## 2026-09-18 — China invoicing uses invoice vocabulary in a fork-only gem, a deliberate divergence from upstream's OSS rule

Plans: `6.1-china-invoicing.md` (new), `6.1-b2b-order-documents.md`.

`6.1-b2b-order-documents.md:90-91` states the rule — "No invoice vocabulary in OSS — no model, number, or document may be called an invoice" — and the architecture plan at `:135` allows it to be amended deliberately here rather than worked around. It is amended, for one fork-only gem, and bounded on four sides: the vocabulary lives only in `spree/extensions/china_invoicing/`, so no core model, API resource or locale key gains the word; the gem **mints no invoice number**, because `6.0-document-numbers.md:117` keeps Spree's numbering out of the legal-invoice business and a Chinese 发票号码 comes from 金税; there is **no PDF stack and no rendering**, because the document arrives from the issuing service as files the client fetches by URL (`pagesB/me/invoice/details/details.js:25-53`); and the gem is never proposed upstream, the boundary every extension in this set carries (`:5739`).

**Why the amendment was necessary rather than convenient.** The rule was written against an Enterprise invoicing product that would number, render and store documents. The concerns it protects are absent here and what remains is a word — and calling a 发票 an "invoice request" while the customer, the operator and the tax system all call it a 发票 would make the code lie in the other direction. The collision cost of the word is bounded to a fork upstream never reads.

**Ruled the same day, and recorded because each constrains the model.** **The seller issues**, and one request holds one seller's orders: the client scopes every invoice call by site and cannot merge across sites (`pagesB/me/invoice/order/order.wxml:11-20`), and `6.0-multi-vendor-marketplace.md:177` makes the seller merchant of record — platform issuance under `tax_remittance: platform` is deferred, not forgotten. **The tax-service integration is out of scope:** the gem owns the title, the request, the frozen amounts, the states, the attachments and the re-send, and the issuance is an operator action plus the event that follows it. **The merge preview is the gem's own read**, because "which historical orders are billable" is not the question the settlement page's price preview answers; that preview keeps only its three capability fields (`6.1-store-api-miniprogram-gaps.md:25`).

## 2026-09-18 — A bundle is a composition and a discount, not a sellable variant

Plans: `6.1-product-bundles.md` (new), `6.1-product-reviews.md`, `6.0-multi-vendor-marketplace.md`.

The inventory carried this plan as "Combos, and the stock question they raise". The question dissolves once the modelling follows the client's own wire: adding a combo writes **one cart line per component**, each tagged `promotionCode: "combo"` with the bundle's id (`miniprogram/components/j-combo/format/format.js:169-190`), the cart endpoint re-groups those lines for display (`pagesF/cart/cart.js:273-315`), and the order surfaces carry no bundle shape at all. A bundle modelled as a sellable variant would therefore have to be given its own price, weight, stock, reservation, fulfilment and review surface — six things the client proves it does not have. Modelled as a composition plus a discount, all six stay where core already puts them: the components are ordinary line items, `Spree::Stock::Quantifier` computes their availability unchanged (`spree/core/app/models/spree/stock/quantifier.rb:29-38`), `Stock::Package` sums their weight (`6.1-weight-bracket-shipping-calculator.md:37`), the existing reservation primitive holds each of them line-item-scoped (`stock_reservation.rb:19,22,129-131`), and the bundle gets no review surface because `6.1-product-reviews.md:44-46` reviews the offer.

**Ruled the same day.** The saving is an order-level discount of the **existing `manual` kind** — not a promotion, because the client's own copy says the combo price "与满减满赠等活动不同享" and the promotion engine clears rows a recalculation does not choose; not a new core kind, because `manual` already exists and this plan therefore needs **no core change at all**, which the points plan could not avoid. **One seller per bundle**, refused at save time, because `Carts::SplitBySeller` re-totals each child order from its own lines and a single bundle price has no home in that fan-out. And the discount is **allocated proportionally** to the component lines with the rounding remainder on the last line, so returning one component reverses its share rather than unwinding the whole saving.

**Two misfilings recorded so the count stops moving.** The mapping grades six endpoints in this domain, two of which are not about combos: `goods/getBuyNotCombo` is the ordinary single-goods buy info and the default branch of the function that chooses the combo one (`components/j-format/j-format.js:298-306`), and `goods/getByGroup` is a paged goods list the search results call. The architecture plan's "~8" counted them; the domain's own count of 6 is right.

**And one false friend named.** `frozen` and `frozenQuantity` ride every combo path and are 冰冻 — a chilled-drink option with its own picker — not a stock hold.

## 2026-09-18 — The loyalty family's shared ownership: a foundation gem for grants, one home per artifact, and four endpoints placed

Plans: `6.1-membership-tiers-and-rights.md`, `6.1-points-and-growth-value.md`, `6.1-coupon-wallet.md`, `6.1-scenario-purchases.md`, `6.1-membership-and-loyalty-points.md`, `6.1-miniprogram-extension-architecture.md`.

An adversarial audit of the membership and points plans found the split between them leaking in eight places: endpoints two plans both claimed or neither did, and artifacts nobody owned. The remedies are ownership rulings rather than designs, taken together because they constrain one another.

**One — the grant/benefit primitive is its own foundation gem.** The architecture plan requires "a small shared model the loyalty-family gems depend on, so a coupon and a membership right are not two unrelated tables" and left the owner open (`:64`, `:154`, Open Question 3). Leaving it to "whichever of membership, coupons or points starts first" would make two of the three wait on the first, and the two that already exist disagree on what a grant is: `spree_membership_grants` is keyed on a right with a `period_key`, `spree_point_grants` is a lot with `remaining` and an expiry. The primitive is therefore extracted into a foundation gem of its own — the same move the administrative-division tree got the same day — and the three consumers depend on that gem rather than on each other's arrival order. **This implies a plan document that does not exist yet.**

**Two — one home per artifact.** A granted coupon is held by the **coupon wallet**; the membership plan records what a grant issued and points at the holding. The growth-value fields of `vip/getUserVipInfo` are served by the **membership** endpoint, whole, because the client fetches that payload once and the tier is its core; the points plan's account endpoint carries points and the ledger only. The membership card issued by a points redemption is issued by the **membership** plan through a non-purchase source on the same lifecycle, not by the points gem, whose own constraint already forbids it.

**Three — four endpoints placed.** `delIntegralOrder` goes to `6.1-scenario-purchases.md` as a `destroy` on the scenario-order resource, because an integral-mall order *is* a scenario order and deletion is a property of that resource rather than of one kind. Every scenario's payment leg — `vip/payByWxMp` and its ChinaUMS twin included — belongs to that plan too, which already owns the twelve scenario-payment endpoints and the ruling that "the payment channel is a column, not a route". The activation gift bag is issued by the **membership** plan, calling the points ledger service and the coupon wallet's service: a three-way header declaration rather than an event round trip, because "once per period, never twice" is easier to hold in one transaction than across three subscribers. And `god/getWineValueDetailPage` stays **excluded** with the rest of `god/*` — the membership plan's handoff of it to the points plan is withdrawn, and that plan's own re-exclusion stands.

**Two items the audit found unowned are *not* settled here** and are recorded rather than assigned: `goods/getVipGoods` and `goods/getGoodsVipCategory` (a VIP-gated catalogue read and its categories) have no owner in any plan, and `coupon/getSvipCoupon` is assigned by the membership plan to the coupon wallet, whose own tables never name it. Both are named in the membership plan's dependency table so the next reader sees them as gaps rather than as done.

**Why the rulings are recorded rather than remembered.** Each of the eight leaks was invisible from inside either plan — the membership plan says "everything below the purchase is self-contained", and the points plan says the four moved endpoints "are the scenario plan's". Both sentences were false, and neither plan could have caught it alone. The audit and this entry are the record.

## 2026-09-18 (later) — The last five plans: eight rulings, and the documents they amend

Plans: `6.1-flash-sales.md`, `6.1-seller-scoped-pricing.md`, `6.1-product-authentication.md`, `6.1-miniprogram-platform-services.md` and `6.1-referral-invitations.md` (all written the same day), with consequences for `6.1-grant-and-benefit-primitive.md`, `6.1-distribution-and-commissions.md`, `6.1-coupon-wallet.md` and `spree/providers/social_auth`.

The five plans that completed the set were written against the restored client, and their rulings are recorded here because two of them reach documents that already existed.

**One — the pool-hold capability waits for group buying.** `6.1-flash-sales.md` and `6.1-group-buying.md` both found the same missing capability: neither `Spree::StockReservation` nor anything else in core can express units held by a record that is neither a cart nor an order, and a hold that must not read as ordinary unavailability. The flash-sales draft proposed building it in core, on the argument that flash sales is Wave 2 and group buying Wave 3, so the second consumer inherits rather than re-opens. **Ruled the other way: flash sales ships a gem-local hold and group buying generalises it.** The cost is stated rather than discovered — generalising it later means a migration over live pools, and the two implementations must not diverge in the meantime. This is the one ruling in the batch taken against the recommendation, and it is recorded with its price.

**Two — a flash sale's price is a gem-owned discount, not a promotion.** The engine reconciles its own rows destructively and winner-only per competition group, and the client's own rule is that an activity refuses points and coupons per activity. The read serves the price pair for display while the write applies a `manual`-kind discount, the shape the bundles plan already uses — and, like member pricing before it, this needs the "promotions = DTC consumers" doctrine argued past rather than ignored.

**Three — a seller-scoped price is the offer variant's base price.** A seller participates in the catalogue by holding offers, an offer is a `Spree::Variant` carrying its `seller_id`, and `Spree::Price` belongs to a variant — so a seller's price is that variant's own `price_list_id IS NULL` row. No seller column on `spree_prices` or `spree_price_lists`, and no seller axis in `Spree::Pricing::Context` or its cache key, which is the hottest read path in the platform.

**Four — the three pricing contexts are channels.** 区域, 现场推广 and 线下 resolve a channel whose default catalog carries the price list, which is core's own stated forward path for channel pricing (`ChannelRule`'s doc comment). The alternative — a new catalog-assignable type, the door `catalog_assignment.rb:6-9` leaves open for "the regional-assortment design" — stays closed until a context genuinely needs an assortment rather than a price.

**Five — `goods/getEstimate` belongs to the price preview.** Its own payload carries no site, shop, address or special, and `6.1-store-api-miniprogram-gaps.md` already calls the cartless price preview "the highest-value single addition in this plan". The mapping filed it under the seller/area domain; it is moved.

**Six — product authentication hangs its content on a Category, and adds no brand model.** Spree 6.0 ships no brand construct by explicit decision and says a brand is a grouping like any other, so the guide's brand **is** a `Spree::Category` — which is also the only thing that can join the client's two unjoined brand ids (the filter facet's and the college's). The gem is `spree_discern`, after the client's own word (鉴别), because `spree_product_authentication` collides with nothing but reads worse beside the plan's own tables.

**Seven — the platform-services row is a plan without a gem.** Four of its five buckets are already answered (dictionaries are the models' vocabularies; `sensors/*` is the gaps plan's, with ingestion forbidden; the click-id pair is D-graded and builds nothing) and the fifth is the relay's. What is genuinely new is **a customer-scoped direct-upload path** — the shipped concern is gated to admin and seller — and that is one route plus a gate, not a gem. The plan exists because `6.1-product-reviews.md` is blocked on a *contract* it explicitly declines to specify, and a contract needs a document. The subscribe-message template map lives on a `Spree::Integration`, the one credential home.

**Eight — a cash referral reward runs through the distribution ledger, and a coupon or goods reward is a grant.** This is the collision the two plans walked into: `6.1-distribution-and-commissions.md:51` already puts `referral` **and** `invite_task` into the kind list of a withdrawable ledger keyed on a distributor, while `6.1-grant-and-benefit-primitive.md` forbids an amount on its row and forbids writing a ledger row at all. **Invitations own the relationship; the distribution ledger owns the money** — it is already withdrawable, already settled, already reconciled — and the invitation gem writes a grant and calls it. Both headers declare the dependency, and the money ledger's uniqueness grows the invitation's own key. The mini-program QR capability goes to `spree/providers/social_auth`, where the mini-program identity already lives and where a second credential home is forbidden; the gem is `spree_referrals`.

**Consequences, all applied in the same pass.** The distribution plan's header now names `6.1-referral-invitations.md`, because its `referral` and `invite_task` kinds have an owner at last; the coupon wallet and the message relay declare that plan as a consumer of their holdings and their delivery; and `6.1-grant-and-benefit-primitive.md` records that its one built-in kind, `referral_reward`, has been claimed. `6.1-group-buying.md` absorbs the hold ruling — its Open Question 1 is settled, its target no longer promises a core capability of its own, and its own document now says when the lift happens. `6.1-product-bundles.md` names `6.1-flash-sales.md` as the owner of the pool and the caps it handed over, and `6.1-product-reviews.md` names `6.1-miniprogram-platform-services.md`, whose route is the upload path its nine images attach through. `6.1-store-api-miniprogram-gaps.md` gains the two brand fields and the facet that point at `6.1-product-authentication.md`'s content, and the architecture plan's Wave 3 step names the hold's sequencing.

**One consequence of the QR ruling is worth its own line.** The mini-program credential the QR needs **already exists** on the WeChat Pay gateway (`preferred_mini_program_app_id`/`_secret`), because JSAPI payment needs it. Placing the capability in `spree/providers/social_auth` therefore means that gem **reads the same `Spree::Integration`**, not that the pair moves and breaks payments — one credential, two readers.

## 2026-09-18 (later) — The grant row lands in core, and `spree_grants` owns the kinds and the service

Plans: `6.1-grant-and-benefit-primitive.md` (new), `6.1-coupon-wallet.md`, `6.1-membership-tiers-and-rights.md`, `6.1-points-and-growth-value.md`, `6.1-gift-card-purchase-and-transfer.md`, `6.1-miniprogram-extension-architecture.md`.

Writing the foundation gem's plan turned up the objection the ruling above had not answered: `6.1-coupon-wallet.md:127` recommends **core** for its own holding, because an extension would otherwise "have to decorate `Spree::CouponCode` and `Spree::Order`". Putting the shared row in a gem leaves that argument standing for every consumer that touches a core model, and answering it once per consumer is how five plans end up disagreeing again.

**Decision — the row is core; the gem owns the kinds and the service.** `spree_grants` and `Spree::Grant` land in `spree/core`, with `SingleStoreResource`, `Metadata`, `has_status`, paranoia, lifecycle events and a `Spree.grant_kinds` registry hook. The `spree_grants` gem at `spree/extensions/spree_grants/` ships the kinds, the service (`grant!`, `claim!`, `consume!`, `revoke!`) and the built-in kinds no other plan owns. A consumer's kind is a gem-local subclass of the core model — its own class, which the extension boundary allows, not a reopening of someone else's.

**Why core rather than the gem, in one line.** It answers the coupon wallet's argument for all four consumers at once, it is the fork's own doctrine ("a generic capability an extension needs lands in core first"), and by the merge-cost rule this set has been using it is the cheap kind of core change: **a new table and a new base model are new files, and it edits no file upstream owns.**

**Ruled with it, and recorded because each constrains the model.** "Shared" means **one table with kinds**, not a base class over per-family tables — the architecture plan's justification is that a coupon and a membership right should not be *two unrelated tables*, and a shared ancestor keeps that criticism intact. The gem is **`spree_grants`**. The gift card's **welfare grant is a kind** — neither consumable nor expiring — rather than a fourth grant-shaped table. And consumption stays **delegated to the kind**, because a primitive-level quantity would put points' partial arithmetic back into the shared row it was just taken out of.

**Consequences.** The architecture plan's Open Question 3 is answered and the "whichever ships first" sentence at `:154` no longer governs. The membership plan's header names the gem and the core row; the coupon wallet's Open Question 1 is answered as core and its holding becomes a kind; the points plan's lot keeps `remaining` and its allocations in its own side table; the gift-card plan's welfare row becomes a kind. The coupon wallet's stored `expired` status is the one item still at odds with expiry-as-a-date and must be reconciled.

## 2026-09-18 (later) — A pricing context is request scope, resolved by core's channel resolution, so the product read does not change

Plan: `6.1-seller-scoped-pricing.md`.

The plan's verification pass left one question it could not answer for itself: the three context reads (区域 / 现场推广 / 线下) were written as a `context=` parameter on `GET /api/v3/store/products/:id`, a route whose controller, serializer and route are core's — so serving it meant either editing an upstream controller (the permanent merge tax this set avoids) or a gem-owned path that shadows core's read.

**Decision.** Neither. **The context is request scope, and the machinery for it already ships.** `Spree::Api::V3::ChannelResolution` resolves the request's channel at the boundary — the publishable key's binding first, then the `X-Spree-Channel` header (code or `ch_…` id, scoped to the store), then `current_store.default_channel` — and writes it to `Spree::Current.channel` (`channel_resolution.rb:40-42,80-81`); the pricing context already falls back to that (`@channel = channel || Spree::Current.channel`, `context.rb:25`), and `Spree::Current.channel` also feeds `catalogs_for` and the global pricing context (`current.rb`). So the product read is **unchanged**, the client asks for the context it is in through the header, and nothing new is built on the server. **The seller is the member that was missing from that scope**, and the routing plan now lands it (its Decisions 17 and 18).

**Why this is the strong answer rather than the cheap one.** The price on the read, the price the cart writes on the line and the price the checkout charges become **one context by construction** instead of three that must be kept in step — and it costs no core edit, no new mechanism and no second payload. It follows the 2026-09-17 ruling's own shape: the request-scope context is where tenancy lives, and the seller is the member that scope was still missing.

**Accepted trade-offs, stated rather than discovered.** The popularize context's detail call carries no identifier of its own (`{ id }` and nothing else), so the context comes from the header the rewritten client sends or, failing that, the customer's binding; a signed-out visitor whose client sends neither sees the default channel's price. And because the context belongs to the request, two contexts cannot be compared inside one request — a screen showing an area price beside an offline price needs two requests.

**Consequences.** The pricing plan's Store API surface shrinks to one route (the seller lookup) and the reads it replaces are core's own. **The routing plan — the keystone — gains the amendment it had been credited with but never written**: its Decision 17 lands `Spree::Current.seller` and the boundary concern, and its Decision 18 puts the seller attribute, that concern and the `StockLocation` geocoding columns in core rather than in the gem. Two plans that read the seller (`6.1-membership-and-loyalty-points.md`, `6.1-seller-scoped-pricing.md`) now have one place to read it from.


## 2026-09-18 (later) — The keystone lands the request-scope seller, and its core-facing pieces land in core

Plan: `6.1-seller-service-area-routing.md` (its v1.3 revision), verified against the client and the codebase in the same pass.

The architecture plan promoted this plan to the keystone on the strength of a sentence its own document never carried: that `resolve_seller` is "the request-scope mechanism every Store API call needs" (`6.1-miniprogram-extension-architecture.md:68,164`, `decisions.md:5747`), and the 2026-09-18 division ruling listed "restate `resolve_seller` as request scope" as one of the five amendments the plan needs. The v1.2 draft specified only the lookup endpoint — `GET /api/v3/store/location/resolve_seller?latitude=&longitude=`, "read-only … no customer context required" — and nothing else. Three plans then chained onto the missing half: the membership plan's cart seller context, the pricing plan's seller scope, and today's pricing ruling, which named the routing plan as the mechanism the channel would be assigned by.

**Decision — the keystone lands both halves.** Discovery stays the endpoint. Scope becomes a **core** addition: `Spree::Current.seller`, written at the request boundary from the client's `siteId`, by a concern of the same shape as the shipped `ChannelResolution` (`spree/api/app/controllers/concerns/spree/api/v3/channel_resolution.rb`) — the header name is the Seller branch's existing `X-Spree-Seller-Id` (`seller/seller_context.rb:20`) rather than a second one — and included by the Store branch's two anchors, which are parallel chains. A request that needs a seller and carries none is an error, never a default.

**And its other core-facing pieces land in core too.** The `StockLocation` geocoding columns (`latitude`, `longitude`, `geocoded_at`, `geocode_provider`, `geocode_status`) and the forward-geocoding job are core's, because the v1.2 draft's alternative was a gem migration into `spree_stock_locations` — an upstream table — which `6.1-miniprogram-extension-architecture.md:163` forbids and which the marketplace's own `seller_id` on that table avoided by going through a core migration (`20260820000001_add_seller_business_identity_and_stock_locations.rb:8`). The gem keeps the binding, the polygon, the reverse geocoders, the code mapper, the decision and the endpoint, and its classes take the shipped extension's naming shape (`Spree::Crm*` in `.custom-extensions/spree_crm`) rather than sitting inside a core namespace: `Spree::SellerRouting::Locate`, not `Spree::Sellers::Locate`.

**Two mechanical corrections the same pass found, both of the kind that fail silently.** The one-active-binding-per-node index is partial — `WHERE active = TRUE` — and `Spree::StockLocation` is paranoid, so the predicate needs `deleted_at IS NULL` or a soft-deleted row keeps its node claimed forever; every partial index this repository ships carries that guard. And MySQL does not support partial indexes at all, which matters in a plan that argues portability as its reason to skip PostGIS: the adapter branch core's migrations already use applies, and the active-only release degrades to a service rule there.

**Consequences.** The keystone's endpoint inventory is written down at last — 7 of the 门店 bucket's 29 plus the one it adds, with the other 22 attributed to the pricing plan, the price preview and the division gem, and the POI reads recorded as unowned — so the architecture plan's "~29" row reconciles. The division gem's plan is corrected on the evidence for its pinyin columns: the client sorts **the location list's province-level rows** (which carry a `siteList`) by `firstPinYin`, not the picker, which is a three-level cascade that neither sorts nor shows anchors. And the two plans that read the seller now have one place to read it from.

## 2026-09-18 (later) — The Cainiao plan is superseded by an instant-delivery provider family, and a parcel is a tracking number

Plans: `6.1-local-delivery.md` (new), `6.1-cainiao-logistics-provider.md` (superseded), `6.1-weight-bracket-shipping-calculator.md` (superseded, no consumer), `6.1-delivery-tracking.md`, `6.0-delivery-scan-events.md`, `6.1-miniprogram-extension-architecture.md`.

The Cainiao plan (720 lines) is a parcels gem: it issues 菜鸟电子面单 for merchants holding their own contracts with Chinese couriers, prints labels, prices 首重+续重 through a weight-bracket calculator it names as a prerequisite, and tracks courier scans. **Nothing in the client consumes it.** The alcohol order journey never touches a parcel — no status, button, map state or timeline names a carrier — the 配送方式 row is a **hardcoded literal** ("酒小二 ({{siteName}}) 配送"), and the delivery surface is eight read-only endpoints keyed by `orderId`. The replace question was therefore not a refactor: the delivery side of this application is instant and local, and the plan was pointed at the wrong supply model.

**Decision — the delivery provider is a local instant-delivery family, and 达达 ships first.** `6.1-local-delivery.md` builds on the seams core already ships — `Spree::FulfillmentProvider::Base#create_fulfillment`/`#cancel_fulfillment`/`#tracking_url` and `Spree::DeliveryRateProvider::Base` — with one member per platform, the shape `spree/providers/social_auth` already uses for four identity providers. **达达 is the first member** because it is the only platform where a licensed single shop can self-serve an account *and* the quoted fee is contractually binding (`queryDeliverFee` → `addAfterQuery` with the `deliveryNo`); 闪送 and WeChat's own 即时配送 channel are the next members. 美团配送 (≥30 stores, ≥1,000 orders/day, ¥50,000–100,000 deposit) and 蜂鸟即配 (software-provider or brand-merchant gate) are recorded as out of reach for a single shop, so no plan may assume them. ~~The rate provider is the **opposite** of the Cainiao one: `uses_calculator?` is false, because these platforms price by distance and time and must be asked.~~ **Superseded the same day** by the entry "The delivery plugin is the platform's": the customer-facing fee is the delivery method's own rule price, so `rate_provider` stays `Internal` and the **calculator** prices, while the platform's live quote is recorded as the merchant's cost. The platform-priced shape remains available for a merchant who wants it; it is not the default.

**Ruled with it — the dispatcher re-dispatches, because no platform does.** Only 达达 publishes a timeout rule (30 minutes unaccepted → 已过期, with a re-dispatch endpoint); every real deployment escalates in the aggregator. So a `Dispatch` service walks the merchant's platform order, two minutes per attempt, escalates the tip where the platform allows it (达达/闪送/蜂鸟 do, 顺丰同城 does not), and cancels every losing attempt once a rider accepts. A failed dispatch leaves the order in 待配送 — the client's own state — and notifies the merchant through the relay.

**Ruled with it — a parcel is a tracking number, and nothing else.** The client does have small parcel flows: a posted paper invoice, a lottery prize, a physical year-gift (`deliveryMode: "express"`, badged 快递发货), with a second address-book tab (`address/{add,list,set}Logistics`) and a prize-tracking read. They need a tracking number shown to the customer and nothing more — which `Spree::Delivery` already models (`tracking_number`, `carrier`, `status`, `tracking_url`). **No waybill, no label, no cloud print, no carrier contracts, and no weight-bracket calculator:** that plan is superseded with this one, having no other consumer, and is to be revived only if a merchant ever prices their own parcel shipping.

**Consequences.** `6.1-delivery-tracking.md` gains its producer — its Open Question 4 ("is there a same-city delivery provider in the loop at all?") is answered, and the delegation of carrier mapping and reconciliation passes from the superseded plan to this one. `6.0-delivery-scan-events.md`'s field-shape examples are re-pointed from Cainiao's response to the platforms this application actually dispatches to, and its Open Question about the `location` column is now thinner rather than settled: 达达's callback carries no location, UU's carries the rider's coordinates, 闪送's detail carries a trail. The division gem no longer consumes or retires anything Cainiao-shaped, and the ISO→bureau crosswalk it was going to ship has **no consumer**, so it is not built. The architecture plan's "Carrier mapping and reconciliation (Cainiao)" row becomes the new plan's, and its "two administrative-division designs" constraint drops to one. Two open questions are carried by the new plan rather than assumed away: whether alcohol is an allowed cargo category on any platform (酒水 has no top-level value anywhere, and it is restricted on some networks), and 达达's callback signature algorithm, which no readable public source states — the ingress fails closed until it is read off the live portal.

## 2026-09-18 (later) — The delivery plugin is the platform's: its fee is a rule, its waivers are promotions, and the platform's quote is cost

Plan: `6.1-local-delivery.md` (renamed from `6.1-instant-delivery-providers.md` in the same pass).

The entry above created the supply half — a provider family dispatching a shop's delivery to a local rider network — and left the price as an open question: pass the platform's live quote through to the customer, or set our own fee. The author ruled the second, and widened the plan from "platform adapters" to **the platform's delivery capability**, because the fee, the gate and the waiver are the same feature from the merchant's side.

**Decision one — the customer-facing fee is the delivery method's own price, and the platform's quote is cost.** `DeliveryMethod` carries two independent columns and this ruling is what they are for: `rate_provider` stays `Internal`, so the **calculator** prices (a flat default fee today; a distance- or time-band calculator is not shipped), while **supply** is `fulfillment_provider`, naming the platform that dispatches. "同城配送 ¥8, dispatched to 达达" is one method. The platform's live quote is recorded on the dispatch attempt as `charged_amount` beside `quoted_amount`, and the difference is the merchant's — or the operator's, when the operator promised the benefit. **The rejected shape is named rather than deleted**: a provider-priced method (`uses_calculator?` false) would pass the live quote through, and the Estimator's `markup_percent`/`markup_flat` is the built-in margin seam for exactly that; it stays available for a merchant who wants the platform's price plus a margin, and the default methods price by rule.

**Decision two — a waiver is a promotion, never a fee engine.** 满额免配送 is `Promotion::Rules::ItemTotal` plus `Promotion::Actions::FreeShipping`; 会员免配送 is `Rules::CustomerGroup` plus the same action, and **the membership plugin needs no change at all**, because a tier is a `Spree::CustomerGroup`. `FreeShipping` writes a `Discount` on the fulfillment for the whole cost and its `persist_at_zero?` is load-bearing — `Order#has_free_shipping?` is a **row-existence test** and `DeliveryRate#final_price` reads it, so a waived fee is a zero row rather than an absent one. The membership plan says in writing that none of its ten rights waives shipping; that stands, and no `free_delivery` kind is added.

**Decision three — who funds a waiver.** A **shop-level** rule is the seller's own money: the customer pays zero, the seller earns less, no transfer is written. An **operator-promised** benefit is funded through `6.1-membership-tiers-and-rights.md`'s `subsidy` seller-transfer kind — the operator owes the seller the waived amount, settled by the sweep and the balance — which is five coupled core edits the membership plan already carries. This plan consumes that capability and does not re-open it.

**Decision four — the plan widens and is renamed.** `6.1-local-delivery.md` owns the platform's delivery capability: supply, the fee and its waivers, and **the 起送 gate**. The gate is a delivery-method rule, which is the shape `DeliveryMethodRules::ItemTotalRule` already is; the client renders a minimum order with **peak and off-peak** variants, and no shipped rule reads the clock, so that rule kind is the one new class here. The client's side is stated so nobody builds what it cannot render: the fee reaches it as `deliveryFee` plus additive `order_service_fee`/`merchant_service_fee` lines (配送服务费总额), a waiver reaches it as **zero**, and a "免配送费 −¥8" row would be a client release rather than a server change.

**Consequences.** Seventeen references across six documents were renamed with the plan, and the architecture plan's two rows for it now describe the widened subject. **Two of the three questions it carried were then answered in the same pass.** 达达's callback signature **is** knowable: `MD5_lower(sort_asc([client_id, order_id, update_time]).join(""))` — three fields, no secret, lowercase, with `update_time` hashed exactly as received because it is milliseconds under `order_status == 1000`. The request side is uppercase, secret-wrapped and over every parameter, so the two signatures live in separate methods; one shared helper would reject every genuine callback. And the ingress question produced a stronger ruling than verification: **a callback is a hint, not a fact** — a terminal state is confirmed by re-querying the platform with our own signed call, which is the only control that holds for **闪送, whose callback carries no signature at all by design**, and for 顺丰同城, whose `sign` concatenation is the one that is genuinely not public. **All three of the questions the plan opened are now answered, and two of the answers corrected the plan's own premises.** Alcohol is a **declared category, not a prohibited cargo**, and the draft had it wrong twice: not every platform takes a two-level category (only the WeChat channel does), and alcohol does have a home — 达达's flat `cargoType` carries **24 酒品**, the WeChat channel carries **日用百货 → 酒水行**, 美团 allows 酒类 with conditions and 蜂鸟 files it under 名酒坊 behind a 食品经营许可证. The clauses that name **酒精** prohibit the flammable liquid, not packaged beverage alcohol. **The exception the dispatcher must respect is 闪送**: no public alcohol category, an unpublished degree threshold in its internal rules, and no 保价 — so it is not a candidate for a spirit order, and alcohol fitness is a per-member fact rather than a family-wide one. The same goes for the capability matrix the walk reads (保价, the 收/取货码 pair, tips, the cancel window), and for the hard caps that can make one delivery impossible — the WeChat channel's ¥5,000 declared-value limit, which a case of 白酒 can breach. Nothing anywhere can verify age, and nothing needs to: the client self-declares 18+ at login, so the integration's alcohol settings are the category, the insurance and the 收货码. **Distance-band pricing stays deferred, and the deferral is now a data question rather than an argument**: no comparable storefront prices delivery by distance (1919 快喝 gives it away with the tiers living in the promise; 歪马送酒 is free above ¥29), the zone workaround is unavailable in this market because the client carries no postal codes, and the plan now records the platform's own routed distance on every attempt — so one month of rows settles whether a flat fee ever loses money. If it does, the parked weight-bracket plan is the template for the calculator's shape.

## 2026-09-18 (later) — Our notifications are ours: the marketplace relay is a different thing, and six plans were reading it as a sender

Plans: `6.1-notifications.md` (new), `6.1-phone-verification-and-payment-pin.md`, `6.1-miniprogram-platform-services.md`, `6.1-china-invoicing.md`, `6.1-local-delivery.md`, `6.1-flash-sales.md`, `6.1-referral-invitations.md`, `6.1-miniprogram-extension-architecture.md`.

The architecture plan's capability table said "`6.1-marketplace-message-relay.md` already designs a relay; extend it rather than adding a second one", and six plans followed it: verification codes, WeChat subscribe messages, invitation reminders, invoice re-sends and dispatch-failure notices were all written as "through the relay", and the phone plan went as far as planning to **amend** that plan into a channel registry.

**Upstream's relay is not a sender.** Read in full on 2026-09-18: it relays messages **between a buyer and a seller** so neither learns the other's address — a `Spree::MessageThread` per (order, seller), an opaque alias per thread on the relay domain, an Action Mailbox ingress, a closing window, and an operator's read-only view for disputes. It is a conversation feature for marketplace disputes, and none of our six needs is what it does.

**Ruled: the platform's notifications are ours.** A foundation gem, `spree_notifications`, owns the single door (`Spree::Notifications.deliver(to:, event:, payload:)`), the channel registry (`sms`, `wechat_subscribe`, `email`) and the enqueue-and-retry policy; the phone plan contributes the SMS channel, the platform-services plan contributes the WeChat subscribe channel, and the other four are consumers with nothing to build. The architecture plan's constraint is rewritten from "do not add a second outbound-notification mechanism" to **"do not add a second *system-notification* sender"**, and the relay is left alone — a document we do not own and a feature we do not touch. Two things are deliberately not modelled: a message archive (fire-and-forget jobs and `Rails.error` are the record) and an opt-out (transactional only, with the open question recorded rather than implied).

**The lesson worth keeping.** This is the same error as the plan-set boundary itself: a document we do not own was read as though it were ours, and a capability was attributed to it that it never claimed. The rule that follows is in `CLAUDE.md` — upstream's plans are read-only and a dependency is declared, never amended.

## 2026-09-18 (later) — The four cross-plan rulings: one transfer row, one member model, one read surface, one balance payment

Plans: `6.1-transfer-primitive.md` (new), `6.1-coupon-wallet.md`, `6.1-gift-card-purchase-and-transfer.md`, `6.1-membership-tiers-and-rights.md`, `6.1-store-api-miniprogram-gaps.md`, `6.1-scenario-purchases.md`, `6.1-phone-verification-and-payment-pin.md`.

The audit that produced `audit-cross-plan-siloing.md` found the same work designed more than once in four places, and each was a decision rather than an edit. All four are ruled here together, because three of them turn on the same principle: **a shared thing gets one row and one owner, and every plan that consumes it names it in its header.**

**One — the three tokenised transfers become one row (`6.1-transfer-primitive.md`).** The coupon wallet, the gift-card plan and the membership plan had each designed the same machine — an opaque token, a claim window, and three endings — with statuses that had already drifted (`accepted` in two, `claimed` in the third). The ruling is the grant primitive's shape with its gem half removed: **the row lands in core**, because a new table, a new base model and a new service are new files and edit nothing upstream owns, and there is no gem because there are no kinds — the transferable's class is the kind. The vocabulary is `pending`/`accepted`/`canceled` with **`expired` derived from the date rather than stored**, which is `Spree::GiftCard`'s own discipline and already the grant primitive's. Each domain implements three contract methods on the thing being moved (`on_transfer_given`, `on_transfer_accepted`, `on_transfer_canceled`) instead of a registered-subclass family, so the audit's registry count does not grow. **The three routes stay per-domain** — the client's link shapes and payloads differ — so this is one row and three thin controllers, not one polymorphic endpoint. **Accepted trade-off, named in the plan:** the gift-card domain reserves something at give time (the card's hold), and a hold cannot be released by a date nobody reads, so that plan owes an idempotent release from its own read path — the one place in the design that needs a reader.

**Two — the tier plan owns the member model, and `spree_crm`'s membership is superseded.** The fork already runs `.custom-extensions/spree_crm` with `Spree::CrmMembership` and `Spree::CrmMembershipPlan` — an expiry sweep in `server/config/recurring.yml`, a purchase-by-SKU path through an `order.placed` subscriber, `auto_renew` and `grace_days` — and **neither side mentioned the other**. Both add and remove customers from a `Spree::CustomerGroup`, which is what member prices *and* the member free-delivery waiver read, so leaving them side by side would have produced two writers of one fact. `6.1-membership-tiers-and-rights.md` owns the model; three things are **ported rather than re-invented** (`auto_renew` and `grace_days` with `past_due` in the term's statuses, the "a tier is bought by SKU" mapping — which resolves to the `vip` scenario kind rather than to a subscriber, since that plan forbids a cart purchase — and the ending-of-term sweep); and `spree_crm` keeps the half nobody touches: relationships, relationship types, company roles and membership roles. **One writer, named:** this plan's transitions are the only thing that adds or removes a tier-group member.

**Three — the gaps plan owns both composite read surfaces.** The price preview is the most-called new endpoint in the set and **its owner named no path anywhere**, while `6.1-seller-scoped-pricing.md` proposed a competing `products/:id/purchase`; separately, five plans wrote fields onto the order payload and one of them registered a serializer of its own. Both are `6.1-store-api-miniprogram-gaps.md`'s now, because every plan already defers to it as Wave 0. The route is named there — **`POST /api/v3/store/price_preview`**, carrying either variant ids and quantities or a cart id, for the client's three `calc/*` calls — and the order payload is merged from a table in that plan rather than by a registration per plan.

**Four — `balancePayOrder` is the gaps plan's, at the order level.** The scenario-purchases plan read it as a scenario purchase whose channel is the customer's stored value; it is a payment against an order already placed, and the payment-PIN plan had already written the seam as "joins when `6.1-store-api-miniprogram-gaps.md` builds it". The route is that plan's and it spends through `Spree::StoreCredits::Apply` — where the PIN is enforced — so no settlement path may reach a balance without the check. The scenario plan's paragraph is rewritten to point there rather than deleted, and both plans carry the constraint.

**Consequences.** The audit's D1, C3, E3, E4 and E7 close with owners; D2 (three balance ledgers), D3 (five registries), D4 (recurring jobs) and the header citations remain open and are mechanical. `6.1-transfer-primitive.md` joins the set as the second foundation plan of the same shape as `6.1-grant-and-benefit-primitive.md`, and the three transfer consumers' `Depends on` headers name it in the same change — the both-headers rule the architecture plan states at `:168`.

## 2026-09-18 (later) — The second batch: claims counted instead of listed, a family with no surface, a timeline with two owners, and three capabilities with none

Plans: `6.1-ledger-primitive.md` (new), `6.1-coupon-wallet.md`, `6.1-group-buying.md`, `6.1-store-api-miniprogram-gaps.md`, `6.1-seller-service-area-routing.md`, `6.1-referral-invitations.md`, `6.1-delivery-tracking.md`, `6.0-delivery-scan-events.md`, `6.1-points-and-growth-value.md`, `6.1-distribution-and-commissions.md`, `6.1-gift-card-purchase-and-transfer.md`, `6.1-notifications.md`, `6.1-miniprogram-platform-services.md`, `6.1-local-delivery.md`, `6.1-membership-tiers-and-rights.md`, `6.1-china-invoicing.md`, `6.1-miniprogram-extension-architecture.md`.

The first batch closed four duplications. This one closed the remaining seven items the audit left, and the evidence changed two of their diagnoses.

**One — the coupon domain's 85 is a count, not a claim.** The three plans were recorded as claiming the bucket three ways; the row-by-row reconciliation (`endpoints-by-domain.md:267-353`) found **no endpoint claimed by two plans** and **25 claimed by none**. The wallet says "85" in its Summary and names 40 (explicitly or by glob); membership names its 16; flash sales names its 4; the remainder — `coupon/page`, `getSelfCoupon*`, `voucherPage`, `receiveCoupon`, the `present*` pair and the rest — belonged to nobody. **Ruled: the wallet owns 65**, with a `Scope` header that names its families and concedes the 20, and the architecture plan's row corrected from "~81" (which had also counted the full-gift cards membership now owns). `coupon/getSvipCoupon`, which the membership plan had flagged as unowned, is the wallet's too.

**Two — `teamBuy/*` was missing a surface, not a design.** All 21 endpoints have live client callers, and 19 are this fork's to serve (the other two are the scenario plan's payment legs). The plan had no `Scope`, no Store API section, never wrote the family's name, and its `Depends on` missed the two plans whose surfaces its own calls resolve to. **The surface is now written row by row**: two resources (activities with their rules and frame as fields; teams with a join verdict), one membership create carrying `join_kind`, one cancellations route, and four calls that resolve elsewhere — `buyCalc` to the one price preview, the link and poster to the one share payload, the reminders to the notification sender. `addRiskRecord` is deliberately deferred with its reason stated.

**Three — the buyer timeline has one owner.** `6.0-delivery-scan-events.md` reserved it as "phase two" when it had no code and still has none; `6.1-delivery-tracking.md` is named for it, already owns the trajectory route, and now has the producer the scan plan was waiting for. **Ruled: the tracking plan owns it**, and the scan plan's paragraph points there rather than reserving it.

**Four — the mini-program `access_token` is one service, in `spree/providers/social_auth`.** Two things had to be corrected before the ruling could be made. First, **the credential is not where our own text said it was**: the app id and secret pair lives on the WeChat Pay **gateway's preferences**, deliberately, because payments keep one credential set per merchant entity (`gateway.rb:5-9`), and the referral plan and `fork-decisions.md` had both described it as "one `Spree::Integration`, two readers" — the wrong carrier, now corrected in both. Second, the ruling is not merely tidy: **WeChat invalidates the previous token whenever a new one is obtained**, so two consumers caching their own take turns breaking each other. One service, cached by appid, single-flight, read by the QR mint, the subscribe-message channel and the WeChat 即时配送 channel.

**Five — one share payload, owned by the gaps plan.** The author chose the shared resource over the shared contract, and the cost is recorded rather than discovered: the route carries the union of its targets' parameters (a binding, a token, a team id, a hub id), while each domain keeps its own data and its own resource. `POST /api/v3/store/shares` composes the path once — the client's `type`/`typeId` grammar had four would-be implementations — and the referral plan's share route becomes a contribution to it rather than a route of its own. The QR and poster it may return come from the identity capability's token service.

**Six — the site record read has an owner, and its flags had a false conflict.** Five calls (`home/allConfig`, `site/getDetails`, `getCount`, `getOftenHot`, `getSiteDeliveryBySiteId`) served the record three plans read fields off, and nobody served it. **Ruled: `6.1-seller-service-area-routing.md` owns it** — it already resolves the request's seller and owns the site lookups — with `GET /api/v3/store/site` as the record, the existing upstream `store/sellers` serving the two list reads, and the three flags (`siteSvip`, `invoiceType`, `provideSpecialTicke`) as seller columns delivered on this payload. The "two competing carriers" the audit recorded were not rivals: the client's own construction decides the carrier (they are fields of `siteDetails`, read in twenty places), and `store/sellers` is the route a customer asks about a *seller* on.

**Seven — one ledger row shape for all three balances.** The author chose the shared model over the written contract, and it is the sharpest of the rulings: `6.1-ledger-primitive.md` puts `spree_ledger_entries` in core with a polymorphic account, a signed amount, a unit, a `balance_after` snapshot where the account has none, an `idempotency_key` supplied by the producer, and **`reverses_entry_id`** — which replaces `consume_return`, the `reversal` kind and the card's `refund` with one pairing convention. The accepted cost is named in the plan and is this batch's only new debt: **the distribution ledger is also a payout record**, so `rate`, `level` and payout state must land beside the payout path rather than on the entry, and that plan owes the answer before its migration step starts.

**Eight — the recurring jobs have a register.** Core ships no cron and the host app schedules (`6.0-multi-vendor-marketplace.md:178`); ten jobs across this set and its delivered extensions had no list, no cadence and no registrar. The architecture plan now carries the table — job, cadence, what breaks if it does not run, owner — with three rules for anything added: safe to run twice, honest about its failure mode, and its row added in the same change as the plan that introduces it.

## 2026-09-18 (later) — The seller scope is the first build, and its four blockers are settled

Plan: `6.1-seller-service-area-routing.md` (ours; Decisions 19–22 and four Open Questions closed). This is the fork's first implementation target, chosen because every seller-scoped read — pricing, catalogue, delivery areas — resolves through it.

**Decision one — the gem is `spree_service_areas`.** It names what the gem owns rather than one of its consumers and leaves `spree_delivery*` free for the delivery work.

**Decision two — the binding and `Spree::DeliveryZone` are not bridged.** The plan recorded a merchant configuring "the same coverage twice": once as a zone for shipping, once as a binding for seller discovery. The two are not the same coverage, and the ruling says so: a zone answers *may this delivery method serve this address* and matches country, state or postal code; the binding answers *which seller serves this point* and matches a division subtree plus a polygon. **A bridge was rejected on a mechanical ground rather than a preference:** zone members stop at the state, so expressing a district-or-deeper binding through them would either lose the precision the binding exists for or require a new member type in an upstream model. This deployment authors no zone for coverage at all — the client carries no postal codes and delivery is instant and local — so coverage is configured once, in the stock-location form, and the rule is written into the plan's constraints rather than left to habit.

**Decision three — the binding and its geocoding columns stay on `spree_stock_locations`, and the shared contract survives.** The worry was that upstream's `6.0-order-routing.md` (upstream's plan) might land `latitude`/`longitude` first with a different meaning. It does not: it mentions them only inside its own open question and defers them to phase two, and the table carries neither column today — while the geocoding primitive that *does* ship is `Spree::Address`'s columns and its job. Whichever side lands first owns the migration and the other consumes it. Two consumers reading where a location is is one fact, not two meanings; if a future upstream point genuinely differs from a shop's public point, that is a second suite of columns on the anchor that owns the service area, never a rename of these.

**Decision four — two tiers, and the campaign tier stays out.** The client resolves delivery area as shop → special → seller, and the plan modelled two. A **special is inside the decoration bucket the architecture plan excludes**, so it does not come back through this plan. What makes the answer safe is that the middle tier is *derived* whenever it returns: a special belongs to one seller and is delivered by a set of its shops, so its coverage is the union of their polygons, and the only new fact would be the campaign-to-warehouse membership — merchandising, not geography. Until then the two address checks that name it answer from the seller tier, which is a superset of any zone the excluded feature would have defined.

## 2026-09-18 (later) — The first extension gem: `spree/extensions/` is open, and the CI gap it needed closed first

Plan: `6.1-administrative-division-gem.md` (In progress), plus `scripts/test/rspec`, `.github/workflows/tests.yml`, `.gitignore`, `CLAUDE.md`.

The seller scope's first slice landed in core. This is the second: the administrative tree becomes the fork's first extension gem, and the repository gains the scaffolding every later gem needs.

**The CI gap closed first, because the plan requires it before an eighteenth gem appears.** `providers/wechat_pay` and `providers/social_auth` were absent from `.github/workflows/tests.yml` — from both install loops and both matrices — so two shipped gems had **never run their specs in CI**. Both now ride the providers lane, together with `extensions/spree_administrative_divisions`; the lane is where the small suites already live, and the comment says so. The fork's `scripts/test/rspec` learned the third engine root (`spree/extensions/<name>/spec`, beside `spree/<engine>` and `spree/providers/<engine>`), and the root `.gitignore` learned `spree/extensions/*/Gemfile.lock`, which the existing two patterns did not cover.

**The gem is `spree_administrative_divisions`, and it ships the tree, not yet the data.** The model and its migration are here — the five levels, the GB/T and bureau code scheme, `dataset_version`, and the `first_pinyin`/`pinyin` columns the client's picker sorts and anchors on. The dataset files, the import task and the tree resource are the next change; nothing reads the table yet, which is why the plan's status is *In progress* rather than *Implemented*.

**Shipping a table from a gem is a path this repository had never walked, and it needs one more file than the provider gems do.** Every gem here that `rake test_app` builds either ships no migrations or ships a **no-op** install generator (`spree_stripe`, `spree_emails` — both say so in their own comments). The dummy-app builder runs `generators/<gem>/install/install_generator` after `db:migrate`, and **skips it silently when the file is absent** — so the first spec run failed with `Could not find table 'spree_administrative_divisions'` while every other step reported success. The gem therefore carries a real install generator that copies its `db/migrate` into the host app, the shape our own out-of-tree `spree_crm` extension already uses. Worth knowing for every gem that follows: **a table without a generator is a table nobody's dummy app has.**

**One correction to the plan's table, made while implementing it.** The `parent_id` column was specified as `uuid`, carried over from the seller plan. **No table in this repository has a uuid primary key** — the check that settled it was the applied schema, where `spree_sellers` and every other table is bigint — so a uuid foreign key would have had nothing to point at. The gem ships `bigint`, and both plans' tables now say so with the correction recorded rather than silently rewritten.

**The dataset question answered itself, and the answer is that there is no annual refresh to plan for.** The plan deferred *which bureau release to start from and at what cadence*; the first import settled it. The release is the bureau's **2023-06-30** snapshot (31 provinces, 342 cities, 2,975 districts, 41,352 townships), mirrored from `modood/Administrative-divisions-of-China`, and it is probably the last full one — **since October 2024 the National Bureau of Statistics publishes the classification rules but no longer the codes themselves.** So a future refresh is a *different source*, not a newer file from the same one, and nothing in the model, the import or the endpoints changes: a new source becomes a new `dataset_version` directory. The shipped files carry the provenance, the counts and a licence note; the preparation script ships beside them so a release is reproducible rather than mysterious.

**Two data facts the import forced into the open, both now impossible to miss.** The publisher writes province and city codes truncated (`11`, `1101`), so preparation widens them to the six digits GB/T 2260 uses — which is also what makes a row's level readable from its code alone. And **three cities administer no districts and the publisher's tree repeats each of them under its own code**, once as a city and once as the single "district" inside it: 东莞市, 中山市 and 儋州市. A code names one node, so the shallower occurrence is kept, the repeat is recorded in the file's `dropped` field, and the townships (36, 23 and 18) still attach to the city. Left alone, the unique index would have decided which of the two survived — silently, and differently on the second run — so the import now **refuses** a release carrying a duplicate code instead of importing it half-way.

**What the three-place wiring means in practice, and where it is not enough.** The gem is in the CI matrix, its own `Gemfile` resolves the monorepo's dependencies, and the starter's `server/Gemfile` lists it — so a worktree loads it after `bundle install` and `rails spree:install:migrations`. `server/` is a clone rather than tracked code, which is worth remembering: the wiring that survives a fresh worktree is the CI list and the gem's own Gemfile, while the starter's line has to be present in each clone.

## 2026-09-19 — The service-area binding lands in core, and the plan's "the gem owns the polygon" is amended

Plan: `6.1-seller-service-area-routing.md` (the binding columns, the polygon and its validation), with the migration in `spree/core`.

The binding is now real: `spree_stock_locations` carries `administrative_division_id`, the polygon and its bounding box, the geocoding columns, and a partial unique index that gives one active warehouse per node — `deleted_at IS NULL` in the predicate because a stock location is paranoid, so a soft-deleted row that never flipped `active` would hold its node forever. MySQL has no partial indexes, so there the rule degrades to one binding per node and the active-only half is the validation's; the migration says which engine gives which, the way core's own API-key migration does.

**Two amendments to the plan, both recorded in it rather than discovered later.**

**The polygon, its validator and the write-time normalisation are in core, not in the gem.** Decision 18 had them in `spree_service_areas`, but a validator has to attach to `Spree::StockLocation` — a core model — and a gem may not reopen one: this fork's own extension ships no decorators at all, and the architecture plan's boundary forbids it. So the classes sit beside the column they protect, keeping the plan's feature-prefixed names (`Spree::SellerRouting::Polygon`, `Spree::SellerRouting::PolygonValidator`) so the gem can reopen that namespace for `Locate` and `Decision` instead of duplicating it. **The gem keeps everything that needs no core class**: the routing service, the decision object, the reverse geocoders, the code mapping and the endpoint.

**The geometry is hand-rolled rather than RGeo's.** The plan chose in-memory RGeo for portability across the three databases; the same portability arrives without adding a dependency to core, because the shapes are hand-drawn and small — self-intersection is a segment-crossing test, containment is a ray cast, and winding is a signed area. `rgeo` stays available to the gem if routing turns out to need more geometry than this, but core does not carry a geometry library to validate one column.

**The coordinates needed a scale, and MySQL is where that showed.** `t.decimal` with no scale is scale 0 on MySQL, which rounds silently: a warehouse at 39.9089 was stored as 39 — a hundred kilometres from where it is — while the other two engines kept the decimals, so nothing local noticed and the MySQL lane in CI did. The columns are now `decimal(10, 7)` through a migration of their own rather than an edit to the one that added them, because that one has run where this has not. **Upstream's `spree_addresses.latitude`/`longitude` carry the same defect** — the same `t.decimal` with no scale, on the same three-engine support — so address geocoding rounds to whole degrees on MySQL today. It is upstream's migration rather than ours to change, and the fork's own copy of that table is not worth a permanent divergence; recorded here because the first person to compare a geocoded address against a map on MySQL will otherwise spend a day on it.

**What the write path guarantees, and the one thing it deliberately does not.** A polygon arrives either way round and is canonicalised rather than refused — an operator's map editor should not fail over an orientation nobody chose — while genuinely unusable geometry (too few points, an open ring, a non-finite or off-map coordinate, a ring that crosses itself) is refused with a readable message. The bounding box is derived on write, because the match path tests it before parsing anything and a box computed per candidate is a box computed thousands of times.

## 2026-09-19 (later) — The administrative tree is readable: one public collection, cached by release

Plan: `6.1-administrative-division-gem.md` — **implemented**, except for the vendor code maps.

`GET /api/v3/store/administrative_divisions` answers the client's three calls, because they differ in filter rather than shape: no filter is the province list a picker opens with, `parent_code` is its lazy cascade, and `keywords` is its search box — which searches the romanisation as well as the name, since the dataset carries both. Each node answers `{ code, name, level, first_pinyin, has_children }`. The controller lives in the gem and draws its route through `Spree::Core::Engine.add_routes` — the hook this repository's own extension already uses — so no core routes file is touched, and inheriting the Store API's base is what gives it the publishable key, the guest gate and the error shape.

**The cache is keyed by release and filter, so a new import is a new key rather than an invalidation** — the previous release's entries age out on their own, and nothing has to be swept. One key does have to be cleared: the memo of *which* release the table holds, because a rake task that writes a release and leaves every reader believing in the old one strands the whole tree. The import clears it.

**One edge case was worth the spec that found it, and it is now a regression test.** The obvious implementation of `parent_code` resolves the code and matches on the id; a code that resolves to nothing makes that `where(parent_id: NULL)` — which is the root's own predicate — so a question about a node that does not exist would have answered the top of the tree. It answers an empty list instead: a picker whose step disappeared shows nothing, which is the truth.

**What the plan still owes is the Tencent and Amap mapping files.** They ship beside the tree — the ruling moved them here — but nothing reads them until the routing slice's reverse geocoder does, and inventing mapping data before it is needed would be guessing at a shape the mapper should decide.
## 2026-09-19 (later) — The reverse geocoder ships Tencent first, and the seam is what makes that cheap

Plan: `6.1-seller-service-area-routing.md` — Decision 8 amended, Open Question 3 narrowed.

The plan had both adapters shipping with a configured primary and fallback. **The first version ships the Tencent LBS adapter only.** The decision is about a resource rather than a design: a vendor account and its quota belong to the operator, and the second vendor earns its place the day someone holds a key for it — while the pluggable seam is worth nothing if it is only written down and never travelled.

**What is deliberately unchanged is everything that makes a late arrival cheap.** The provider interface, the store-level provider and fallback configuration, and the cache key carrying the provider all stay exactly as written; the fallback path stays in the code even though, in the first version, there is no second provider to try — that is the state a fallback is in most of the time, and `Resolve` handles it. Adding Amap later is one adapter class and one mapping file under `data/administrative/amap/`, not a change to the orchestration, the cache key or the API.

**What the mapping turned out to be, once a vendor was actually read.** The plan (and this entry's own first draft) assumed a translation table: vendor code in, bureau code out, shipped as versioned data beside the tree. Tencent does not need one — **its `adcode` is the bureau's own code at the level it resolved**, so the mapper walks the imported tree from that code instead of translating it, and matches a township by name inside the district. `data/administrative/tencent/` is therefore an exceptions file with nothing in it: it earns its first row the day a vendor name and a bureau name disagree about the same place. The township check the plan owed is unchanged, and it is now the only thing standing between a district-level match and a township-level one.

**Consequences recorded rather than discovered.** The administrative gem's mapping debt is now a single file — `data/administrative/tencent/` — and the plan's township-coverage check narrows to Tencent, with the same check owed for Amap only if it ships. The `preferred_reverse_geocode_fallback_provider` setting stays in the store preferences, unused, because removing and re-adding it would be a migration for a column that costs nothing.

## 2026-09-19 (later) — The fork's features are documented in their gem, not in upstream's docs tree

Decided while shipping `spree_service_areas`, and it applies to every gem this fork builds.

`CLAUDE.md` says a new model or entity gets a page under `docs/developer/`, and `docs/developer` is upstream's Mintlify tree — deployed to the public documentation site and rewritten by every upstream sync. **For the fork's own features the documentation is the gem's `README.md` plus the plan** (`docs/plans/6.1-*.md`), which is what `spree_administrative_divisions` already does and what this ruling makes deliberate rather than accidental. A page there would be a merge conflict on every sync, for content that describes software nobody outside this deployment runs.

**What still goes in `docs/developer`** is anything that belongs to upstream's own surface: a core or API behaviour this fork changes, or a page upstream's readers expect to find. That is the original rule's real subject, and it is unchanged.

## 2026-09-19 (later) — A seller can serve abroad, and the store's market is not how we say so

Plan: `6.1-seller-service-area-routing.md` — its new Decision 23 and the boundary in its constraints. Raised by the author, asking whether a seller that faces other countries can be scoped to a region.

**Today's answer is no, and not by oversight.** Spree's `Spree::Market` is the *store's* region: which countries it sells into, with currency, tax and return window, resolved per request from the country header. **Nothing links a seller to a market** — and upstream records that a market-scoped assortment waits for its own regional-assortment design (`catalog_assignment.rb`). This fork's coverage is a node of the Chinese administrative tree, and that tree's `country` level holds exactly one node. A seller with no Chinese warehouse therefore binds nothing, and `resolve_seller` says so honestly: `matched: false`, which the client renders as 当前城市未开通服务.

**What was ruled instead of built.** Coverage gains a second form on the same row: beside the node and the polygon, a warehouse may carry **the countries it serves**, defaulting to the country it stands in — because a seller shipping from abroad has no shape worth drawing, and its coverage genuinely is a country list. Routing needs no new path for it: the algorithm already walks up to the country level, and `match_type: 'country'` already exists in the decision. **It stays unbuilt** because this client is China-domestic (no cross-border flow appears in the mini program or in any plan beside this one), and a branch nothing exercises is a branch nobody maintains. What it waits on when the need arrives is named in the plan: the vendor's nation name mapped to ISO, since Tencent answers a name and the normalizer currently hard-codes the `CN` root.

**The boundary, written down because it is the tempting mistake.** A store's market must not become the switch for which sellers a customer sees. That question is seller discovery, it belongs to this binding, and a market's countries describe the store's commerce rather than any shop's coverage.

## 2026-09-19 (later) — A price context is the client's own word, and the offline price is the operator's cost

Plan: `6.1-seller-scoped-pricing.md`, whose last open question this settles. Four rulings, all daily questions for whoever runs this deployment.

**The seller axis needed no schema, and that is the whole design.** A seller's price is its offer variant's **base** price (`price_list_id IS NULL`) — the offer *is* the seller's participation in the catalogue — so `spree_prices` gains no `seller_id`, no list gains one, and `Spree::Pricing::Context` (the platform's hottest read path, and its cache key) gains no seller axis. The three pages the client draws — 区域, 现场推广/分销, 线下 — differ in **which context the request is in**, and that is a channel, which is what core's `ChannelRule` already names as the forward path and what `Spree::Api::V3::ChannelResolution` already resolves at the request boundary. So the product read the client already calls is untouched: no parameter, no second route, no per-context endpoint.

**The three contexts go on the wire under the client's own names — `area`, `scene`, `offline`.** Those are the values the client already sends as `cartType`, they are the channel `code` the operator creates, and they are what a rewritten client puts in `X-Spree-Channel`. The channel's human name is the operator's own text, so 区域 / 现场推广 / 线下 live in the admin as data rather than as a wire value, and no mapping layer sits between the two. A fourth context is a row the operator creates, not a release.

**The offline context's price is the operator's cost.** 线下 activities are run by the platform, so the platform bears the difference and the seller's payout follows the price it agreed to — through the `subsidy` transfer kind the membership plan already carries. **This is a ruling, not a build**: nothing is wired for it here, because the capability belongs to `6.1-membership-tiers-and-rights.md` and re-opening it would fork the ledger's design. The alternative — that an offline price is the seller's own concession — was refused because it changes what the books say about who paid for the promotion.

**The gem is `spree_price_contexts`.** Routing (`spree_service_areas`) resolves *which seller* serves a coordinate; this one prices *for which context* a request asks. Two gems because they are two questions, and because the second is mostly convention: what ships is the channels' naming, the sellers read (`GET /api/v3/store/products/:id/sellers`, answering which sellers hold an offer a shopper could buy today — never whether one serves the customer's location), and the naming in the panels.

**Kept apart deliberately:** the sellers read has no eligibility rule of its own (the routing plan owns that), the three contexts are not Ruby classes, and the seckill stays out — `6.1-flash-sales.md` prices it as a discount, not as a channel.

## 2026-09-19 (later) — A price's provenance is two reporting dimensions, and the base price is its own group

Plan: `6.1-seller-scoped-pricing.md`, which named the requirement and left the shape open. It matters beyond that plan: any report about money has to say which price it read.

**The platform's reporting vocabulary gains `price_list` and `price_source`, registered by `spree_price_contexts`.** Grouping gross sales by `price_list` answers "what did the offline channel sell at" — a context's prices live on a list, and the order line already records which one priced it. `price_source` answers who quoted the price: nil for the platform's own walk, `manual` for a price an admin negotiated on the line, anything else a pricing provider's key.

**A line with no list is a group of its own, not a missing value.** It carries a base price — the seller's own (its offer variant's price) or the operator's — which is the category the EU Omnibus rule tracks, and a price list's price is internal segmentation. Every report this fork writes about money must say which of the two it is reading; the `price_list` dimension is where that distinction is expressed rather than in prose.

**The seller is not a third dimension here.** `spree_line_items.seller_id` already has a `seller` dimension in the platform's vocabulary, so whose price a line carries is answered beside the provenance rather than by reaching through the variant.

**Where a plan should record its reporting needs:** a plan that introduces a money figure registers its dimensions through `Spree.reporting`, in an initializer, the way this one does — the registry is the extension point, and the dashboard reads it, so a new dimension needs no dashboard release.
## 2026-09-19 (later) — The seckill pool never overrides the shelf, and the queue is the plan's

Plan: `6.1-flash-sales.md`, whose last two open questions these settle.

**A seckill sells neither what the shelf does not hold nor more than the activity promised.** Effective availability for a seckill line is the smaller of the pool's remaining units and the goods' own stock — the pool is an additional cap, never a substitute for stock. The ruling matches the check order the client already applies (商品库存不足, then 活动库存不足, then the per-customer caps) and puts it where the write happens, so an activity can read 已抢光 with pool left: the pool was right and the shelf was empty. The alternative — letting the pool authorise a sale the shelf cannot fill — would need the oversell/backorder machinery this fork has not built, and it would make a 秒杀 an unbounded promise to the customer.

**The ticket queue is this plan's, not a gap.** `promotion/addTicket`, `promotion/getTicket` and the 开售提醒 pair are owned by the flash-sales gem. The inventory's ambiguous "~4" is read as those four: without a claim path the pool is a counter nobody draws from, and the alternative reading would leave the only mechanism that makes the pool work unowned by any plan.

**The hold is still the gem's own, and it is built to be lifted.** Group buying generalises it into core when it arrives (ruled 2026-09-18); this run builds it against the four questions that lift depends on — owner, TTL, visibility to `Stock::Quantifier`, and the backorderable case that a pool-scoped hold answers by owning no `StockLevel` at all. Two implementations of a hold must not diverge in the meantime.

**What is deliberately not built here: the settlement discount and the activity's 积分/优惠券 refusal flags.** Both attach to the price preview, which `6.1-store-api-miniprogram-gaps.md` owns as one of three shared surfaces — a second price calculation is exactly what that plan forbids. A seckill therefore prices at the preview's work, and this gem stops at the claim.

## 2026-09-20 — The seckill price is a discount the cart charges but does not label as one

Plan: `6.1-flash-sales.md`'s settlement step, built on the 2026-09-18 ruling that a seckill's price is a `manual`-kind discount rather than a promotion action.

**The ruling holds, and this is what it means in the ledger.** A cart sums *every* discount into `adjustment_total` and therefore into the customer's total, so a seckill's saving is charged correctly — two units at 100 with an activity at 60 come to 120. But the cart's and the line's own `discount_total` columns count `promotion` rows only, so that column stays 0 for a seckill. **Any report looking for seckill sales must read the discount rows — `kind: 'manual'` with the activity and the ticket in their metadata — and never `discount_total`.** The line's *displayed* price is not that column either: it comes from the price preview, which quotes the activity and strikes the catalogue price through.

**The row carries both rows it came from.** `metadata.flash_sale_id` and `metadata.flash_sale_ticket_id` are what make a seckill line's refund recomputable and what lets the order settle the tickets it was priced with, rather than guessing from whoever happens to be holding one.

**A claim prices a cart that already holds the goods.** The claim is where a ticket and a cart meet, so a cart with the line is discounted then rather than waiting for the settle page — and a re-claim updates the same row by its code instead of stacking a second discount on the line.

## 2026-09-20 (later) — The preview answers both shelves, and the price's window travels with the price

Plan: `6.1-seller-scoped-pricing.md`'s step 4, which had to consume the timer `6.1-flash-sales.md` settled rather than invent a second one.

**A line answers the goods' own availability and, when the request names a warehouse, that shop's.** `stock_location_quantity` sits beside `available_quantity`: the area context's `shopStock`/`specStock` pair, which the client switches between on the basket it is building. The location is read through `current_store.stock_locations`, so an id from another tenant is a 404 rather than a figure, and it is nil when the request names no shop — every read that is not an area page.

**The price's window is one field, whatever time-boxes it.** `price_ends_at` is the instant the answered price stops holding: a preview source's own window when a source priced the line, otherwise the pricing list's `ends_at`. It is what the client's countdown renders, and it is the fork's *only* timer — a seckill answers the open stretch's close (or the activity's end when it is sold throughout) through the same field, so the area's time-boxed price and the seckill price do not grow two mechanisms. **A preview source that time-boxes its price returns `ends_at` beside `amount`, `label` and `flags`.**

**The window is hidden with the price; the shelf figures are not.** `price_ends_at` is the answered price's own context — a countdown to a price nobody may see is meaningless — so a storefront that hides prices hides it too. The stock figures (`available_quantity`, `stock_location_quantity`, `in_stock`) are inventory rather than price and travel either way, which is the posture the read has always had: what the goods cost is hidden, whether they are there is not.

## 2026-09-20 (later) — A share link's values are escaped, and what can be shared is a seam

Plan: `6.1-store-api-miniprogram-gaps.md`'s one share payload — the last piece of its first migration step, and the surface four other plans were each about to compose for themselves.

**The link's values are percent-escaped, because this API's own ids carry the grammar's separator.** The client's decoder splits `typeId` on `_`, turns each `-` into `=`, and joins the parts with `&` (`static/behaviors/locationLinkBehavior.js:7-13`), so a prefixed id like `prod_UkLWZg9DAJ` would decode as two fields — `id-prod` and `UkLWZg9DAJ`. The composer escapes `% _ - & =` in every value and the decoder is left untouched: the page reads its query through a URL parser, which is where an escaped value comes back as it was written. A second implementation of this grammar would have to remember the escaping, which is the third reason there is one composer.

**What can be shared is a config seam, not a branch in the route.** `Spree.shareable_targets` maps an `api_type` shorthand to the relation its target is resolved through — the product row resolves through the store's available catalogue, narrowed by the request's channel and the buyer's catalogs, so another store's product and a product this storefront does not show are both 404s. An extension adds its type to the map; a record that does not answer `share_descriptor` is not shareable whatever the map says. The referral, coupon, group-buy and product-authentication plans add their own types without touching the route.

**The QR and the poster are absent on purpose, and the fields ship anyway.** The mini-program identity capability that mints them does not exist yet, and no domain may fetch a token itself — so a target answers them nil and a client renders what it has. They are in the payload from the start so that the day the capability lands, a client's rendering does not change.
## 2026-09-20 (later) — A bundle's rule is a preference, and an extension's API is its own document

Plan: `6.1-product-bundles.md`, whose first four migration steps are built.

**The discount rule is a preference pair, not columns.** `preferred_discount_kind` (`amount` or `percentage`) and `preferred_discount_value` on the bundle, which is why the table carries a `preferences` column the plan's schema did not list — every Spree model keeps its own preferences that way, and the rule is configuration rather than data. The figures are computed from the components' *current* prices at the store's currency, so a component moving upstream moves the bundle with it and no stale number is ever stored.

**A Chinese title keeps its own text as the slug.** `parameterize` drops everything that is not ASCII, so 套餐 1 becomes `1` — a slug a merchant cannot read and cannot tell apart from the next one. A title carrying any non-ASCII character is therefore its own slug; an ASCII title takes the usual one, and a title with no words at all falls back to a generated string. This is the fork's answer to the same trap `Spree::Seller#normalize_slug` still has, and it is deliberately local to this gem rather than a core patch.

**An extension gem's endpoints are documented by the gem, not by the generated artifacts.** Typelizer reads `spree/api`'s serializers and `store.yaml` is written by that engine's integration suite, so an extension's routes cannot appear in either — `spree_flash_sales`, `spree_price_contexts` and `spree_service_areas` are absent from both today. The convention this plan now records: the gem's `README.md` is its API documentation, its specs are request specs rather than rswag, and the plan's "regenerate types" step applies only to the parts of a plan that touch core.

## 2026-09-20 (later) — A bundle's lines name their bundle, and the group row carries its owner

Plan: `6.1-product-bundles.md`'s cart step, built on a synchronous subscriber rather than a second write path.

**A line names its bundle in its own metadata, and there is no second add path.** The client adds a set through the ordinary cart write — one line per component, each tagged `bundle_id` in the line's metadata — and a synchronous subscriber on the line item's own lifecycle events reads the tag, writes the group row and recomputes the saving. A subscriber rather than a controller patch, because a gem ships migrations and not core changes; synchronous (`async: false`) rather than queued, because the cart the client reads back has to show the set it just added and the saving is part of that answer. The alternative — a gem-owned "add this bundle" endpoint — was refused because it duplicates the cart write and gives the same operation two roads.

**The group row carries its owner, which the plan's schema did not.** A line item is hard-deleted, so at the moment the group needs to be recomputed — the customer removed a component — the line is already gone and its owner with it. `spree_bundle_line_items.owner` (polymorphic, cart or order) is what keeps the cart recoverable, and it also makes a recomputation a single indexed lookup instead of a join through line items.

**The saving is allocated by the plan's own rule, not by core's distributor.** `Spree::Orders::Discounts::Create` distributes an order-level discount over the whole order by largest remainder; a bundle's saving belongs to its own lines only, so the gem computes the shares itself (in proportion to what each line costs, the rounding remainder on the last line by position, as ruled 2026-09-18) and writes one row per line through the same `manual`-kind rows every other gem-owned discount uses. The rows carry a gem-owned `code`, which is what makes a recomputation replace its own rows and no others.

## 2026-09-20 (later) — A cart's sets are the bundles gem's own read

Plan: `6.1-product-bundles.md`, whose cart step said the grouped read "is the cart API's". Building it changed the answer, and the reason is a boundary rather than a preference.

**The cart read stays core's, and the sets are `GET /api/v3/store/carts/:cart_id/product_bundles`.** For core's cart payload to carry a set, core would need a seam for "whatever the lines form", a value type to describe one, and a serializer for it — three pieces of core vocabulary that exist for a concept only this fork has, and that a merchant with no bundles would still carry. The client pays one extra call on the cart page instead, and the payload it gets is the one the gem owns: the set's title and figures, plus the cart's own lines serialized by the Store API's line item serializer, because a set is a reading of the cart and not a second kind of line. **The same reasoning applies to any future gem that groups cart lines** — an extension reads them itself rather than asking core to know about them.

## 2026-09-20 (later) — A cart line is ticked or not, and the money still counts every line

Plan: `6.1-store-api-miniprogram-gaps.md`'s cart selection, whose Key Decision 28 says a cart's money is computed over its selected lines. The state is built; the pricing half is not, and that is deliberate rather than forgotten.

**The tick is durable cart state, named on the wire as the model names it.** `spree_line_items.selected` (default true — a line is ticked when it is added), `selected` per line and `selected_quantity` on the cart in the payload, written in bulk by `PATCH /api/v3/store/carts/:cart_id/selection` with `{ selected, line_item_ids }`. The client's own words are `choose` and `chooseTotalQuantity`; the API keeps its own vocabulary and the rewrite maps them, the same way every other field in this fork reads and writes under one name. **Completion copies only the ticked lines** — and a fulfillment whose items were all unticked reaches the order with no items rather than failing, because the copy maps cart lines to order lines and an uncopied line has no counterpart to point at.

**The money scoping is its own slice, and the reason is the surface it touches.** "A cart's money is computed over its selected lines" means `item_total`, tax, promotions, delivery estimation and the order's `total` all read the selected set — the totals workflow, the promotion adjuster, the tax estimator and the shipping estimator each iterate `line_items` today. Shipping is the sharpest edge: a package is priced over its contents, so an unticked line either leaves the package or changes what the package weighs. Doing that in the same change as the flag would put a money rewrite and a new column in one review; the flag alone is useful (a client sums the ticked lines' own amounts to show a chosen subtotal) and changes no figure anyone is charged. **Until then, a cart's `total` is every line's**, and no plan may describe a cart's money as the chosen subset's. *(Built later the same day — see the entry below, which retires this "until then".)*

## 2026-09-20 (later still) — A cart's money is the chosen lines', and the counts are not

Plan: `6.1-store-api-miniprogram-gaps.md`'s Key Decision 28, whose state half shipped in the selection PR and whose pricing half is this one. It retires the "until then" clause of the entry above: a cart's `item_total`, `adjustment_total`, `delivery_total` and `total` are now the ticked lines' figures.

**One seam defines the priced set, and every money path reads it.** `priced_line_items` — a cart answers with its ticked lines, an order with all of its own — replaces `line_items` in the five places that price a record: the totals workflow (the subtotal, and the fold that re-sums discount and tax rows into the record's columns), the promotion adjuster (per-line candidates, the order-wide winner's distribution, and the applicability gate), the tax item list, the inventory-unit builder, and completion's copy. The unit builder is where the delivery half lands: packages are packed from inventory units, so filtering there scopes package contents, weight, every delivery-method rule, every rate calculator and the fulfillment costs in one place. **Pricing and completion must never disagree about which lines are being bought**, so they read one set rather than two rules that happen to match.

**Counts are not money, and stay whole-cart.** `total_quantity` keeps counting everything in the cart: it is the cart's size rather than what is being bought, and `digital?` and promotion eligibility both read it. The chosen half has its own answer, `selected_quantity`, which shipped with the flag.

**A row on an unticked line is excluded from the money, never deleted.** A merchant's manual discount — and this fork's own bundle saving, which writes the same manual rows — must still be there when the shopper ticks the line again, and nothing recomputes those rows. So the fold excludes rows by the line they hang off instead of the adjuster removing them, which leaves an unticked line carrying its own figures while contributing none of them to the cart.

**A cart with nothing ticked cannot be checked out.** The checkout requirement for line items now reads the priced set, so completion refuses with `validation_failed` and a structured error, rather than placing an empty order from a full cart. The message is the generic "line items required" one; a client that wants to say "choose at least one item" says it itself.

**The seam reaches every question the checkout asks about the purchase, not just the money columns.** A review of the first cut found the promise broken anywhere a line was still read directly: completion's stock and quantity-rule gates refused a cart because of an unticked line the shopper had deliberately set aside (blocking checkout until they *deleted* what they had merely unticked); the delivery-step and shipping-address gates asked for an address on a purchase that ships nothing; the digital predicates called a cart with one unticked physical line a shipped one; and promotion eligibility answered "does this purchase contain product X" from every line, so a trigger product sitting unticked still fired the discount on what was being bought. All four now read the priced set, through the seam and through `priced_product_ids` beside it. **The order's own `total_quantity` is the count of what was copied into it**, not the cart's whole count: the "counts stay whole-cart" rule is about the cart, and an order reporting lines it does not hold would overstate the sale on every later view.

**The tick belongs to the shopper, and no other write flips it.** Adding a variant that already sits unticked in the cart grows that line's quantity and leaves it unticked. Auto-ticking would read naturally from "a line is ticked when it is added", but the same write is what a cart-page quantity edit sends, and silently re-ticking a line the shopper had cleared — or charging for one they did not tick — is the worse of the two surprises. A client that wants an add to tick says so, with the bulk write it already has.

**What is deliberately still unbuilt: the unticked line keeps its stock reservation.** `Spree::StockReservations::Reserve` iterates the cart's lines when checkout begins, so a line the shopper unticked holds stock it is not being bought for until the reservation expires. That is inventory rather than money — the reach this decision set out to bound — and releasing it needs the reservation's own lifecycle (a re-tick must not double-hold, an expiry must not free a held line), so it waits for its own slice rather than riding this one.

## 2026-09-20 (late) — The counts get their own read, in the cart's own words

Plan: `6.1-store-api-miniprogram-gaps.md`'s `cart/getCartCount`, which left the shape open — "read the count off the cart serializer or add a dedicated lightweight endpoint".

**A dedicated read: `GET /api/v3/store/carts/{cart_id}/count`.** The client asks for the badge on the pages that have no other business with the cart — the product pages, the menu — and the cart payload answers with every line and its variant, media and prices. Reading a full cart to print a number is work nobody asked for, and it is asked for often. The payload carries `total_quantity` and `selected_quantity`, the cart serializer's own field names rather than a `count` of its own: one vocabulary means a client that reads a count off the cart reads the same word here, and the chosen figure (which the client shows on the cart page as `chooseTotalQuantity`) comes from the same read rather than a second one. The write side is addressed like every other cart sub-resource — nested under the cart, `CountController` beside `SelectionController` — so the shape is the API's rather than the client's endpoint name.

**Recorded beside it: the selection has a reference entry now.** The endpoint shipped in the flag's PR without one, which left `PATCH /api/v3/store/carts/{cart_id}/selection` out of the published API reference while every other endpoint this fork added has one. Its integration spec rides this change.

## 2026-09-20 (the batch write) — A set of lines is written as the cart should hold it

Plan: `6.1-store-api-miniprogram-gaps.md`'s batch cart rows (`cart/addCartBatch`, `setCartBatch`), whose table read "one request carrying many lines, applied atomically".

**Its own path, not the collection's: `POST /api/v3/store/carts/{cart_id}/items/batch`.** The collection's POST *adds* to a line and the batch *sets* it, and one verb carrying two meanings is a trap a client finds out about by being charged twice; a path of its own keeps each verb meaning one thing. It is the shape the codebase already gives a batch write — `POST /translations/batch` is the precedent, comment and all — and it keeps the published reference's diff to the new operation rather than re-cutting the file.

**The quantities are the set the cart should end up holding.** A retried batch writes the same cart instead of adding twice, which is what a phone on a flaky network needs, and it is the semantics the cart's single-item `PATCH` already speaks. An entry names the variant to buy or a line of this cart — a combo knows its goods, a group's quantity edit knows its lines — and a line id outside this cart is refused rather than skipped, because the client asked for a set and answering with a different one hides its stale state. Zero and negatives are refused too: removals are the DELETE endpoint's, and a write that reads as an edit must not delete.

**A refused entry warns; a request that cannot be applied is refused whole** (the author's ruling, 2026-09-20). The plan's "applied atomically" is narrowed deliberately: an entry the cart's own rules refuse — out of stock, an unsellable currency — rides the cart's existing `warnings` channel and the rest of the set still applies. The workflow was built that way for cart restores, and a shopper restoring saved goods must not lose everything to one unbuyable line. What is refused whole is what is not a fact about one entry: an empty set, a quantity that is not a positive whole number, a line this cart does not hold.

**What the client sends that this deliberately ignores: `promotionCode`, `promotionId` and `frozenQuantity`.** Choosing between promotions is its own slice (Key Decision 29), and a combo's discount is the server's own business here — this fork's bundles gem prices a set from the lines it sees, so a client writes lines and no promo identity. `frozenQuantity` is the old server's promotional hold, which has no counterpart in this cart yet.

## 2026-09-20 (the payment deadline) — The deadline is told, not enforced

Plan: `6.1-store-api-miniprogram-gaps.md`'s order-reads row, and the correction that row needed.

**The client asks for a countdown, and the deadline it counts to is display state.** Its order list and detail read `orderPayEndTime`, `orderPayLimitTime` and a local `tamp`, and flip their own status to "超时未支付, 订单已取消" when the clock passes (`static/behaviors/orderListBehavior.js:12-20`, `pagesF/static/behaviors/orderInfoBehavior.js:62-63`). The server answers **`payment_deadline`** on the order payload — `completed_at` plus the store's **`unpaid_order_timeout_minutes`** preference (default 30, declared beside the stock-reservation TTL it mirrors) — and nulls it while there is nothing to pay: a draft, a settled order, a canceled one, a free one, or a store that keeps no timeout (zero or less). Anything the client shows beyond that instant is its own arithmetic.

**Nothing cancels an expired order** (the author's ruling, 2026-09-20). A deadline that fires work of its own would need a background job, an expiry state and a rule for a payment that arrives late — and the merchant already has a cancellation flow for deciding what happens to an order nobody paid for. Written down so the next reader does not "finish" the feature by adding a job.

**The plan's row was wrong, and is corrected in place: 超时赔付 is late-delivery compensation, not payment expiry.** The two client calls the row listed read an approval status and its review nodes, not a payment clock. The deadline those rows were mistaken for was on the order list all along under a different name.

## 2026-09-20 (the order search) — A buyer's keyword search reaches the goods

Plan: `6.1-store-api-miniprogram-gaps.md`'s order-list row, whose keyword filter the client's own search implies.

**`Order.search` now matches the names of the goods an order holds.** A shopper looks for "the tea set I bought" as often as for a number, and their own order list is where they look. It is asked as an **EXISTS rather than a join**: an order with three matching lines is still one order to list, and a join would both repeat it and inflate a paginated count. Upstream's reach — the order number, the purchase-order reference, the buyer's name and email — is untouched, so nothing that found an order before stops finding it.

**A filter the reference documented and the code never had is corrected.** The Store reference's order list documented `q[state_eq]` and `q[payment_state_eq]`; the allowlist carries `status` and `payment_status`. This is the quiet kind of wrong: Ransack ignores a filter it cannot build rather than refusing it, so a client following the reference got an unfiltered list and no error to notice. The reference now names the filters that exist, and the keyword's reach.

## 2026-09-20 (the confirm-receipt identifiers) — A payment names its gateway's identifiers, and the gateway answers a contract

Plan: `6.1-store-api-miniprogram-gaps.md`'s order-payload row, which lists `merchantTradeNo`, `orderNo` and `transactionId` as fields this plan owns.

**They ride the payment, not the order.** They describe one payment: the merchant the sale was taken for, the reference the gateway knows that order by, and the gateway's own id for the transaction. So the order payload carries them where it already carries payments — `payments[]` — and the client picks the one that was paid. Reading the client's names against ours also settled what each one is: `merchantTradeNo` is not a trade number at all but WeChat's **merchant number** (the mchid), `orderNo` is the **merchant order number** our `Payment#response_code` already carries, and `transactionId` is WeChat's own — which for this flow lands in the payment's metadata, because `response_code` is already spoken for.

**The generic serializer asks the gateway, and the gateway answers a contract.** `merchant_id` and `gateway_transaction_id` are read through `try` off whatever payment method the payment used: the WeChat gem answers both (`preferred_merchant_id`, and the metadata its own `payment_metadata` wrote), every other gateway answers neither and the fields are null. The API layer never names the provider gem — the same duck-typed shape the share composer and the pricing sources use — and the merchant number is publishable by nature: WeChat issues it for the client to hand back inside `wx.openBusinessView`.

## 2026-09-21 — The balance refuses nothing yet, because a gift card is not a line here

Plan: `6.1-store-api-miniprogram-gaps.md`'s settlement-page work. The client's settle page carries a verdict the old server computed — "订单商品不支持" with a list of the goods the balance cannot pay for (`pagesF/settlement/settlement.js:733`, `settlement.wxml:80,167`) — and the author ruled the rule worth having (2026-09-21).

**It is not built, and that is a fact about the data model rather than a deferral.** In this codebase a gift card is not merchandise: `Spree::GiftCard` and `Spree::GiftCardBatch` associate with a store, a customer and a batch, and with no product or variant at all — cards are minted in batches of a fixed amount and *applied* at checkout (`carts/gift_cards`), never sold as a cart line. A rule that refuses "a line that is a gift card" therefore has no line to refuse, and code written for it would never run. The old server evidently sold gift cards as goods, which is where its rule came from.

**It belongs to `6.0-6.1-loyalty-gift-cards-store-credits.md`**, the plan that will model buying one. The rule lands there, beside whatever identifies such a purchase, rather than here where it would be dead. If this deployment does sell gift cards today through some other shape — an ordinary product carrying a custom field, say — then the rule has a subject and can be built against that flag; nothing else in the settlement page's tender picture needs work, because the cart read already answers it: `payment_methods` is the list *available for this purchase* (the stored-value method appears only when the customer actually holds a balance), and the applied and available amounts ride `store_credit_total`, `gift_card_total` and the customer's own stored-value read.

## 2026-09-21 (the customer's cancellation) — A cancellation is created, and the button and the write answer the same question

Plan: `6.1-store-api-miniprogram-gaps.md`'s `order/cancelOrder` row: "Store API has no cancel route; the workflow and the actor columns already exist".

**`POST /api/v3/store/customers/me/orders/{order_id}/cancellation`, nested under the customer's own orders.** The nesting is the authorization, not decoration: the order is resolved through the customer's own association, so somebody else's is a 404 rather than a refusal that confirms it exists — the same reading every customer-scoped read in this API already has. It *creates a cancellation* rather than performing a `cancel` action, because that is what happens: one act, once, whose result is the order in its canceled state, which is what the response carries. The route lives under `customers/me` rather than on the store's order surface, where a guest holding an order token can read an order but has no business calling one off.

**`cancellable` on the order payload, and the workflow asks again.** The button reads `allow_cancel?` off the payload so a control that would be refused is not offered; the workflow re-asks when the write arrives, because an order can be dispatched in between — the second question is not redundant, it is the one that decides. The payload answers `false` the moment the order is canceled.

**A customer is not an actor, so nothing is attributed to them.** The cancel workflow takes a `canceler`, and the actor columns exist to name the staff or system identity that acted — this codebase's actor registry holds the admin user class and `Spree::ApiKey`, and its own spec asserts exactly that list. Handing it a customer raises rather than recording one, and widening the registry to include customers would change what those columns mean everywhere, not just here. So a customer's own cancellation stamps *when* it happened, the reason and their note, and leaves the actor columns empty: the merchant's own reason vocabulary is what tells them a customer called it off. If a later slice wants customer attribution, the actor registry is the thing to decide about, once.

**`status` joins the store order payload for this reason and no other.** A customer who has just called their order off has to be able to see that it was, and the payload carried only `fulfillment_status` and `payment_status` — how the order is being served, not what it is. The value list is `Spree::Order::STATUSES`, the same closed set the admin payload uses.

**The reasons are the merchant's own, and retired ones stay behind.** `GET /api/v3/store/order_cancellation_reasons` lists the active reasons — merchant-owned like the return and claim vocabularies, so nothing branches on the value. A retired reason is not offered again while staying on the orders that already carry it, which is what reporting needs. A reason from another store is not a reason here: the read resolves it through the store's own list and the workflow holds the same line, so the refusal never depends on which of the two got there first.

## 2026-09-21 (the hidden order) — Hiding is the customer's own history, not the order

Plan: `6.1-store-api-miniprogram-gaps.md`'s `order/delUserOrder`, which asked for a per-customer hide flag rather than a soft delete.

**A `customer_hidden_at` column on the order, reached through `DELETE /api/v3/store/customers/me/orders/{id}`.** One customer per order means the flag belongs on the order itself — a join table would model many viewers where there is one — and a timestamp records *when*, which is the first thing a support conversation needs. The write wears a delete because that is what the customer is asking for: they want the order gone from their history, and the client's own name for it is `delUserOrder`.

**"Gone" is the customer's side of their orders — the list and the lookup by id alike.** `Store::Customer::OrdersController#scope` reads `visible_to_customer`, so one scope answers both: a hidden order leaves the list and 404s on `show`. The merchant's side never reads the flag — the row keeps its state and its place in fulfillment, refunds and reporting, and the Admin API has no notion of hidden — which is the whole point of not making this a soft delete. A filter on the list endpoint alone, or a second scope for `show`, would have been two places to keep in step.

**There is deliberately no un-hide.** The plan asks for a delete, the client offers no undo, and a way back needs its own surface — a "hidden orders" list the customer has no way to reach once the row is out of sight — rather than a flag nobody can see. Written down so the next reader does not "finish" the feature by adding one.

**The customer's other order writes do not read the flag, and must not start.** Calling an order off is a fact about the sale, not about the customer's list, so the cancellation finds the order through the customer's own association whether or not it is hidden — and the order-level balance payment, when it lands, is the same kind of write. Hiding says nothing about what the customer may still do with their order.

**What the flag deliberately does not reach: everything that is not that list.** The purchase history still counts a hidden order's goods — they were bought, and one-tap reorder is built on that fact — and no merchant surface reads the flag at all. What was asked for is a customer taking an order off their own list; each further reach (history, ratings, notifications) is its own decision with its own reason, and quietly widening this one is how a "hide" turns into a second, invisible delete.

## 2026-09-21 (the balance payment) — An order is paid from the balance, not reserved against it

Plan: `6.1-store-api-miniprogram-gaps.md`'s `order/balancePayOrder`, which ruled on 2026-09-18 that the plan owns paying an *order* from stored value and that it must spend through `Spree::StoreCredits::Apply`.

**A `store_credits` resource under the customer's own order: `POST /api/v3/store/customers/me/orders/{order_id}/store_credits`.** The cart's balance is addressed the same way (`carts/{cart_id}/store_credits`), and the customer's own order is where their balance may be spent — a guest order has no balance to spend, which is the nesting rather than a rule to remember. It answers with the order, because that is the only thing the write can be read in terms of.

**The balance is spent through the service and captured in the same call.** `Spree::StoreCredits::Apply` writes a `checkout` payment, which on a *cart* is exactly right — the money must stay reversible until the customer submits. On a completed order it is only a reservation: the customer cannot spend that balance anywhere else, and nothing later captures it, so the workflow captures what it applied. Trying to pay from the order would otherwise leave an order that says `balance_due` with the money already held.

**All or nothing, and the refusal names the shortfall.** The client's call is one act — it sends the PIN and nothing else (`miniprogram/components/order/balance-pay/balance-pay.js:66-78` sends `{ id, payPassword }`) and reads a truthy response as "paid" — so applying what a short balance happens to cover would answer a partial payment as a paid order while the rest is still owed. A balance that cannot cover the order is refused with the amount still due in the message, which is also the old server's own verdict (its `511015` branch: 礼品卡余额不足). **No `amount` parameter and no un-apply**: a partial balance payment is not what was asked for, and once captured the money is spent — undoing that is a refund, which is the merchant's.

**The PIN is not read in the API layer, on purpose.** The plan's seam is `Spree.payment_verifications`, consulted once by the apply service; a check in this endpoint would guard exactly one caller and let every other one through. The client's `payPassword` is therefore ignored until that registry lands — written down because a parameter accepted and not checked is the kind of thing that looks handled.

**A type bug this shipped: `amount_due` answered an Integer when nothing was due.** `[outstanding_balance - total_applied_store_credit, 0].max` takes the literal `0` when the balance is settled, and the serializers emit a BigDecimal as a decimal string but an Integer as a JSON number — so a paid order answered `amount_due: 0` where every other order answers a string, which is what the response schema declares. The floor is a `BigDecimal(0)` now, which is the shape `LineItem#taxable_amount` already uses. It surfaced only because this is the first recorded example of a *paid* order.

## 2026-09-21 (the customer's own profile) — What a person says about themselves is a column, and erasure reaches all of it

Plan: `6.1-store-api-miniprogram-gaps.md`'s customer-account rows.

**The profile is four columns, not metadata and not custom fields** (the author's ruling, 2026-09-21). `nickname`, `gender`, `birthday` and `city` are read on every app boot and will be segmented on — a birthday campaign wants an index, not a JSON scan — while metadata is documented as private developer data and custom fields are store-owned, which a global customer is not. The cost is named: a fifth profile field is a migration.

**The avatar needed no column: the customer already had one.** `avatar` has been an ActiveStorage attachment since 6.0, so the profile exposes it as `avatar_url` and accepts a direct-upload signed id as `avatar` — the same read/write split the admin profile already makes, because a URL is what a reader can use and a signed id is what an uploader has.

**"New user" is a count on the payload, not an endpoint.** The client asks `info/newUserCheck` to choose between first-order offers and what the customer usually buys; the payload carries `orders_count` — the completed orders this customer has in the request's store, so a sibling store's buyer is still new here — and the client derives its boolean.

**Erasure and the access export both name the whole profile.** A nickname identifies as well as the name it was registered with, so `Customers::Anonymize` clears all four columns and the picture, `Customers::DataExport` discloses them (the avatar by filename, since the file itself is purged), and the personal-data tripwire now watches for a `nickname`, a `birthday` or a `city` on any table — it would have missed all three before.

**Account closure is the erasure request, not a second soft-close** (the author's ruling, 2026-09-21). The mini program's 注销账户 is `POST /customers/me/data_requests` with `kind=erasure`; its four blockers — open orders, membership, balance, after-sales — are not enforced, because the right to erasure does not depend on what the customer still owes or holds, and the orders survive as anonymised records either way. One consequence is written down for the phone plan: the confirmation this path asks for is the account password, and a WeChat account has none, so the phone-code attestation from `6.1-phone-verification-and-payment-pin.md` is what will confirm for those accounts.

## 2026-09-21 (the wishlist's tabs) — The categories come from the goods, and the list pages

Plan: `6.1-store-api-miniprogram-gaps.md`'s wishlist-grouping row, which guessed the grouping "needs a field on the wishlist item".

**It needs no field: the categories are the goods' own.** A wishlist item reaches its product, and a product is in the catalogue's categories — a field on the item would be a second copy of that, kept in step by hand, and it would go stale the moment the merchandise department moved a product. The tabs are their own read instead (`GET /wishlists/{wishlist_id}/items/categories`) rather than an attribute the client groups by: a page with three hundred collected goods would otherwise fetch all of them to draw five tabs.

**The list is a list.** `GET /wishlists/{wishlist_id}/items` is paginated and ordered by when each good was collected, because that is what a wishlist page opens on; the items had only `create`/`update`/`destroy` before, so the client's `collect/page` — which pages with `current`/`size` and re-reads on every tab change — had nothing to call.

**`category_id` narrows through the store's own catalogue, and a category the store does not have is a 404.** The filter is not `where(category_id: params[:category_id])` falling back to an empty page: a tab naming another store's category, or one deleted since the page was drawn, is a tab that should not exist, and saying so is how the client finds out to re-read the tabs. Same line the controllers hold everywhere else — an incidental id is resolved through `current_store.<association>`.

**`collect/page`'s `offShelfList` is the client's own arithmetic, deliberately.** The old server split unavailable goods out for it; here the item payload carries the good and its variant, and the storefront decides what to show as unsellable — the `goodsShowRule` the client already applies. A server-side "off the shelf" flag would be promotion and availability state this API does not own yet.

## 2026-09-21 (the unticked line's stock) — A line nobody is buying stops holding stock

The piece `2026-09-20 (later still)` left open: an unticked cart line kept its reservation until it expired.

**The hold follows the tick.** `Spree::Carts::SelectLines` withdraws the reservation of every line it unticked — the stock goes back on the shelf at once rather than at the reservation's expiry — and takes a hold again for the lines it ticked, because a tick is the shopper saying this line is being bought. `Reserve` builds its targets from `priced_line_items` rather than every line the cart holds, so a later write to the cart cannot re-hold a line nobody ticked. Together the two halves mean the hold always matches what the cart's money is about.

**A tick that cannot be held still stands.** Ticking a line the shelf cannot cover is refused at checkout, not at the tick: the failure is the reservation service's, and the selection write answers with the cart rather than an error the shopper cannot act on from that page.

**Only what moved.** A client re-sending the same set — select-all over an already-ticked cart — touches no reservation, not even its clock, which is the rule the money math already followed.

## 2026-09-21 (the picker's answer) — The shopper's choice outranks the engine's winner

Plan: `6.1-store-api-miniprogram-gaps.md`'s promotion-selection row, whose Key Decision called the candidate computation the seam to reuse.

**The candidate set already existed; it was private.** `Spree::Adjusters::Promotion` computes, for every line, the promotions that could discount it (`line_item_candidates`, memoized) and then applies one winner. The work was to publish that list (`candidates_for`) and to let a choice name one of its entries — not to teach the engine anything new. The cart payload carries it per line as `promotion_candidates`, beside `promotion_id`.

**The preference is a column on the line, and the policy seam is untouched.** `spree_line_items.chosen_promotion_id`, like `selected`: a line's own state, and not a discount row — those are rebuilt on every recalculation and would forget it. The adjuster honours the choice in `apply_line_item_discounts` rather than in `select_applicable`, because the plan says 6.1 stacking "replaces just that method" and a choice is a line-level question, not a change to how candidates compete.

**A stale choice is not a discount.** When the chosen promotion stops applying to its line — the goods changed, it expired, it was deleted — the line falls back to the engine's winner rather than losing its discount. The choice stays on the record, so it takes effect again if the promotion becomes applicable again; nothing clears a column behind the shopper's back.

**Both identities come from the server's own list.** The write refuses a promotion that is not a candidate for the line, and a code that no longer matches the candidate's own: a list that went stale between rendering and choosing is a stale list, not a licence to apply the id to whatever answers to it today. A promotion that discounts several lines of the cart needs `line_item_id`, because "this promotion" then means more than one line and the write should say what it means.

## 2026-09-21 (the cart's shelf) — Recommendations are a query over what the catalogue already shows

Plan: `6.1-store-api-miniprogram-gaps.md`'s `cart/getCartRecommendGoods`, whose source the author settled on 2026-09-21: **Meilisearch**.

**The shelf is a query, not a new capability.** No "similar products" facility exists in this codebase — Meilisearch's `/similar` is never called and no embedder is configured — so the recommendations are read the way any catalogue read is: the categories the basket's goods are in (`in_categories`), ranked by what sells (`best_selling` over `units_sold_count`, which is a real column and a sort both providers implement), asked through `Spree.search_provider`. A store with an index answers from it and a store without one answers from the database, because the provider decides; nothing new was added to the provider contract.

**The basket's own goods are excluded on the scope, not as a filter.** `build_filter_condition` has no id-exclusion branch and silently drops a filter it does not recognise, so an exclusion passed as a search filter would quietly do nothing — and it would do nothing for the database provider too, which has no such filter. The exclusion lives on the ActiveRecord scope the provider intersects with, which both providers honour.

**A recommendation cannot see what the catalogue would not show.** The read answers from the same `ProductCatalogue` every other product read uses — the store's buyable goods, narrowed to the request's channel, company and group — so the shelf is a view of the catalogue rather than a second door into the catalogue. It is its own cart-scoped route (`GET /carts/{cart_id}/recommendations`) rather than a parameter on the products collection, because the question is about a basket and the client asks it that way; the row's "may be served from the products endpoint" was a guess at the shape.

## 2026-09-21 (the code and the PIN) — The verification service is built, and the send goes through our own notification gem

Plans: `6.1-phone-verification-and-payment-pin.md` (built), `6.1-notifications.md` (its first slice), `6.1-store-api-miniprogram-gaps.md` (the two account rows it unblocks).

**Two gems and one core seam.** `spree/extensions/spree_notifications` ships the door (`Spree::Notifications.deliver`), the channel registry, `Spree::NotificationChannel::Base`, `DeliverJob` and the `sms` channel with its **Tencent Cloud SMS** transport; `spree/extensions/spree_verification_codes` ships the code and the PIN. The SMS transport is the vendor the author chose (腾讯云), the notifications plan's Open Question 1 is answered, and its `email` and `wechat_subscribe` channels remain for its own run. The plan's own claim that "the plan's one core change is the seam" is true of *behaviour*: core also gains two registry keys (`payment_verifications`, `notification_channels`), two store preferences for a code's life and attempts, and a rate-limit preference in `spree/api` — all named here because a reader counting core edits should find them.

**The tender is guarded by a registry, and the phone write by a service.** `Spree.payment_verifications` holds objects answering `required?(order:)` and `verify(order:, proof:)`; `Spree::StoreCredits::Apply` consults them once, before it writes, and the refusal travels as a `Spree::PaymentVerification::Refusal` whose `kind` the API maps onto `payment_pin_required` / `_invalid` / `_locked`. `balance_not_password` is the same question asked for a client, answered on the order payload and on the price preview from one method (`Purchase::StoreCredits#balance_requires_verification?`). The phone write needs a second, smaller seam — `Spree::Dependencies.customer_phone_verification_service` — because a *deployment* decides whether a number needs proof: with none named, the customer write is exactly what upstream ships, and the gem names `Spree::VerificationCodes::VerifyPhone` when it is installed. **The erasure request uses the same seam**, which is how a WeChat account with no password can be closed by a code and still be refused on a deployment that cannot check one.

**Readings taken while building, each a deviation from the plan's text rather than from its intent.** The **check does not consume and takes `purpose` optionally** — the client's own check posts only `{phone, code}`, so the family is enforced where the code is *spent* (the PIN write, the phone write, the erasure), and a caller that names one gets the stricter answer. **`DELETE /payment_pin` closes the prompt rather than destroying the credential** — the client's 关闭支付密码 is followed by a switch write, and the three states it renders (no PIN / PIN required / PIN not required) only exist if the row survives; erasure is what destroys it. **A send is one route with a `purpose`**, and the PIN's family is gated further: a signed-in customer's own number only, because an endpoint anybody could aim at any number is a way to spend this store's template on strangers. **The code travels through the job's arguments** — `spree_notifications` enqueues, and the enqueue is a row in the queue for as long as the job lives. That is the one place a plaintext code exists outside the request and the vendor call, and it is stated rather than hidden: the table keeps its digest, the API never returns or logs one, and **manual QA without an SMS account runs through `spree:verification_codes:issue`, which prints a code and refuses in production**.

**The captcha stays deferred**, as the plan's Open Question says: the send accepts `ticket` and does not validate it, because nothing is configured to validate it against and refusing the client's own payload would break a send that is safe without it. The rate limiting is the defence either way — per IP through the API's own mechanism, and per number through a cache bucket the API's limit cannot see.

**What the review round changed, and what it left.** A full review of the branch (three angles, then a verification pass) found twenty items; the substantive ones are fixed and each is a rule rather than a patch: a refusal writes a failed-attempt counter, so the customer routes ask the tender *before* they take a lock — a counter written inside a transaction the refusal then rolls back is a lockout that never engages; only a *presented* proof counts as a guess, so an operator applying stored value or an internal recomputation is refused without being counted against; codes, lookups and the per-number bucket are all store-scoped, because a store column that is written and never read is a tenancy the next reader would believe in; a code's newest row is the only one usable, spent or not, and spending takes a row lock; erasure reaches this gem's rows from the anonymize flow's own hooks, deleting rather than soft-deleting, because a paranoid row keeps the phone it was meant to forget; the notification job does not log its arguments, which is where the plaintext code would otherwise sit for the life of the log; the voice channel is refused until a deployment has a transport for it; and the vendor call gets a circuit breaker beside its timeouts, since a send is deliberately not retried. **The other way a row is orphaned is covered too:** the erasure flow has hooks, and a customer deleted outright — the admin API can, and so can an app — is heard through the bus (`customer.deleted`), because a gem may not add a `dependent: :destroy` association to a core model. **Left as recorded rather than fixed:** the PIN lookup is still one query per order on the order payload's own verdict (the field is what the client reads, and preloading it needs an association a gem may not add to a core model), and no voice transport exists yet.

## 2026-09-21 (the shared floor) — The grant row is core's, and a kind is a class rather than a row

Plan: `6.1-grant-and-benefit-primitive.md`, built. The four rulings of 2026-09-18 stand; this records what building them settled, and one place where the built shape reads differently from the plan's words.

**A kind is a registered class, not an STI row.** The plan says a consumer's kind is "a gem-local subclass of the core model" and sends the reader to `Spree::CommissionRule`'s registry. Built, it is the notifications channel's shape instead: `Spree::Grants::Kind` is a plain class a consumer's gem subclasses, `Spree.grant_kinds` holds the classes, and the row's `kind` column stores the class's `api_type`. Three reasons. Every typed family in this repository discriminates on a column named `type` holding a Ruby class name, and this row has no `type`: the plan names the column `kind`, and its key reads as a vocabulary value — `(store_id, kind, idempotency_key)` — rather than as a constant. Rails' STI would need `inheritance_column = 'kind'`, which no model here does. And the consumer that says in writing what it builds collides with an STI subclass of the same name: `6.1-membership-tiers-and-rights.md` gives its grant a side table at `spree_membership_grants` keyed to the shared row. So the kind answers as a class — `api_type`, `idempotency_key_for`, `consumable?`, `expires?`, `consume!` — and a kind's extra columns go in its own side table, which is the rule the plan already carried.

**The key is taken across deleted rows.** `grant!` looks its key up with `with_deleted` and answers the row that holds it, the model's uniqueness validation runs with `conditions: -> { with_deleted }`, and the unique index carries no partial predicate. The reason is coherence with the paranoid default scope: a validation that ignores deleted rows beside an index that does not turns a re-run after a cleanup into `RecordNotUnique` instead of a no-op. It is also the honest reading — the key says the debt was already recorded, and removing a row is not a reason to record it again — and it costs no MySQL special case, where a partial index would have needed the generated-column emulation `20260920000003_put_the_mysql_service_area_index_behind_its_predicate` had to add.

**The core edits the plan called free are three lines and one key.** The table and the model are new files, as ruled. The registry is not: `Spree.grant_kinds` is a member of `Spree::Core::Engine::Environment` in `engine.rb`, an initializer that assigns it, an accessor in `core.rb`, and one message key in core's `en.yml` for the validation that refuses an expiry on a kind that never expires. The plan's "edits no existing core file" is corrected here — every registry key this fork has added cost the same four edits.

**The service refuses; it does not perform.** `grant!` records and is idempotent; `claim!` gives an unclaimed row its holder and refuses one that already belongs to somebody else; `consume!` refuses what is not usable and otherwise hands the row to the kind, whose answer — refusal included — is the caller's; `revoke!` keeps the row and refuses what was consumed. Nothing here holds a balance, sums a ledger or issues an artifact, and no built-in kind ships today: `referral_reward` is `6.1-referral-invitations.md`'s and the annual gift is a membership right kind.

**The take is one statement, and the review round is why.** A grant is taken once even when two callers ask at the same moment, so the check and the write are one `UPDATE ... WHERE` over the row's own conditions: `Spree::Grant#consume!` and `#claim!` merge the `usable` scope and report whether their update matched, and the second caller is told it lost rather than both being told they won. The same reasoning closes the insert: `grant!` catches the unique index's refusal when two callers pass the same key and answers the row that won. **Accepted trade-off:** a kind consumed in part keeps its own locking — the primitive's guarantee covers the take it performs, and the plan's rule that a partial kind consumes through the plan that owns the thing is unchanged.

**Two refusals a consumer will meet, and the rule behind them.** `grant!` answers `:already_recorded` when the row holding the key is one somebody removed, and `:key_belongs_to_another_customer` when the caller names a customer the existing row is not on. The second is the sharp one: the key is the whole identity of a debt, so a kind that owes the same thing to several customers must build its key from the customer — a campaign-wide key means the first customer takes the debt and everybody else is refused. The membership plan's per-right period key is per customer by construction; the coupon wallet's is the code itself; the points lot's is its source.

## 2026-09-22 (the ledger's row) — One row for every balance movement, and a reversal is a pair

Plan: `6.1-ledger-primitive.md`, step 1 built. The rulings of 2026-09-18 stand; this records what building them settled.

**The snapshot is the account's own answer, and nil is an answer.** `Spree::Ledger.record!` writes `balance_after` from `account.ledger_balance`, so the account decides whether a snapshot belongs on its rows: a points account answers its balance (it has no balance column of its own — its balance *is* its history), and a gift card answers nil, because its `amount_used` is authoritative and a snapshot here would be the second source of truth its plan forbids. The contract is two methods — `ledger_unit` and `ledger_balance` — and an account that answers neither is refused by name, with the missing method spelled out, rather than writing a row nobody can read a balance from.

**The reversal's sign is not the caller's to choose, and two layers say so.** `Spree::Ledger.reverse!` writes the negative counterpart and points it at what it undoes; `record!(reverses:)` exists for a producer with a source of its own (the refund rather than the order it refunds) and forces the opposite sign and takes the unit from the reversed entry, so the service cannot produce a mismatched pair. The row states the same rule for anything that writes it another way — a console, an import, a fixture — and refuses an entry that reverses another account's movement, one in another unit, or one moving the same way. The account rule is the one the service cannot guarantee by construction, so it is also the one a caller meets as a refusal. Three families each inventing their own reversal convention is the money bug with the long fuse this plan exists to remove.

**Append-only is enforced rather than promised.** `Spree::LedgerEntry#readonly?` is true for a persisted row, so `update` raises `ActiveRecord::ReadOnlyRecord` instead of quietly editing money. There is no `status`, no soft delete and no update path anywhere in the primitive: a wrong entry is reversed by its pair.

**A key already held by another movement is refused, not answered.** The producer's key is the whole identity of a movement, so `record!` compares the row it finds with the movement asked for (`kind`, `unit`, `amount` and what it reverses) and answers `:key_reused` when they differ. The review round is why: a key built too coarsely — the distribution plan's per-order key where one order earns twice, a points key naming only the account and the month — would otherwise return `success` with no row written and no error anywhere, and `reverse!` handed the original entry's key would answer with the positive original as a successful clawback. **The rule a consumer implements:** build the key from every dimension that makes the movement distinct, because the primitive can only tell two movements apart by comparing what it was given.

**The history index leads with the account, not the store.** The plan sketched `(store_id, account_type, account_id, occurred_at)`; built, it is `(account_type, account_id, occurred_at)`, because every reader this primitive exposes constrains the account, and a store column sitting between the account and the time leaves the ordering on MySQL and SQLite to a full sort. A store-wide report is a consumer's read, and an index for it is a one-line migration when one appears.

**The snapshot looks forward, so the primitive adds the movement.** `balance_after` promises where the balance stood *after* the entry, while `ledger_balance` can only answer where it stands *now* — the entry is not written yet when the account is asked. The two are one movement apart, so `record!` adds the amount to the account's answer rather than writing it verbatim; a live check on PostgreSQL is what turned this up (a history-only account snapshotted the balance *before* its own row, which is a systematic one-entry error for the points plan, the one consumer whose balance is its history). **The rule a consumer implements:** `ledger_balance` answers the balance as of now, counting nothing this call is about; an account that keeps no balance of its own answers nil and gets no snapshot.

**Two Open Questions stay where they were.** Where the distribution plan's `rate`, `level` and payout state land (they are payout facts, not balance movements) and whether a commission's inputs live in the entry's metadata or on the payout row are that plan's to settle before its migration step — this primitive deliberately defines no payout table and no commission vocabulary.

## 2026-09-22 (the points balances) — Two rates on the store, and the reason key is the entry's kind

Plans: `6.1-points-and-growth-value.md` (steps 1 and 2 built), riding the grant and ledger primitives built the day before.

**Two store-level rates, and no seller override (ruled 2026-09-22).** The plan's one open question was where the two figures live: `points_earn_rate` (每消费N元得1积分) and `points_redeem_rate` (每N个可抵扣1元) are now preferences on `Spree::Store`, beside `points_expiry_warning_days`. Two rather than one because the client shows two and a single preference would conflate them. **Accepted trade-off:** a seller-level rate — which the seller-scoped member shelf might have wanted — is a third mechanism deliberately not built; the client reads one store-wide figure, so nothing yet proves the need.

**The run is steps 1 and 2, not the whole plan.** The author chose the slice: the balances and their history, with earning, the catalogue, the issue steps and the deduction to follow in the plan's own order. The reason is size — the full plan is roughly three times the primitives that preceded it, and a review of that diff would be a review of nothing.

**A movement's reason is the ledger entry's `kind`.** The plan said the reason rides the entry's metadata; built, the entry's `kind` *is* the reason's key and the label is resolved from `spree_point_reasons` at read time. The primitive already has the column and defines it as the account's own vocabulary, so one vocabulary carries the movement and its label rather than two that can disagree — and a key the operator's list does not hold yet still reads, as itself, which is what lets the review extension earn before its reason row is added.

**The two balances are always answered, and neither is stored.** The balances read synthesises a zero row for a kind the customer has no account for — a customer whose balance is zero has a balance — which keeps a read a read. And the balance itself is derived (the sum of usable lots' `remaining`), so `Spree::PointAccount#balance` answers zero for an unsaved row and an expiry needs no job. The account row exists for the lock a spend takes and for `lifetime_earned`, and for nothing else.

**The growth-value account is not spendable, and the credit path refuses a date on it.** 成长值 never lapses and is never spent; the first is enforced where the date is written (`:growth_value_does_not_expire`) and the second where the spend is attempted (`:not_a_spendable_balance`), rather than by a flag on the account.

**This gem's endpoints are documented where the gem is, not in the generated spec.** `spree/api/config/initializers/typelizer.rb` scans the api gem's serializer directory and rswag runs inside the api dummy app, which does not load an extension — so the two points reads, like the payment-PIN endpoints before them, produce no SDK types and no OpenAPI entry. They are documented in `spree/extensions/spree_points/README.md`, the same rule the docs tree already follows for the fork's own gems (2026-09-19). Extending the scanner was considered and declined for now: it would change what every extension's serializers emit, in a step whose subject is two reads.

## 2026-09-22 (earning) — The ledger row's two columns, and the goods' own points in a Custom Field

Plan: `6.1-points-and-growth-value.md`, step 3 built. Three rulings of this run, one reading that turned up in core's own comments, and one boundary the reversal draws.

**The seller and the order are columns on the shared ledger row (ruled 2026-09-22).** `spree_ledger_entries` gains `seller_id` and `order_id`, and `Spree::Ledger.record!` takes them beside `source`. The plan's own words are "two columns, not a derivation", and the reason is that `source` points at reviews and adjustments as readily as at orders — neither of which has a seller. It also answers `6.1-ledger-primitive.md`'s Open Question 2: the seller lives on the shared row, so the distributor's earnings and a card's movements record it the same way. Where a movement has no single seller — a points earn on an order whose goods come from several — the column stays nil rather than guessing.

**商品积分 is a product Custom Field (ruled 2026-09-22).** Two names in the `points` namespace — `points.extra_per_unit` and `points.line_cap` — which an operator fills per good. Nothing is stored on the line: the line's own figure is the good's amount times the quantity, held to the cap, and it is derived where it is read. A merchant-managed number is a Custom Field by the repository's own rule, and this keeps a second copy out of the schema.

**成长值 uses the earn's own formula (ruled 2026-09-22).** The plan's table of inputs does not distinguish the two balances, so one number is earned and credited to both. If the operator later wants growth value to follow the paid amount one to one, that is a second rate in the same formula, not a second formula.

**The event is the gate, not `order.paid?`.** Core's own comment (`spree/core/app/models/spree/payment/custom_events.rb:39`) records that `order.paid` fires from the payment's commit *before* `payment_total` is written, so a service that asked `order.paid?` inside that subscriber would silently earn nothing. `Spree::Points::Earning` therefore takes the caller's word: the `order.paid` event is what makes an order paid, and the calculator computes.

**A reversal may touch a balance that is never spent, and never more than the balance holds.** 成长值 is clawed back on a refund — the plan's own words — so the spend path's "not a spendable balance" refusal is relaxed for a movement that carries a reversal pointer: a correction of the history is not a spend. And a clawback is held to three ceilings at once — the return's share, what the earn has not already given back, and what the balance actually holds — because points a customer has already spent cannot be taken back without a negative balance, which this ledger has no way to write. **Accepted trade-off:** a fully-spent balance means the store loses that part of the clawback rather than recording a debt.

## 2026-09-22 (the points shop) — A registry the gem owns, and a kind that is a class

Plan: `6.1-points-and-growth-value.md`, step 4 built: `spree_point_products` and the catalogue reads.

**A good's kind is a registered class, and the registry is the gem's own.** `Spree::PointProduct` registers subclasses through `SpreePoints.point_product_types`, with `Spree::PointProducts::{Coupon,VipCard,Good}` as the built-ins — `api_type` `coupon`, `vip_card`, `good`. The plan called the discriminator `jxeType` and gave it a string column; built, it is Rails' own `type` column with `api_type` on the wire, which is what every other typed family in this repository does. The registry lives in the gem rather than as another key in core's `Environment`: only a points shop has goods, and a registry is not a generic capability.

**The keys are concrete columns, which is the point of the shape.** `coupon_campaign_id`, `customer_group_id` and `variant_id` sit on the one table and each kind validates its own, so a row cannot name something no plan can issue — `Spree::CommissionLine`'s rule, adopted rather than re-argued. `coupon_campaign_id` is a **forward reference**: the coupon wallet owns that table and is not built, so the column is validated rather than the association, which would resolve a class that does not exist. The client's `coupon: { … }` payload is deliberately absent from the read until that plan supplies the shape, rather than invented here.

**Eligibility is stored on the row and enforced where it can be.** `limit_user_type`, `subscribe_site_wechat`, `we_chat_qr_code` and `site_equal` are columns, and the reads answer what a deployment can answer today — the seller shelf follows the request's resolved seller. **Accepted trade-off:** a good restricted to members or to WeChat subscribers is *shown* to everyone until the membership and notification plans can answer those conditions, and refused at redemption rather than hidden at read time — hiding needs a fact this build does not have, and a catalogue that silently drops goods is the harder failure to explain.

**Enforced, not just stated.** Review turned the shape into a rule the row keeps itself: a kind declares its one payload column through `issues`, and the base then requires it and forbids the other two — so a row carrying two things is refused rather than left for the issue step to disambiguate — while `type` is validated against the registry read off the column, so a row written with a class name no registry holds is refused too. The category strip is read off the shelf rather than off the page, because a facet narrowed by the filter it exists to describe leaves one tab on the strip.

**A member shelf that names no site is refused, not narrowed.** `audience=member` is answered for the request's resolved seller — that site's goods beside the store's — and a request that names no site is refused (422 `parameter_invalid`) rather than served the store's own shelf: a shelf quietly missing a seller's goods reads as a shelf with nothing in it, which is the failure `SellerResolution` already refuses for an unresolvable `siteId`. The same argument put `X-Spree-Seller-Id` into the shared Vary list beside the channel (`spree/api`'s `HttpCaching`), so a guest response answered for one site is not served as another's.
